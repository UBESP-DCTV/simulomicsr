% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{cache_init}
\alias{cache_init}
\title{Inizializza una cache locale append-only (JSONL + indice SQLite)}
\usage{
cache_init(dir, namespace = "default")
}
\arguments{
\item{dir}{directory esistente o creabile dove vivono i file di cache}

\item{namespace}{nome corto della partizione di cache (es. \code{"stage1"})}
}
\value{
oggetto \code{cache} (list opaca) usato dalle altre funzioni \verb{cache_*}
}
\description{
La cache vive in una directory \code{dir}. Per ogni \code{namespace} (es.
\code{"stage1"}, \code{"stage2"}) crea due file: \verb{<namespace>.jsonl} (record
append-only, una riga JSON per put) e \verb{<namespace>.sqlite} (indice
chiave → offset/byte_size dell'ultima versione).
}
\details{
Idempotente: chiamarla ripetutamente sulla stessa dir non altera lo stato.
}
\keyword{internal}
