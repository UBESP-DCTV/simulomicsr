# P3.5-B — Eval benchmark Stadio 2 sui 15 GSE — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Validare il classificatore Stadio 2 di simulomicsr (P3) contro gold binary `trtctr_EP` (xlsx) e contro RummaGEO sui 15 GSE già classificati. Produrre un Quarto report con 4 sezioni (binary accuracy, granularity disagreement enumeration, anchor coverage cross-studio, RummaGEO head-to-head). Cost LLM zero (riusa cache P3); evidence base n≈150-200 sample come prototipo per P3.5-A scaled benchmark successivo.

**Architecture:** Layer eval indipendente in R puro (no LLM). Riusa `study_designs_validated`, `comparisons_table`, fixture stage2-mini, e xlsx gold. Aggiunge fetch RummaGEO con strategia 3-livelli (API ufficiale → bulk dump → replica algoritmo interno fallback). Output finale: Quarto in `analysis/eval/p35-benchmark.Rmd`. Pipeline orchestrata via 7 nuovi target in `analysis/_targets.R`.

**Tech Stack:** R 4.5+, P1-P3 stack (`httr2`, `jsonlite`, `dplyr`, `tibble`, `tidyr`, `targets`, `tarchetypes`), in più `ggplot2` per i grafici del report e `rmarkdown`/`knitr` per la build Quarto. RummaGEO fetch via `httr2` (no nuove dipendenze).

---

## File Structure

| File | Responsabilità | LOC stimato |
|---|---|---|
| `R/eval-stage2.R` | `design_role_to_binary()` mapping (13→3), `eval_binary_accuracy()`, `eval_per_design_kind()`, `flag_granularity_disagreement()` | ~180 |
| `R/eval-rummageo.R` | `fetch_rummageo_signatures(gse, cache_dir)` con cache, `parse_rummageo_labels()` (signature → per-GSM control/treated), `rummageo_baseline_internal()` (K-means+keyword fallback) | ~250 |
| `tests/testthat/test-eval-stage2.R` | Test mapping + accuracy + granularity flag su esempi minimi | ~180 |
| `tests/testthat/test-eval-rummageo.R` | Test fetch + parse con cassette JSON mockate, fallback K-means su sample fittizi | ~140 |
| `tests/testthat/fixtures/rummageo-mock-GSE145941.json` | Cassette JSON per test offline | ~30 |
| `analysis/_targets.R` (estensione) | Target nuovi: `gold_table_subset`, `eval_stage2_gold_join`, `eval_stage2_metrics`, `rummageo_cache_dir`, `rummageo_signatures`, `rummageo_metrics`, `eval_p35_report` (tar_render) | ~110 (estende ~210→320) |
| `analysis/eval/p35-benchmark.Rmd` | Quarto report 4 sezioni: binary accuracy, granularity, anchor coverage, RummaGEO head-to-head | ~220 |

Totale nuovo codice: ~970 LOC + 350 LOC test. Tutto offline-buildable (eccetto Task 5 RummaGEO fetch live).

Sono **NON** in scope di P3.5-B (vedono spec §11):
- Scaling oltre i 15 GSE — P3.5-A
- Effect-size pooling REM (ADR-0006 Output 3) — P5
- Re-classification con modello arbitro — deferred
- Integrazione MetaHQ — deferred

---

## Task 1: Branch + dipendenze (ggplot2 + scales)

**Files:**
- Modifica: `DESCRIPTION`
- Modifica: `analysis/_targets_packages.R`
- Modifica: `renv.lock` (via `renv::snapshot()`)

- [ ] **Step 1.1: Verifica master pulito e tag P3 presente**

```bash
git status
git tag --list 'p3-*'
git rev-parse HEAD
```

Expected: working tree clean, `p3-stage2-complete` presente, HEAD su master post-merge P3.

- [ ] **Step 1.2: Crea branch `p3.5b-eval`**

```bash
git checkout -b p3.5b-eval
```

Expected: `Switched to a new branch 'p3.5b-eval'`.

- [ ] **Step 1.3: Aggiorna `DESCRIPTION` con `ggplot2` + `scales` in Suggests**

Apri `DESCRIPTION` e nella sezione `Suggests:` aggiungi `ggplot2,` e `scales,` in ordine alfabetico (rispettivamente prima e dopo posizione opportuna). Non in Imports perché sono usati solo nel report Quarto, non nel runtime libreria.

- [ ] **Step 1.4: Aggiorna `analysis/_targets_packages.R`**

Aggiungi `library(ggplot2)` dopo `library(rentrez)`. Risultato finale del file:

```r
# Pacchetti caricati nei worker `targets`.
#
# Allineato con `Imports`/`Suggests` di ../DESCRIPTION (vedi
# docs/decisions/0002-struttura-research-compendium.md).

library(tibble)
library(dplyr)
library(tidyr)
library(readxl)
library(rentrez)
library(ggplot2)
library(rmarkdown)
library(tarchetypes)
library(simulomicsr)
```

- [ ] **Step 1.5: Installa nuove dipendenze in renv**

```r
renv::install(c("ggplot2", "scales"))
```

- [ ] **Step 1.6: Snapshot renv**

```r
renv::snapshot(type = "implicit", prompt = FALSE)
```

Verifica: `grep -E '"ggplot2"|"scales"' renv.lock | head` deve mostrare entrambi.

- [ ] **Step 1.7: Commit**

```bash
git add DESCRIPTION analysis/_targets_packages.R renv.lock
git commit -m "P3.5b Task 1: ggplot2 + scales in Suggests + targets_packages"
```

---

## Task 2: `R/eval-stage2.R` — `design_role_to_binary()` mapping

**Files:**
- Crea: `R/eval-stage2.R`
- Crea: `tests/testthat/test-eval-stage2.R`

`design_role_to_binary(role)`: mappa il vocabolario design_role v3 (13 valori) al binary `treated`/`control`/`NA` per il confronto con gold xlsx. Implementa la tabella spec §5 (estensione spec v5 §6.2).

- [ ] **Step 2.1: Scrivi i test (failing) `tests/testthat/test-eval-stage2.R`**

```r
test_that("design_role_to_binary mappa correttamente i 13 valori", {
  # treated
  expect_equal(design_role_to_binary("perturbed"), "treated")
  expect_equal(design_role_to_binary("case"), "treated")
  expect_equal(design_role_to_binary("secondary_arm"), "treated")
  # control
  expect_equal(design_role_to_binary("vehicle_control"), "control")
  expect_equal(design_role_to_binary("untreated_control"), "control")
  expect_equal(design_role_to_binary("negative_genetic_control"), "control")
  expect_equal(design_role_to_binary("negative_inducer_control"), "control")
  expect_equal(design_role_to_binary("baseline_t0"), "control")
  expect_equal(design_role_to_binary("comparison"), "control")
  # NA (escluso da metrica)
  expect_true(is.na(design_role_to_binary("bystander")))
  expect_true(is.na(design_role_to_binary("positive_control")))
  expect_true(is.na(design_role_to_binary("excluded")))
  expect_true(is.na(design_role_to_binary("unclear")))
})

test_that("design_role_to_binary errors on unknown role", {
  expect_error(
    design_role_to_binary("not_a_real_role"),
    class = "simulomicsr_invalid_design_role"
  )
})

test_that("design_role_to_binary vectorized: applica element-wise", {
  roles <- c("perturbed", "vehicle_control", "bystander", "case")
  expected <- c("treated", "control", NA_character_, "treated")
  expect_equal(design_role_to_binary(roles), expected)
})
```

- [ ] **Step 2.2: Run test, vedi fallire**

```bash
Rscript --vanilla -e 'devtools::test(filter = "eval-stage2")'
```

Expected: tutti FAIL — `design_role_to_binary` non definita.

- [ ] **Step 2.3: Implementa `design_role_to_binary()` in `R/eval-stage2.R`**

```r
#' Mappa design_role v3 (13 valori) al binary trtctr_predicted
#'
#' Implementa la tabella di mapping spec sec.6.2 estesa ai 13 valori
#' del vocabolario design_role. NA = sample escluso dal calcolo accuracy
#' (non un errore, e' una scelta esplicita).
#'
#' Estensione vs spec sec.6.2 originale:
#' - bystander -> NA (paracrine, non-direttamente perturbed)
#' - negative_inducer_control -> control (no-Dox arm di sistema inducibile)
#' - secondary_arm -> treated (trattamento alternativo)
#'
#' @param role character vector di design_role values
#' @return character vector con valori "treated", "control" o NA_character_
#' @export
design_role_to_binary <- function(role) {
  if (length(role) == 0L) return(character(0))
  vapply(role, .design_role_to_binary_one, character(1))
}

#' @noRd
.design_role_to_binary_one <- function(r) {
  if (is.na(r)) return(NA_character_)
  switch(
    r,
    perturbed = "treated",
    case = "treated",
    secondary_arm = "treated",
    vehicle_control = "control",
    untreated_control = "control",
    negative_genetic_control = "control",
    negative_inducer_control = "control",
    baseline_t0 = "control",
    comparison = "control",
    bystander = NA_character_,
    positive_control = NA_character_,
    excluded = NA_character_,
    unclear = NA_character_,
    rlang::abort(
      sprintf("design_role non riconosciuto: '%s'", r),
      class = "simulomicsr_invalid_design_role"
    )
  )
}
```

- [ ] **Step 2.4: Reinstall + ri-run test**

```bash
Rscript --vanilla -e 'devtools::install(upgrade = "never", quiet = TRUE)' || R CMD INSTALL .
Rscript --vanilla -e 'devtools::test(filter = "eval-stage2")'
```

Expected: 3 PASS (mapping completo + error + vectorized).

- [ ] **Step 2.5: Document + commit**

```bash
Rscript --vanilla -e 'devtools::document()'
git add R/eval-stage2.R tests/testthat/test-eval-stage2.R \
        man/design_role_to_binary.Rd NAMESPACE
git commit -m "P3.5b Task 2: design_role_to_binary() mapping 13->3 + 3 test"
```

---

## Task 3: `R/eval-stage2.R` — `eval_binary_accuracy()` + `eval_per_design_kind()`

**Files:**
- Modifica: `R/eval-stage2.R`
- Modifica: `tests/testthat/test-eval-stage2.R`

`eval_binary_accuracy(gold, predicted)` ritorna `list(n, accuracy, sensitivity, specificity, f1, confusion_matrix)`. Esclude NA da entrambi i lati.

`eval_per_design_kind(gold_join_df)` riceve un tibble con colonne `gold_binary`, `predicted_binary`, `design_kind`, computa accuracy per ogni design_kind.

- [ ] **Step 3.1: Aggiungi test in `tests/testthat/test-eval-stage2.R`**

```r
test_that("eval_binary_accuracy: tutti corretti", {
  gold <- c("treated", "treated", "control", "control")
  pred <- c("treated", "treated", "control", "control")
  res <- eval_binary_accuracy(gold, pred)
  expect_equal(res$n, 4L)
  expect_equal(res$accuracy, 1.0)
  expect_equal(res$sensitivity, 1.0)
  expect_equal(res$specificity, 1.0)
  expect_equal(res$f1, 1.0)
})

test_that("eval_binary_accuracy: casi misti", {
  gold <- c("treated", "treated", "control", "control")
  pred <- c("treated", "control", "control", "treated")  # 1 FP, 1 FN
  res <- eval_binary_accuracy(gold, pred)
  expect_equal(res$n, 4L)
  expect_equal(res$accuracy, 0.5)
})

test_that("eval_binary_accuracy: NA esclusi correttamente", {
  gold <- c("treated", "treated", "control", "control", NA)
  pred <- c("treated", "treated", "control", NA, "treated")
  res <- eval_binary_accuracy(gold, pred)
  expect_equal(res$n, 3L)  # solo le coppie complete
  expect_equal(res$accuracy, 1.0)  # tutti i 3 disponibili sono corretti
})

test_that("eval_binary_accuracy: confusion matrix struttura corretta", {
  gold <- c("treated", "treated", "control", "control")
  pred <- c("treated", "control", "control", "treated")
  res <- eval_binary_accuracy(gold, pred)
  expect_true(is.matrix(res$confusion_matrix) || is.table(res$confusion_matrix))
  expect_setequal(rownames(res$confusion_matrix), c("treated", "control"))
  expect_setequal(colnames(res$confusion_matrix), c("treated", "control"))
})

test_that("eval_per_design_kind: breakdown per kind", {
  df <- tibble::tibble(
    gold_binary = c("treated", "treated", "control", "control",
                    "treated", "control"),
    predicted_binary = c("treated", "control", "control", "control",
                         "treated", "treated"),
    design_kind = c("time_course", "time_course", "time_course", "time_course",
                    "factorial", "factorial")
  )
  res <- eval_per_design_kind(df)
  expect_true(tibble::is_tibble(res))
  expect_setequal(res$design_kind, c("time_course", "factorial"))
  tc_row <- res[res$design_kind == "time_course", ]
  expect_equal(tc_row$n, 4L)
  expect_equal(tc_row$accuracy, 0.75)  # 3/4 corretti
  fc_row <- res[res$design_kind == "factorial", ]
  expect_equal(fc_row$n, 2L)
  expect_equal(fc_row$accuracy, 0.5)  # 1/2 corretti
})
```

- [ ] **Step 3.2: Run test, vedi fallire**

Expected: 5 nuovi FAIL.

- [ ] **Step 3.3: Implementa in `R/eval-stage2.R`**

Aggiungi al fondo:

```r
#' Calcola binary accuracy per un vettore di predicted vs gold
#'
#' Coppie con NA in gold OR predicted vengono escluse dal calcolo.
#' Sensitivity/specificity calcolate considerando "treated" come la classe
#' positiva, "control" come la classe negativa.
#'
#' @param gold character vector (treated/control/NA)
#' @param predicted character vector (treated/control/NA), stessa lunghezza
#' @return list con campi n, accuracy, sensitivity, specificity, f1,
#'   confusion_matrix
#' @export
eval_binary_accuracy <- function(gold, predicted) {
  stopifnot(length(gold) == length(predicted))
  keep <- !is.na(gold) & !is.na(predicted)
  g <- gold[keep]
  p <- predicted[keep]
  n <- length(g)
  if (n == 0L) {
    return(list(
      n = 0L, accuracy = NA_real_, sensitivity = NA_real_,
      specificity = NA_real_, f1 = NA_real_,
      confusion_matrix = .empty_confusion_matrix()
    ))
  }
  # Forza factor levels uniformi
  g_f <- factor(g, levels = c("control", "treated"))
  p_f <- factor(p, levels = c("control", "treated"))
  cm <- table(predicted = p_f, gold = g_f)

  tp <- cm["treated", "treated"]
  tn <- cm["control", "control"]
  fp <- cm["treated", "control"]
  fn <- cm["control", "treated"]
  accuracy <- (tp + tn) / n
  sensitivity <- if (tp + fn > 0) tp / (tp + fn) else NA_real_
  specificity <- if (tn + fp > 0) tn / (tn + fp) else NA_real_
  precision <- if (tp + fp > 0) tp / (tp + fp) else NA_real_
  f1 <- if (!is.na(precision) && !is.na(sensitivity) &&
            (precision + sensitivity) > 0) {
    2 * precision * sensitivity / (precision + sensitivity)
  } else NA_real_

  list(
    n = n, accuracy = accuracy,
    sensitivity = sensitivity, specificity = specificity, f1 = f1,
    confusion_matrix = cm
  )
}

#' @noRd
.empty_confusion_matrix <- function() {
  m <- matrix(0L, nrow = 2, ncol = 2,
              dimnames = list(predicted = c("control", "treated"),
                              gold = c("control", "treated")))
  as.table(m)
}

#' Breakdown binary accuracy per design_kind
#'
#' @param df tibble con colonne gold_binary, predicted_binary, design_kind
#' @return tibble con una riga per design_kind: design_kind, n, accuracy,
#'   sensitivity, specificity
#' @export
eval_per_design_kind <- function(df) {
  stopifnot(all(c("gold_binary", "predicted_binary", "design_kind") %in% names(df)))
  kinds <- unique(df$design_kind)
  rows <- lapply(kinds, function(k) {
    sub <- df[df$design_kind == k, ]
    metrics <- eval_binary_accuracy(sub$gold_binary, sub$predicted_binary)
    tibble::tibble(
      design_kind = k,
      n = metrics$n,
      accuracy = metrics$accuracy,
      sensitivity = metrics$sensitivity,
      specificity = metrics$specificity,
      f1 = metrics$f1
    )
  })
  dplyr::bind_rows(rows)
}
```

- [ ] **Step 3.4: Reinstall + ri-run test**

Expected: 5 nuovi PASS, totale 8 PASS in eval-stage2.

- [ ] **Step 3.5: Document + commit**

```bash
Rscript --vanilla -e 'devtools::document()'
git add R/eval-stage2.R tests/testthat/test-eval-stage2.R \
        man/eval_binary_accuracy.Rd man/eval_per_design_kind.Rd NAMESPACE
git commit -m "P3.5b Task 3: eval_binary_accuracy() + eval_per_design_kind() + 5 test"
```

---

## Task 4: `R/eval-stage2.R` — `flag_granularity_disagreement()`

**Files:**
- Modifica: `R/eval-stage2.R`
- Modifica: `tests/testthat/test-eval-stage2.R`

`flag_granularity_disagreement(gold_join_df)` enumera i sample dove simulomicsr ha prodotto un design_role più granulare del binary. Output: tibble con colonne `geo_accession`, `series_id`, `design_role`, `gold_binary`, `predicted_binary`, `granularity_kind`.

- [ ] **Step 4.1: Aggiungi test**

```r
test_that("flag_granularity_disagreement: enumera negative_inducer_control", {
  df <- tibble::tibble(
    geo_accession = c("GSM1", "GSM2", "GSM3"),
    series_id = c("GSE1", "GSE1", "GSE1"),
    design_role = c("perturbed", "negative_inducer_control", "vehicle_control"),
    gold_binary = c("treated", "control", "control"),
    predicted_binary = c("treated", "control", "control")
  )
  res <- flag_granularity_disagreement(df)
  # Solo negative_inducer_control e' "more granular than binary"
  expect_equal(nrow(res), 1L)
  expect_equal(res$geo_accession, "GSM2")
  expect_equal(res$granularity_kind, "negative_inducer_control")
})

test_that("flag_granularity_disagreement: enumera baseline_t0, secondary_arm, bystander", {
  df <- tibble::tibble(
    geo_accession = paste0("GSM", 1:5),
    series_id = rep("GSE1", 5),
    design_role = c("perturbed", "baseline_t0", "secondary_arm", "bystander", "vehicle_control"),
    gold_binary = c("treated", "control", "treated", NA_character_, "control"),
    predicted_binary = c("treated", "control", "treated", NA_character_, "control")
  )
  res <- flag_granularity_disagreement(df)
  expect_setequal(res$granularity_kind,
                  c("baseline_t0", "secondary_arm", "bystander"))
})

test_that("flag_granularity_disagreement: ritorna 0 righe se nessuna granularita'", {
  df <- tibble::tibble(
    geo_accession = c("GSM1", "GSM2"),
    series_id = rep("GSE1", 2),
    design_role = c("perturbed", "vehicle_control"),
    gold_binary = c("treated", "control"),
    predicted_binary = c("treated", "control")
  )
  res <- flag_granularity_disagreement(df)
  expect_equal(nrow(res), 0L)
})
```

- [ ] **Step 4.2: Run test, vedi fallire**

Expected: 3 nuovi FAIL.

- [ ] **Step 4.3: Implementa in `R/eval-stage2.R`**

Aggiungi al fondo:

```r
# Vocabolario design_role che e' "piu' granulare del binary":
# il binary mapping non perde solo informazione (treated/control), ma il
# design_role originale dice qualcosa che il gold non distingue.
.GRANULAR_DESIGN_ROLES <- c(
  "negative_inducer_control",  # no-Dox arm di sistema inducibile
  "baseline_t0",                # t=0h time course
  "secondary_arm",              # trattamento alternativo
  "bystander"                   # cellule paracrine
)

#' Enumera i sample dove simulomicsr ha prodotto un design_role piu'
#' granulare del binary gold xlsx
#'
#' Sample con design_role in negative_inducer_control / baseline_t0 /
#' secondary_arm / bystander forniscono informazione che il binary gold
#' non puo' rappresentare. Questi NON sono errori — sono valore aggiunto.
#'
#' @param df tibble con colonne geo_accession, series_id, design_role,
#'   gold_binary, predicted_binary
#' @return tibble con righe filtrate ai casi granulari, piu' colonna
#'   `granularity_kind` (= design_role)
#' @export
flag_granularity_disagreement <- function(df) {
  stopifnot(all(c("geo_accession", "series_id", "design_role",
                  "gold_binary", "predicted_binary") %in% names(df)))
  out <- df[df$design_role %in% .GRANULAR_DESIGN_ROLES, ]
  out$granularity_kind <- out$design_role
  out
}
```

- [ ] **Step 4.4: Reinstall + ri-run test**

Expected: 3 nuovi PASS. Totale 11 PASS in eval-stage2.

- [ ] **Step 4.5: Document + commit**

```bash
Rscript --vanilla -e 'devtools::document()'
git add R/eval-stage2.R tests/testthat/test-eval-stage2.R \
        man/flag_granularity_disagreement.Rd NAMESPACE
git commit -m "P3.5b Task 4: flag_granularity_disagreement() + 3 test"
```

---

## Task 5: `R/eval-rummageo.R` — fetch + parse signatures (con cache)

**Files:**
- Crea: `R/eval-rummageo.R`
- Crea: `tests/testthat/test-eval-rummageo.R`
- Crea: `tests/testthat/fixtures/rummageo-mock-GSE145941.json`

Esplora l'API RummaGEO, implementa fetch + parse + cache. **Esplorativo nel primo step** — l'API è da scoprire al runtime via WebFetch / curl. Documenta i findings nel codice come comment e nel report.

- [ ] **Step 5.1: Esplora l'API RummaGEO**

Run da bash:
```bash
# Tentativo 1: API GraphQL standard
curl -s -X POST 'https://rummageo.com/graphql' \
  -H 'Content-Type: application/json' \
  -d '{"query": "{ __schema { types { name } } }"}' | head -200

# Tentativo 2: REST endpoint per signature search per GSE
curl -s 'https://rummageo.com/api/signatures?gse=GSE145941' | head -200

# Tentativo 3: HTML scrape della pagina GSE specifica
curl -s 'https://rummageo.com/study/GSE145941' | head -100
```

Documenta i findings in un commento all'inizio di `R/eval-rummageo.R`. Se nessuna delle 3 funziona pulitamente, vai al fallback C (algoritmo replica) — vedi Task 6.

- [ ] **Step 5.2: Crea cassette mock `tests/testthat/fixtures/rummageo-mock-GSE145941.json`**

Sulla base dei findings di 5.1, crea un mock JSON realistico. Esempio se l'API ritorna signatures con `treated_samples` + `control_samples`:

```json
{
  "gse": "GSE145941",
  "signatures": [
    {
      "id": "GSE145941__1",
      "label": "10Gy_irradiated_vs_0Gy",
      "treated_samples": ["GSM4340021", "GSM4340022", "GSM4340024", "GSM4340025"],
      "control_samples": ["GSM4340018", "GSM4340019", "GSM4340020", "GSM4340023"]
    }
  ]
}
```

(Adatta i GSM ID ai sample reali della fixture P3 — leggi `inst/extdata/stage2-fixtures-mini/GSE145941-sample-facts.json` per i veri GSM IDs.)

- [ ] **Step 5.3: Scrivi test (failing) `tests/testthat/test-eval-rummageo.R`**

```r
test_that("fetch_rummageo_signatures: cache hit non chiama API", {
  cache_dir <- withr::local_tempdir()
  # Pre-popola cache
  fixture_path <- testthat::test_path("fixtures/rummageo-mock-GSE145941.json")
  fs::file_copy(fixture_path, fs::path(cache_dir, "GSE145941.json"))

  # Mock httr2 per fail-loud se chiamato
  withr::local_mocked_bindings(
    request = function(...) stop("should not be called when cache hit"),
    .package = "httr2"
  )
  out <- fetch_rummageo_signatures("GSE145941", cache_dir = cache_dir)
  expect_equal(out$gse, "GSE145941")
  expect_true(length(out$signatures) >= 1L)
})

test_that("parse_rummageo_labels: estrae control/treated per GSM", {
  data <- jsonlite::read_json(
    testthat::test_path("fixtures/rummageo-mock-GSE145941.json"),
    simplifyVector = FALSE
  )
  labels <- parse_rummageo_labels(data)
  expect_true(tibble::is_tibble(labels))
  expect_setequal(colnames(labels), c("geo_accession", "rummageo_label"))
  expect_setequal(labels$rummageo_label, c("treated", "control"))
})

test_that("fetch_rummageo_signatures errors on input invalido", {
  expect_error(
    fetch_rummageo_signatures("not-a-gse"),
    class = "simulomicsr_invalid_series_id"
  )
})
```

- [ ] **Step 5.4: Run test, vedi fallire**

Expected: 3 FAIL.

- [ ] **Step 5.5: Implementa `R/eval-rummageo.R` parte fetch+parse**

```r
# Findings RummaGEO API exploration (Task 5.1):
# [Da popolare dopo lo step 5.1; documenta endpoint scelti, schema risposta,
#  rate limits, fallback se non disponibile.]

#' Fetch RummaGEO signatures per un GSE con cache filesystem JSONL
#'
#' Strategia: API ufficiale rummageo.com (preferred). Cache su disco evita
#' re-fetch. Se API restituisce 404/500/timeout dopo retry, abort con
#' classe `simulomicsr_rummageo_unavailable`.
#'
#' @param gse GSE accession (es. "GSE145941")
#' @param cache_dir Directory di cache (NULL = disattivata)
#' @param api_base URL base API (default rummageo.com); override per test
#' @return list con campi gse, signatures
#' @export
fetch_rummageo_signatures <- function(gse, cache_dir = NULL,
                                      api_base = "https://rummageo.com") {
  if (!grepl("^GSE[0-9]+$", gse)) {
    rlang::abort(
      sprintf("series_id non valido: '%s'", gse),
      class = "simulomicsr_invalid_series_id"
    )
  }
  if (!is.null(cache_dir)) {
    cache_path <- fs::path(cache_dir, paste0(gse, ".json"))
    if (fs::file_exists(cache_path)) {
      return(jsonlite::read_json(cache_path, simplifyVector = FALSE))
    }
  }

  # [API call concreta — adatta secondo findings 5.1]
  # Esempio placeholder (sostituire con endpoint reale):
  url <- paste0(api_base, "/api/signatures?gse=", gse)
  resp <- tryCatch(
    httr2::request(url) |>
      httr2::req_retry(max_tries = 3L, backoff = function(n) 2^(n - 1L)) |>
      httr2::req_perform(),
    error = function(e) {
      rlang::abort(
        sprintf("RummaGEO fetch failed for %s: %s", gse, conditionMessage(e)),
        class = "simulomicsr_rummageo_unavailable"
      )
    }
  )

  if (httr2::resp_status(resp) >= 400L) {
    rlang::abort(
      sprintf("RummaGEO HTTP %d for %s", httr2::resp_status(resp), gse),
      class = "simulomicsr_rummageo_unavailable"
    )
  }

  out <- httr2::resp_body_json(resp, simplifyVector = FALSE)

  if (!is.null(cache_dir)) {
    fs::dir_create(cache_dir, recurse = TRUE)
    jsonlite::write_json(
      out, cache_path, auto_unbox = TRUE, null = "null", pretty = TRUE
    )
  }

  out
}

#' Estrae assegnazione control/treated per GSM dai signature record RummaGEO
#'
#' @param data list ritornato da fetch_rummageo_signatures()
#' @return tibble con colonne geo_accession, rummageo_label (treated/control)
#' @export
parse_rummageo_labels <- function(data) {
  sigs <- data$signatures %||% list()
  rows <- list()
  for (sig in sigs) {
    treated <- sig$treated_samples %||% character(0)
    control <- sig$control_samples %||% character(0)
    for (gsm in treated) {
      rows[[length(rows) + 1L]] <- tibble::tibble(
        geo_accession = gsm, rummageo_label = "treated"
      )
    }
    for (gsm in control) {
      rows[[length(rows) + 1L]] <- tibble::tibble(
        geo_accession = gsm, rummageo_label = "control"
      )
    }
  }
  if (length(rows) == 0L) {
    return(tibble::tibble(
      geo_accession = character(0), rummageo_label = character(0)
    ))
  }
  out <- dplyr::bind_rows(rows)
  # Se un GSM appare in piu' signature, prendi la prima occorrenza
  out <- out[!duplicated(out$geo_accession), ]
  out
}
```

- [ ] **Step 5.6: Reinstall + ri-run test**

Expected: 3 PASS.

- [ ] **Step 5.7: Document + commit**

```bash
Rscript --vanilla -e 'devtools::document()'
git add R/eval-rummageo.R tests/testthat/test-eval-rummageo.R \
        tests/testthat/fixtures/rummageo-mock-GSE145941.json \
        man/fetch_rummageo_signatures.Rd man/parse_rummageo_labels.Rd NAMESPACE
git commit -m "P3.5b Task 5: fetch_rummageo_signatures() + parse_rummageo_labels() + 3 test"
```

---

## Task 6: `R/eval-rummageo.R` — `rummageo_baseline_internal()` fallback

**Files:**
- Modifica: `R/eval-rummageo.R`
- Modifica: `tests/testthat/test-eval-rummageo.R`

Replica algoritmo RummaGEO internamente come fallback quando API/dump non disponibili. K-means clustering su metadati + keyword matching per control identification (per Marino et al. *Patterns* 2024).

- [ ] **Step 6.1: Aggiungi test**

```r
test_that("rummageo_baseline_internal: 2-cluster split su metadati semplici", {
  samples <- tibble::tibble(
    geo_accession = paste0("GSM", 1:6),
    series_id = rep("GSE1", 6),
    string = c(
      "treatment: DMSO",
      "treatment: DMSO",
      "treatment: DMSO",
      "treatment: drug X 10nM",
      "treatment: drug X 10nM",
      "treatment: drug X 10nM"
    )
  )
  out <- rummageo_baseline_internal(samples)
  expect_true(tibble::is_tibble(out))
  expect_setequal(colnames(out), c("geo_accession", "rummageo_label"))
  # I 3 DMSO devono essere control (keyword)
  dmso_labels <- out$rummageo_label[out$geo_accession %in% paste0("GSM", 1:3)]
  expect_true(all(dmso_labels == "control"))
  # I 3 drug devono essere treated
  drug_labels <- out$rummageo_label[out$geo_accession %in% paste0("GSM", 4:6)]
  expect_true(all(drug_labels == "treated"))
})

test_that("rummageo_baseline_internal: keyword 'control' in metadata", {
  samples <- tibble::tibble(
    geo_accession = paste0("GSM", 1:4),
    series_id = rep("GSE1", 4),
    string = c(
      "treatment: control siRNA",
      "treatment: control siRNA",
      "treatment: shGAPDH",
      "treatment: shKRAS"
    )
  )
  out <- rummageo_baseline_internal(samples)
  expect_equal(out$rummageo_label[1:2], rep("control", 2))
  # Gli altri due (no keyword) sono treated
  expect_true(all(out$rummageo_label[3:4] == "treated"))
})
```

- [ ] **Step 6.2: Run test, vedi fallire**

Expected: 2 nuovi FAIL.

- [ ] **Step 6.3: Implementa in `R/eval-rummageo.R`**

Aggiungi al fondo:

```r
# Keyword di control per il fallback K-means+keyword (per Marino 2024)
.RUMMAGEO_CONTROL_KEYWORDS <- c(
  "ctrl", "control", "wildtype", "wild-type", "wild type", "wt",
  "dmso", "vehicle", "untreated", "mock", "uninfected", "naive",
  "non-targeting", "scrambled", "sint", "sineg", "shnt",
  "empty vector", "ev", "parental"
)

#' Replica fallback dell'algoritmo RummaGEO (Marino 2024)
#'
#' Quando l'API ufficiale non e' disponibile, applichiamo internamente
#' lo stesso algoritmo di base: keyword matching su control terms +
#' default "treated" per il resto. Questo NON e' equivalente al benchmark
#' "vs RummaGEO ufficiale" — controlla noi il metodo. Documentato come
#' baseline interno nel report.
#'
#' Differenza vs Marino 2024: NON facciamo K-means (non ha senso su
#' subset di 15 GSE; Marino lo usa per gestire studi grandi). Solo
#' keyword matching.
#'
#' @param samples tibble con colonne geo_accession, series_id, string
#' @return tibble con colonne geo_accession, rummageo_label
#' @export
rummageo_baseline_internal <- function(samples) {
  stopifnot(all(c("geo_accession", "string") %in% names(samples)))
  rows <- lapply(seq_len(nrow(samples)), function(i) {
    s <- tolower(samples$string[i])
    is_ctrl <- any(vapply(
      .RUMMAGEO_CONTROL_KEYWORDS,
      function(kw) grepl(paste0("\\b", kw, "\\b"), s, fixed = FALSE),
      logical(1)
    ))
    tibble::tibble(
      geo_accession = samples$geo_accession[i],
      rummageo_label = if (is_ctrl) "control" else "treated"
    )
  })
  dplyr::bind_rows(rows)
}
```

- [ ] **Step 6.4: Reinstall + ri-run test**

Expected: 2 PASS. Totale 5 PASS in eval-rummageo.

- [ ] **Step 6.5: Document + commit**

```bash
Rscript --vanilla -e 'devtools::document()'
git add R/eval-rummageo.R tests/testthat/test-eval-rummageo.R \
        man/rummageo_baseline_internal.Rd NAMESPACE
git commit -m "P3.5b Task 6: rummageo_baseline_internal() fallback + 2 test"
```

---

## Task 7: Target eval pipeline in `analysis/_targets.R`

**Files:**
- Modifica: `analysis/_targets.R`

Aggiunge 7 nuovi target. Connette gold xlsx + study_designs + RummaGEO.

- [ ] **Step 7.1: Aggiungi target dopo `comparisons_table` esistente**

```r
  # ============================================================
  # P3.5-B eval benchmark sui 15 GSE curated
  # ============================================================

  # Gold subset: filtra il xlsx ai (series_id, geo_accession) dei 15 GSE.
  tar_target(
    gold_table_subset,
    {
      curated <- curated_stage2_gse  # 15 GSE
      samples_input %>%
        dplyr::filter(series_id %in% curated) %>%
        dplyr::transmute(
          geo_accession = geo_accession,
          series_id = series_id,
          string = string,
          gold_raw = trtctr_EP,
          gold_binary = dplyr::case_when(
            tolower(trtctr_EP) %in% c("treated") ~ "treated",
            tolower(trtctr_EP) %in% c("control") ~ "control",
            TRUE ~ NA_character_  # outlier non binari (es. specific drug names)
          )
        )
    }
  ),

  # Join: per ogni GSM dei fixture, trova design_role nello study_designs
  # corrispondente e mappa a binary.
  tar_target(
    eval_stage2_gold_join,
    {
      rows <- list()
      for (design in study_designs_validated) {
        sid <- design$series_id
        for (g in design$replicate_groups) {
          for (gsm in g$sample_ids) {
            rows[[length(rows) + 1L]] <- tibble::tibble(
              series_id = sid,
              geo_accession = gsm,
              design_role = g$design_role,
              design_kind = design$design_kind,
              predicted_binary = design_role_to_binary(g$design_role)
            )
          }
        }
      }
      pred_df <- dplyr::bind_rows(rows)
      # Join con gold
      out <- dplyr::left_join(
        pred_df, gold_table_subset[, c("geo_accession", "series_id",
                                       "string", "gold_raw", "gold_binary")],
        by = c("geo_accession", "series_id")
      )
      out
    }
  ),

  # Metriche binary su tutto il join + breakdown per design_kind +
  # granularity disagreement
  tar_target(
    eval_stage2_metrics,
    {
      list(
        overall = eval_binary_accuracy(
          eval_stage2_gold_join$gold_binary,
          eval_stage2_gold_join$predicted_binary
        ),
        per_kind = eval_per_design_kind(eval_stage2_gold_join),
        granularity = flag_granularity_disagreement(eval_stage2_gold_join)
      )
    }
  ),

  # RummaGEO cache + fetch per i 15 GSE
  tar_target(
    rummageo_cache_dir,
    fs::dir_create(here::here("analysis", "cache", "rummageo")),
    format = "file"
  ),

  tar_target(
    rummageo_signatures,
    {
      out_per_gse <- lapply(curated_stage2_gse, function(gse) {
        tryCatch({
          data <- fetch_rummageo_signatures(gse, cache_dir = rummageo_cache_dir)
          labels <- parse_rummageo_labels(data)
          if (nrow(labels) == 0L) {
            # Fallback interno
            sub <- gold_table_subset[gold_table_subset$series_id == gse, ]
            labels <- rummageo_baseline_internal(sub)
            attr(labels, "source") <- "internal_fallback"
          } else {
            attr(labels, "source") <- "rummageo_official"
          }
          labels$series_id <- gse
          labels
        }, error = function(e) {
          # Total failure: fallback interno
          sub <- gold_table_subset[gold_table_subset$series_id == gse, ]
          if (nrow(sub) == 0L) return(NULL)
          labels <- rummageo_baseline_internal(sub)
          attr(labels, "source") <- "internal_fallback"
          labels$series_id <- gse
          labels
        })
      })
      out <- dplyr::bind_rows(Filter(Negate(is.null), out_per_gse))
      out
    }
  ),

  # Join RummaGEO labels con gold + simulomicsr predicted -> metriche
  # head-to-head
  tar_target(
    rummageo_metrics,
    {
      joined <- dplyr::left_join(
        eval_stage2_gold_join,
        rummageo_signatures[, c("geo_accession", "series_id", "rummageo_label")],
        by = c("geo_accession", "series_id")
      )
      list(
        rummageo_vs_gold = eval_binary_accuracy(
          joined$gold_binary, joined$rummageo_label
        ),
        simulomicsr_vs_gold = eval_binary_accuracy(
          joined$gold_binary, joined$predicted_binary
        ),
        rummageo_vs_simulomicsr = eval_binary_accuracy(
          joined$predicted_binary, joined$rummageo_label
        ),
        joined_table = joined
      )
    }
  ),

  # Quarto report finale
  tarchetypes::tar_render(
    eval_p35_report,
    here::here("analysis", "eval", "p35-benchmark.Rmd"),
    output_dir = here::here("analysis", "eval"),
    params = list(
      stage2_metrics = eval_stage2_metrics,
      rummageo_metrics = rummageo_metrics,
      comparisons_table = comparisons_table,
      curated_gse = curated_stage2_gse
    )
  ),
```

- [ ] **Step 7.2: Run targets fino a `rummageo_metrics`**

```bash
Rscript --vanilla -e '
suppressMessages({library(targets); library(dplyr)})
setwd("analysis")
tar_make(c(gold_table_subset, eval_stage2_gold_join, eval_stage2_metrics,
           rummageo_cache_dir, rummageo_signatures, rummageo_metrics),
         callr_function = NULL)
'
```

Expected: 6 target popolati. RummaGEO può cadere su fallback interno (acceptable). Stampa metriche overall:
```bash
Rscript --vanilla -e '
suppressMessages({library(targets)})
setwd("analysis")
m <- tar_read(eval_stage2_metrics)
cat("Stage 2 binary accuracy: n =", m$overall$n, ", accuracy =",
    round(m$overall$accuracy, 3), "\n")
cat("Per design_kind:\n")
print(m$per_kind)
cat("Granularity flagged:", nrow(m$granularity), "\n")
rm <- tar_read(rummageo_metrics)
cat("RummaGEO vs gold accuracy:", round(rm$rummageo_vs_gold$accuracy, 3), "\n")
cat("simulomicsr vs gold accuracy:", round(rm$simulomicsr_vs_gold$accuracy, 3), "\n")
'
```

- [ ] **Step 7.3: Commit**

```bash
git checkout -- analysis/_targets/meta/meta 2>/dev/null
git add analysis/_targets.R
git commit -m "P3.5b Task 7: target eval pipeline (gold join + stage2 metrics + rummageo + report)"
```

---

## Task 8: Quarto report `analysis/eval/p35-benchmark.Rmd`

**Files:**
- Crea: `analysis/eval/p35-benchmark.Rmd`

Report Quarto 4 sezioni con grafici ggplot2.

- [ ] **Step 8.1: Crea `analysis/eval/p35-benchmark.Rmd`**

```rmd
---
title: "P3.5-B Benchmark: Stage 2 vs gold + RummaGEO head-to-head"
output: html_document
params:
  stage2_metrics: NA
  rummageo_metrics: NA
  comparisons_table: NA
  curated_gse: NA
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(echo = FALSE, warning = FALSE, message = FALSE)
library(dplyr)
library(tibble)
library(ggplot2)
library(scales)
m <- params$stage2_metrics
rm <- params$rummageo_metrics
ct <- params$comparisons_table
gse_list <- params$curated_gse
```

# P3.5-B benchmark report

Subset: 15 GSE curati (P3 fixture mini), classificati Stage 1+2 con
gpt-5.5. Gold: `trtctr_EP` xlsx (binary normalizzato: treated/control/NA).

## 1. Stage 2 binary accuracy

```{r overall}
ov <- m$overall
cat(sprintf(
  "**N (escluso NA): %d** | accuracy: **%.1f%%** | sensitivity: %.1f%% | specificity: %.1f%% | F1: %.3f\n",
  ov$n,
  100 * ov$accuracy,
  100 * ov$sensitivity,
  100 * ov$specificity,
  ov$f1
))
```

### Confusion matrix (rows = simulomicsr predicted, cols = gold)

```{r cm}
print(ov$confusion_matrix)
```

### Breakdown per design_kind

```{r per_kind, results = "asis"}
knitr::kable(
  m$per_kind |>
    mutate(across(c(accuracy, sensitivity, specificity, f1),
                  ~ round(.x, 3))),
  caption = "Binary accuracy per design_kind"
)
```

```{r per_kind_plot, fig.width = 7, fig.height = 4}
ggplot(m$per_kind, aes(x = design_kind, y = accuracy, fill = design_kind)) +
  geom_col() +
  geom_text(aes(label = sprintf("n=%d", n)), vjust = -0.3, size = 3) +
  scale_y_continuous(labels = percent_format(), limits = c(0, 1.05)) +
  labs(x = NULL, y = "Binary accuracy",
       title = "Stage 2 binary accuracy per design_kind") +
  theme_minimal() +
  theme(legend.position = "none",
        axis.text.x = element_text(angle = 30, hjust = 1))
```

## 2. Granularity disagreement enumeration

simulomicsr produce `design_role` valori che il gold binary non distingue.
Questi NON sono errori — sono valore aggiunto del classificatore design-aware.

```{r granularity, results = "asis"}
gr <- m$granularity
if (nrow(gr) > 0L) {
  knitr::kable(
    gr |> select(series_id, geo_accession, design_role, predicted_binary, gold_binary, granularity_kind),
    caption = sprintf("Casi con granularity (n = %d)", nrow(gr))
  )
} else {
  cat("Nessun caso di granularity nel subset 15 GSE.\n")
}
```

```{r granularity_count}
cat(sprintf(
  "**Granularity flagged: %d sample** su %d totali (%.1f%%)\n",
  nrow(gr), nrow(m$per_kind |> summarise(n_total = sum(n)) |> pull()) %||% 0,
  100 * nrow(gr) / max(nrow(m$per_kind |> summarise(n_total = sum(n)) |> pull()) %||% 1, 1)
))
```

## 3. Anchor coverage cross-studio (intra-15)

```{r anchor_coverage, results = "asis"}
anchor_counts <- ct |>
  group_by(comparability_anchor) |>
  summarise(n_studies = length(unique(series_id)),
            n_comparisons = dplyr::n(),
            .groups = "drop") |>
  arrange(desc(n_studies))

multi_studio <- anchor_counts |> filter(n_studies >= 2L)

cat(sprintf(
  "**Anchor unici totali:** %d. **Anchor in N>=2 GSE:** %d. **Comparisons totali:** %d.\n\n",
  nrow(anchor_counts), nrow(multi_studio), nrow(ct)
))

if (nrow(multi_studio) > 0L) {
  knitr::kable(
    head(multi_studio, 20),
    caption = "Top anchor con N>=2 studi (potential meta-analyses)"
  )
} else {
  cat("Nessun anchor cross-studio nel subset di 15 GSE — atteso, scaling a 50-100 GSE in P3.5-A produrra collisioni.\n")
}
```

```{r anchor_dist, fig.width = 6, fig.height = 4}
ggplot(anchor_counts, aes(x = n_studies)) +
  geom_histogram(binwidth = 1L, fill = "steelblue", color = "white") +
  scale_x_continuous(breaks = 1:10) +
  labs(x = "N GSE per anchor", y = "Frequenza",
       title = "Distribuzione studi-per-anchor") +
  theme_minimal()
```

## 4. RummaGEO head-to-head

```{r rummageo_summary, results = "asis"}
joined <- rm$joined_table
n_total <- nrow(joined)
n_with_rg <- sum(!is.na(joined$rummageo_label))
n_with_gold <- sum(!is.na(joined$gold_binary))

cat(sprintf(
  "**Coverage:** %d sample con label RummaGEO su %d totali (%.0f%%). %d con gold.\n\n",
  n_with_rg, n_total, 100 * n_with_rg / n_total, n_with_gold
))

cat(sprintf(
  "**RummaGEO vs gold:** accuracy = %.1f%% (n = %d)\n",
  100 * (rm$rummageo_vs_gold$accuracy %||% NA),
  rm$rummageo_vs_gold$n
))
cat(sprintf(
  "**simulomicsr vs gold:** accuracy = %.1f%% (n = %d)\n",
  100 * (rm$simulomicsr_vs_gold$accuracy %||% NA),
  rm$simulomicsr_vs_gold$n
))
cat(sprintf(
  "**simulomicsr vs RummaGEO:** agreement = %.1f%% (n = %d)\n",
  100 * (rm$rummageo_vs_simulomicsr$accuracy %||% NA),
  rm$rummageo_vs_simulomicsr$n
))
```

### Confusion matrix RummaGEO × gold

```{r rg_cm}
print(rm$rummageo_vs_gold$confusion_matrix)
```

### McNemar test sui disagreement

```{r mcnemar}
joined_clean <- joined |>
  filter(!is.na(gold_binary), !is.na(predicted_binary), !is.na(rummageo_label))

if (nrow(joined_clean) >= 25L) {
  sim_correct <- joined_clean$predicted_binary == joined_clean$gold_binary
  rg_correct <- joined_clean$rummageo_label == joined_clean$gold_binary
  ct_table <- table(simulomicsr = sim_correct, rummageo = rg_correct)
  print(ct_table)
  if (all(c(2L, 2L) %in% dim(ct_table))) {
    mc <- mcnemar.test(ct_table, correct = TRUE)
    cat(sprintf("\nMcNemar test: chi^2 = %.2f, p = %.4f\n",
                mc$statistic, mc$p.value))
  }
} else {
  cat(sprintf("n = %d sample, troppo pochi per McNemar test (richiesti >= 25)\n",
              nrow(joined_clean)))
}
```

### Per-design_kind: simulomicsr vs RummaGEO

```{r per_kind_h2h, results = "asis"}
joined_clean <- joined |>
  filter(!is.na(gold_binary), !is.na(predicted_binary), !is.na(rummageo_label))

if (nrow(joined_clean) > 0L) {
  per_kind_h2h <- joined_clean |>
    group_by(design_kind) |>
    summarise(
      n = dplyr::n(),
      simulomicsr_acc = mean(predicted_binary == gold_binary),
      rummageo_acc = mean(rummageo_label == gold_binary),
      delta = simulomicsr_acc - rummageo_acc,
      .groups = "drop"
    ) |>
    arrange(desc(delta))
  knitr::kable(
    per_kind_h2h |>
      mutate(across(c(simulomicsr_acc, rummageo_acc, delta),
                    ~ round(.x, 3))),
    caption = "Head-to-head per design_kind (delta = simulomicsr - rummageo)"
  )
} else {
  cat("Nessun sample con coverage completo per head-to-head per-design_kind.\n")
}
```

## Conclusioni P3.5-B

```{r conclusioni, results = "asis"}
ov_acc <- 100 * (m$overall$accuracy %||% NA)
n_granular <- nrow(m$granularity)
n_anchor_multi <- sum(anchor_counts$n_studies >= 2L)

cat(sprintf("- Stage 2 binary accuracy: **%.1f%%** su n = %d sample\n", ov_acc, m$overall$n))
cat(sprintf("- Granularity flagged: **%d sample** con design_role piu' granulare del binary gold\n", n_granular))
cat(sprintf("- Anchor cross-studio: **%d anchor** in N>=2 GSE su un subset di 15\n", n_anchor_multi))
cat("- Prossimo step: P3.5-A scaled benchmark su 50-100 GSE per evidenza paper-ready.\n")
```
```

- [ ] **Step 8.2: Run target Quarto**

```bash
Rscript --vanilla -e '
suppressMessages({library(targets)})
setwd("analysis")
tar_make(eval_p35_report, callr_function = NULL)
'
ls analysis/eval/p35-benchmark.html  # verifica file generato
```

- [ ] **Step 8.3: Commit**

```bash
git checkout -- analysis/_targets/meta/meta 2>/dev/null
git add analysis/eval/p35-benchmark.Rmd
git commit -m "P3.5b Task 8: Quarto report 4 sezioni (binary acc / granularity / anchor / rummageo)"
```

---

## Task 9: R CMD check + tests + merge + tag

**Files:** nessuno; cerimonie chiusura.

- [ ] **Step 9.1: Run R CMD check**

```bash
Rscript --vanilla -e 'devtools::check(error_on = "error")'
```

Expected: 0 errors. Eventuali nuovi warning da affrontare.

- [ ] **Step 9.2: Run full test suite**

```bash
OPENAI_API_KEY="$(grep OPENAI_API_KEY .Renviron.local | cut -d= -f2 | tr -d '\"')" \
  Rscript --vanilla -e 'devtools::test()'
```

Expected: tutti PASS, 0 FAIL.

- [ ] **Step 9.3: Pulizia working tree**

```bash
git checkout -- analysis/_targets/meta/meta 2>/dev/null
git status
```

- [ ] **Step 9.4: Bump version + NEWS**

In `DESCRIPTION` cambia `Version: 0.0.0.9004` in `Version: 0.0.0.9005`.

In `NEWS.md` aggiungi sezione in cima:

```markdown
# simulomicsr 0.0.0.9005

## P3.5-B — eval benchmark Stadio 2 sui 15 GSE (prototipo)

- `R/eval-stage2.R`: `design_role_to_binary()` (mapping 13->3 esteso vs spec
  v5 §6.2), `eval_binary_accuracy()`, `eval_per_design_kind()`,
  `flag_granularity_disagreement()`. Tutti export pubblici.
- `R/eval-rummageo.R`: `fetch_rummageo_signatures()` (cache filesystem),
  `parse_rummageo_labels()`, `rummageo_baseline_internal()` (fallback
  K-means+keyword se API non disponibile).
- `analysis/_targets.R`: 7 nuovi target (gold_table_subset,
  eval_stage2_gold_join, eval_stage2_metrics, rummageo_*, eval_p35_report).
- `analysis/eval/p35-benchmark.Rmd`: Quarto report 4 sezioni.

## Risultati run reale 15 GSE

| Metrica | Valore |
|---|---|
| Stage 2 binary accuracy | (popolare a runtime) |
| Granularity flagged | (popolare a runtime) |
| Anchor cross-studio (N>=2 GSE) | (popolare a runtime) |
| RummaGEO vs gold | (popolare a runtime, se data acquisition succeed) |
```

(Dopo il run del report, popola i numeri concreti nel NEWS — bump-fix commit.)

- [ ] **Step 9.5: Commit bump + NEWS**

```bash
git add NEWS.md DESCRIPTION
git commit -m "P3.5b Task 9.4: bump 0.0.0.9005 + NEWS aggiornato"
```

- [ ] **Step 9.6: Merge fast-forward su master**

```bash
git checkout master
git merge --ff-only p3.5b-eval
git log --oneline -10
```

- [ ] **Step 9.7: Tag `p3.5b-eval-complete`**

```bash
git tag -a p3.5b-eval-complete -m "P3.5-B — eval benchmark Stadio 2 sui 15 GSE prototipo"
git tag --list 'p3*'
```

- [ ] **Step 9.8: Aggiorna CLAUDE.md "Stato corrente"**

Apri `CLAUDE.md` e aggiorna la sezione "## Stato corrente" con:
- nuovo Master HEAD
- Tag aggiunto: `p3.5b-eval-complete`
- nuova sotto-sezione "Cosa P3.5-B ha consegnato" con bullet point dei deliverable
- aggiungi i numeri reali del benchmark (Stage 2 binary accuracy, granularity count, anchor coverage, RummaGEO comparison)

Commit:
```bash
git add CLAUDE.md
git commit -m "P3.5b chiusura: aggiorna CLAUDE.md Stato corrente con HEAD + tag + risultati benchmark"
```

- [ ] **Step 9.9: Verifica finale**

```bash
git log --oneline -15
git tag --list
ls analysis/eval/p35-benchmark.html
```

P3.5-B chiuso. Pronti per: brainstorm + plan P3.5-A scaled benchmark (50-100 GSE) come fase successiva.

---

## Self-review notes

**Spec coverage check:** ogni section/requisito di spec P3.5-B ha un task corrispondente:
- §3 architettura → Task 7 (targets)
- §4 componenti file → Task 2-8
- §5 mapping table → Task 2 (design_role_to_binary)
- §6 RummaGEO 3-livelli → Task 5 (5.1 esplora API, 5.5 implementa fetch) + Task 6 (fallback interno)
- §7 data flow → Task 7 (targets pipeline)
- §8 4 sezioni Quarto → Task 8 (p35-benchmark.Rmd)
- §9 error handling → distribuito (Task 5 cache + retry, Task 6 fallback, Task 7 try/catch)
- §10 testing → Task 2-6 hanno test TDD; smoke E2E è Task 8.2 + 9.2
- §13 done criteria → Task 9 (R CMD check, merge, tag, CLAUDE.md)

**Type consistency check:**
- `design_role_to_binary(role)` — firma identica in Task 2 (def) e Task 7 (uso in eval_stage2_gold_join body).
- `eval_binary_accuracy(gold, predicted)` — firma identica Task 3 (def) e Task 7 (uso in eval_stage2_metrics + rummageo_metrics).
- `fetch_rummageo_signatures(gse, cache_dir, api_base)` — firma Task 5 e Task 7 chiamata.
- `parse_rummageo_labels(data)` — Task 5 e Task 7.
- `rummageo_baseline_internal(samples)` — Task 6 e Task 7.

**Placeholder scan:**
- Task 5.1 ha "[Da popolare dopo lo step 5.1]" nei comments del codice — è un placeholder INTENZIONALE: l'API è esplorativa, va scoperta a runtime. L'implementer legge Step 5.1 e popola il commento con i findings veri prima di committare. Acceptable.
- Task 9.4 NEWS ha "(popolare a runtime)" sui valori — sono placeholder INTENZIONALI: i numeri si popolano DOPO il run del report (Step 9.4 si fa post-Task 8). Acceptable.

**Fixed-in-flight:**
- Step 5.2 cassette mock potrebbe avere GSM IDs sbagliati se la fixture P3 ha numeri diversi. L'implementer va a leggere `inst/extdata/stage2-fixtures-mini/GSE145941-sample-facts.json` per i veri GSM e adatta — istruzioni esplicite in 5.2.
- Step 7.1 il join `eval_stage2_gold_join` assume che il `samples_input` (P2) contenga tutti i GSM dei 15 fixture GSE. Se NON è così (es. la build extension Task 10 ext ha pescato GSE non nel dev set), il join produrrà righe con `gold_binary = NA` per quei GSM. È coerente con error handling §9. Da verificare a runtime.
