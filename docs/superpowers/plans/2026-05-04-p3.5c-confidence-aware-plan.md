# P3.5-C — Confidence-aware classification — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Risolvere in un'unica fase tre obiettivi interconnessi: (1) cost-quality validation di modelli LLM cheap vs gpt-5.5 per P4, (2) difficulty score automatico via cross-model agreement, (3) mini-gold design-aware (100 sample) come metrica di qualità affidabile, sostituendo il gold xlsx come fonte primaria.

**Architecture:** Layer multi-modello sopra l'infrastruttura P3.5-A. Stage 1 fissato a gpt-5.5 (cache hit), Stage 2 variabile su 5 modelli (gpt-5.5 baseline cache, gpt-5.4-mini, gpt-5.4-nano, claude-haiku-4-5, claude-sonnet-4-6). Cross-model agreement diventa difficulty score. Mini-gold stratificato per difficoltà valida tutti i modelli. Output: report Quarto unico `analysis/eval/p35c-confidence.html` con 4 sezioni.

**Tech Stack:** R 4.5+, packages: `httr2`, `jsonlite`, `targets`, `dplyr`, `tibble`, `readr`, `metafor` (per future fasi), `quarto`. New deps: nessuna (Anthropic via httr2).

**Riferimenti:** Spec `docs/superpowers/specs/2026-05-04-p3.5c-confidence-aware-design.md`. Spec P3.5-A esistente (riusiamo `curated_p35a_gse`, `sample_facts_p35a_validated`).

---

## Mappa file

| File | Stato | Responsabilità |
|---|---|---|
| `R/llm-client-anthropic.R` | NUOVO | Adapter Anthropic Messages API + structured output via tool_use. Mirror diretto di `R/llm-client-openai.R`. |
| `R/llm-client.R` | MODIFICATO (~+15 LOC) | Aggiunta dispatch `provider == "anthropic"`. |
| `R/p35c-multi-classify.R` | NUOVO | `multi_classify_study()` — dispatcher che chiama `classify_study()` su una lista di `{provider, model}` per un singolo GSE. |
| `R/p35c-confidence.R` | NUOVO | `compute_pairwise_agreement()`, `aggregate_confidence_score()`, `assign_difficulty_tier()`. |
| `R/p35c-minigold.R` | NUOVO | `sample_minigold_stratified()`, `export_minigold_csv()`, `import_minigold_reviewed()`, `eval_against_minigold()`. |
| `analysis/_targets.R` | MODIFICATO (~+150 LOC in coda) | Target P3.5-C suffisso `_p35c`: `study_designs_p35c_<model>` × 4 nuovi modelli, `confidence_scores_p35c`, `minigold_pool_p35c`, `minigold_template_csv_p35c`, `minigold_reviewed_p35c`, `eval_p35c_metrics`, `eval_p35c_report`. |
| `analysis/eval/p35c-confidence.Rmd` | NUOVO | Quarto 4 sezioni (cost-quality / confidence calibration / error patterns / decisione P4-ready). |
| `tests/testthat/test-llm-client-anthropic.R` | NUOVO | Test mocked Anthropic adapter (build_request, parse_response, errori tipizzati). |
| `tests/testthat/test-p35c-multi-classify.R` | NUOVO | Test multi_classify_study con mock adapter. |
| `tests/testthat/test-p35c-confidence.R` | NUOVO | Test agreement + tier assignment con input controllato. |
| `tests/testthat/test-p35c-minigold.R` | NUOVO | Test sampling stratificato + roundtrip CSV. |
| `inst/extdata/p35c-minigold-template-empty.csv` | NUOVO (committato vuoto) | Template CSV con sole header per riferimento e schema dei campi. |
| `inst/extdata/p35c-minigold-reviewed.csv` | NUOVO (artefatto post-review) | Mini-gold reviewato dall'utente, committato dopo Task 12. |
| `NEWS.md` | MODIFICATO | Bump versione 0.0.0.9007 + sommario P3.5-C. |
| `DESCRIPTION` | MODIFICATO | Bump versione. |
| `CLAUDE.md` | MODIFICATO | Aggiornamento status post-P3.5-C alla chiusura. |

---

## Task 1: Adapter Anthropic — build_request + parse_response

**Files:**
- Create: `R/llm-client-anthropic.R`
- Test: `tests/testthat/test-llm-client-anthropic.R`

Anthropic Messages API differisce da OpenAI in due punti:
1. Endpoint: `https://api.anthropic.com/v1/messages`
2. Structured output: si fa via `tools` con `input_schema` (JSON Schema) invece di `response_format.json_schema`. Il modello viene forzato a chiamare il tool tramite `tool_choice = {"type": "tool", "name": "..."}`. La risposta arriva in `content[[i]]$type == "tool_use"`, con i parametri JSON in `content[[i]]$input`.
3. Auth header `x-api-key` (non `Authorization: Bearer`) e `anthropic-version: 2023-06-01`.

- [ ] **Step 1: Crea il file `R/llm-client-anthropic.R` con scheletro vuoto**

```r
# Endpoint costante
.ANTHROPIC_MESSAGES_URL <- "https://api.anthropic.com/v1/messages"
.ANTHROPIC_API_VERSION  <- "2023-06-01"
```

Salva il file.

- [ ] **Step 2: Scrivi i test failing prima dell'implementazione**

Crea `tests/testthat/test-llm-client-anthropic.R` con:

```r
withr::local_envvar(ANTHROPIC_API_KEY = "sk-ant-fake-for-tests")

test_that(".anthropic_chat_structured costruisce request con tool_use forzato", {
  req <- .anthropic_build_request(
    model = "claude-haiku-4-5",
    messages = list(list(role = "user", content = "Say pong as JSON.")),
    response_schema = system.file("schemas/llm-call-envelope.v1.json", package = "simulomicsr"),
    schema_name = "llm_call_envelope_v1"
  )

  expect_match(req$url, "^https://api\\.anthropic\\.com/v1/messages$")

  hdrs <- httr2::req_get_headers(req, redacted = "reveal")
  expect_equal(hdrs[["x-api-key"]], "sk-ant-fake-for-tests")
  expect_equal(hdrs[["anthropic-version"]], "2023-06-01")

  body <- jsonlite::fromJSON(rawToChar(req$body$data), simplifyVector = FALSE)
  expect_equal(body$model, "claude-haiku-4-5")
  expect_equal(length(body$tools), 1L)
  expect_equal(body$tools[[1]]$name, "llm_call_envelope_v1")
  expect_true(!is.null(body$tools[[1]]$input_schema))
  expect_equal(body$tool_choice$type, "tool")
  expect_equal(body$tool_choice$name, "llm_call_envelope_v1")
  expect_equal(body$messages[[1]]$role, "user")
})

test_that(".anthropic_parse_response estrae il payload da content[[i]]$type=='tool_use'", {
  fake_response <- list(
    id = "msg_x",
    type = "message",
    role = "assistant",
    model = "claude-haiku-4-5",
    stop_reason = "tool_use",
    content = list(
      list(type = "text", text = "Reasoning step omitted."),
      list(
        type = "tool_use",
        id = "toolu_x",
        name = "llm_call_envelope_v1",
        input = list(question = "ping", answer = "pong", confidence = 0.9)
      )
    )
  )
  parsed <- .anthropic_parse_response(fake_response)

  expect_equal(parsed$question, "ping")
  expect_equal(parsed$answer, "pong")
  expect_equal(parsed$confidence, 0.9)
})

test_that(".anthropic_parse_response solleva errore tipizzato se stop_reason non e' 'tool_use' o 'end_turn'", {
  bad <- list(
    stop_reason = "max_tokens",
    content = list(list(type = "text", text = "incomplete"))
  )
  expect_error(
    .anthropic_parse_response(bad),
    class = "simulomicsr_anthropic_truncated"
  )
})

test_that(".anthropic_parse_response solleva errore tipizzato se manca tool_use block", {
  bad <- list(
    stop_reason = "end_turn",
    content = list(list(type = "text", text = "I will not call the tool."))
  )
  expect_error(
    .anthropic_parse_response(bad),
    class = "simulomicsr_anthropic_no_tool_use"
  )
})

test_that("missing ANTHROPIC_API_KEY -> errore tipizzato", {
  withr::local_envvar(ANTHROPIC_API_KEY = "")
  expect_error(
    .anthropic_build_request(
      model = "claude-haiku-4-5",
      messages = list(list(role = "user", content = "x")),
      response_schema = system.file("schemas/llm-call-envelope.v1.json", package = "simulomicsr"),
      schema_name = "x"
    ),
    class = "simulomicsr_anthropic_missing_key"
  )
})

test_that(".anthropic_build_request setta max_tokens (Anthropic richiede sempre max_tokens)", {
  req <- .anthropic_build_request(
    model = "claude-haiku-4-5",
    messages = list(list(role = "user", content = "x")),
    response_schema = system.file("schemas/llm-call-envelope.v1.json", package = "simulomicsr"),
    schema_name = "x"
  )
  body <- jsonlite::fromJSON(rawToChar(req$body$data), simplifyVector = FALSE)
  expect_true(body$max_tokens >= 1024L)
})
```

- [ ] **Step 3: Esegui i test e verifica che falliscono**

```
OPENAI_API_KEY="" Rscript --vanilla -e 'devtools::test(filter = "llm-client-anthropic")'
```

Expected: 6 errori "could not find function `.anthropic_build_request`" o `.anthropic_parse_response`.

- [ ] **Step 4: Implementa `.anthropic_build_request` in `R/llm-client-anthropic.R`**

Aggiungi al file:

```r
#' Costruisce (senza inviare) la `httr2` request per Anthropic Messages
#' con structured output via tool_use forzato.
#'
#' @keywords internal
.anthropic_build_request <- function(model,
                                     messages,
                                     response_schema,
                                     schema_name,
                                     max_tokens = 4096L,
                                     api_key = NULL) {
  api_key <- api_key %||% Sys.getenv("ANTHROPIC_API_KEY", unset = "")
  if (!nzchar(api_key)) {
    rlang::abort(
      "ANTHROPIC_API_KEY non impostata. Vedi `.Renviron.local` (gitignored) o `Sys.setenv()`.",
      class = "simulomicsr_anthropic_missing_key"
    )
  }

  schema_json <- jsonlite::fromJSON(
    readr::read_file(response_schema),
    simplifyVector = FALSE
  )

  # Anthropic supporta solo messages role=user/assistant. Eventuali system
  # messages OpenAI-style vengono separati nel campo top-level `system`.
  sys_blocks <- vapply(messages, function(m) identical(m$role, "system"), logical(1))
  system_text <- if (any(sys_blocks)) {
    paste(vapply(messages[sys_blocks], function(m) m$content, character(1)),
          collapse = "\n\n")
  } else NULL
  user_messages <- messages[!sys_blocks]

  body <- list(
    model = model,
    max_tokens = as.integer(max_tokens),
    messages = user_messages,
    tools = list(list(
      name = schema_name,
      description = paste0("Return structured response conforming to schema ", schema_name),
      input_schema = schema_json
    )),
    tool_choice = list(type = "tool", name = schema_name)
  )
  if (!is.null(system_text)) body$system <- system_text

  httr2::request(.ANTHROPIC_MESSAGES_URL) |>
    httr2::req_method("POST") |>
    httr2::req_headers(
      `x-api-key`         = api_key,
      `anthropic-version` = .ANTHROPIC_API_VERSION,
      `Content-Type`      = "application/json"
    ) |>
    httr2::req_body_raw(
      charToRaw(jsonlite::toJSON(body, auto_unbox = TRUE, null = "null")),
      type = "application/json"
    ) |>
    httr2::req_user_agent("simulomicsr (https://github.com/UBESP-DCTV/simulomicsr)") |>
    httr2::req_timeout(seconds = 120) |>
    httr2::req_retry(
      max_tries = 3L,
      backoff = function(i) min(60, 2 ^ i),
      is_transient = function(resp) {
        s <- httr2::resp_status(resp)
        s == 429L || s >= 500L
      }
    )
}
```

- [ ] **Step 5: Implementa `.anthropic_parse_response` in `R/llm-client-anthropic.R`**

Aggiungi al file:

```r
#' Estrae l'oggetto R dalla risposta Anthropic (tool_use block).
#'
#' Errori tipizzati:
#' - `simulomicsr_anthropic_truncated` se stop_reason non e' tool_use/end_turn
#' - `simulomicsr_anthropic_no_tool_use` se manca il tool_use block
#'
#' @keywords internal
.anthropic_parse_response <- function(resp_body) {
  stopifnot(is.list(resp_body))

  sr <- resp_body$stop_reason %||% "unknown"
  if (!sr %in% c("tool_use", "end_turn")) {
    rlang::abort(
      glue::glue("Anthropic stop_reason='{sr}', non 'tool_use' o 'end_turn'."),
      class = "simulomicsr_anthropic_truncated",
      stop_reason = sr
    )
  }

  blocks <- resp_body$content %||% list()
  tool_use_idx <- which(vapply(blocks, function(b) identical(b$type, "tool_use"), logical(1)))
  if (length(tool_use_idx) == 0L) {
    rlang::abort(
      "Anthropic non ha incluso un tool_use block (modello non ha chiamato il tool).",
      class = "simulomicsr_anthropic_no_tool_use"
    )
  }
  blocks[[tool_use_idx[1]]]$input
}
```

- [ ] **Step 6: Implementa `.anthropic_chat_structured` (HTTP wrapper)**

Aggiungi al file:

```r
#' Esegue la chiamata HTTP completa Anthropic e ritorna l'oggetto R parsed.
#'
#' Chiamato da `llm_call_structured()` quando `provider == "anthropic"`.
#'
#' @keywords internal
.anthropic_chat_structured <- function(model,
                                       messages,
                                       response_schema,
                                       schema_name = "response",
                                       max_tokens = 4096L,
                                       api_key = NULL,
                                       ...) {
  req <- .anthropic_build_request(
    model = model,
    messages = messages,
    response_schema = response_schema,
    schema_name = schema_name,
    max_tokens = max_tokens,
    api_key = api_key
  )
  resp <- httr2::req_perform(req)
  body <- httr2::resp_body_json(resp, simplifyVector = FALSE)
  .anthropic_parse_response(body)
}
```

- [ ] **Step 7: Esegui i test e verifica che passano**

```
OPENAI_API_KEY="" Rscript --vanilla -e 'devtools::test(filter = "llm-client-anthropic")'
```

Expected: tutti i test pass (6/6 PASS).

- [ ] **Step 8: Esegui suite completa per verificare zero regression**

```
OPENAI_API_KEY="" Rscript --vanilla -e 'devtools::test()'
```

Expected: zero failure (pre-esistenti 364 PASS + 6 nuovi = 370 PASS attesi).

- [ ] **Step 9: Genera Rd con roxygen e verifica R CMD check**

```
Rscript --vanilla -e 'devtools::document()'
Rscript --vanilla -e 'devtools::check(error_on = "warning", document = FALSE)'
```

Expected: 0 errors, 0 warnings (note pre-esistenti OK).

- [ ] **Step 10: Commit**

```
git add R/llm-client-anthropic.R tests/testthat/test-llm-client-anthropic.R man/
git commit -m "P3.5-C Task 1: adapter Anthropic Messages API con tool_use forzato"
```

---

## Task 2: Provider dispatch in llm_call_structured

**Files:**
- Modify: `R/llm-client.R` (extend dispatch around lines 62-81)
- Test: `tests/testthat/test-llm-client.R` (add new test)

- [ ] **Step 1: Aggiungi un test failing per provider="anthropic"**

Aggiungi a `tests/testthat/test-llm-client.R`:

```r
test_that("llm_call_structured(provider='anthropic') intercetta tramite .mock_adapter", {
  schema <- system.file("schemas/llm-call-envelope.v1.json", package = "simulomicsr")
  withr::local_envvar(ANTHROPIC_API_KEY = "sk-ant-fake")

  res <- llm_call_structured(
    provider = "anthropic",
    model = "claude-haiku-4-5",
    messages = list(list(role = "user", content = "Say pong.")),
    response_schema = schema,
    .mock_adapter = function(model, messages, response_schema, ...) {
      list(question = "Say pong.", answer = "pong", confidence = 0.95)
    }
  )

  expect_true(res$validated)
  expect_equal(res$value$answer, "pong")
  expect_equal(res$provider, "anthropic")
})
```

- [ ] **Step 2: Esegui il test e verifica che fallisce**

```
OPENAI_API_KEY="" Rscript --vanilla -e 'devtools::test(filter = "llm-client$")'
```

Expected: il test "anthropic" fallisce con `simulomicsr_unknown_provider`.

- [ ] **Step 3: Estendi il dispatch in `R/llm-client.R`**

Modifica il blocco di dispatch (~righe 62-81). Trova:

```r
  } else if (provider == "openai") {
    .openai_chat_structured(model = model, messages = messages,
                            response_schema = response_schema, ...)
  } else {
```

E sostituisci con:

```r
  } else if (provider == "openai") {
    .openai_chat_structured(model = model, messages = messages,
                            response_schema = response_schema, ...)
  } else if (provider == "anthropic") {
    .anthropic_chat_structured(model = model, messages = messages,
                               response_schema = response_schema, ...)
  } else {
```

E nel messaggio di errore aggiorna `Supportati: 'openai', 'mock'` in `Supportati: 'openai', 'anthropic', 'mock'`.

- [ ] **Step 4: Esegui il test e verifica che passa**

```
OPENAI_API_KEY="" Rscript --vanilla -e 'devtools::test(filter = "llm-client$")'
```

Expected: tutti i test pass.

- [ ] **Step 5: Suite completa**

```
OPENAI_API_KEY="" Rscript --vanilla -e 'devtools::test()'
```

Expected: zero failure.

- [ ] **Step 6: Commit**

```
git add R/llm-client.R tests/testthat/test-llm-client.R
git commit -m "P3.5-C Task 2: dispatch provider='anthropic' in llm_call_structured"
```

---

## Task 3: Multi-model dispatcher (`R/p35c-multi-classify.R`)

**Files:**
- Create: `R/p35c-multi-classify.R`
- Test: `tests/testthat/test-p35c-multi-classify.R`

`multi_classify_study()` riceve un GSE (con `sample_facts_list` + `study_summary`) e una lista di `model_specs` (provider+model+optional max_tokens), e ritorna una lista named per modello con il risultato di `classify_study()`. È sottile glue, ma isolato per testabilità.

- [ ] **Step 1: Scrivi i test failing**

Crea `tests/testthat/test-p35c-multi-classify.R`:

```r
test_that("multi_classify_study itera su tutti i model_specs e ritorna lista named", {
  model_specs <- list(
    list(provider = "mock", model = "m1", label = "model_1"),
    list(provider = "mock", model = "m2", label = "model_2")
  )

  fake_response <- list(
    series_id = "GSE0001",
    extraction = list(
      schema_version = "stage2.v1",
      model = "mock:m1",
      confidence = 0.5,
      ambiguity_flags = list(),
      input_sample_count = 2L,
      input_truncated = FALSE
    ),
    factors = list(),
    replicate_groups = list(
      list(
        group_id = "g1", design_role = "perturbed",
        sample_ids = list("GSM1", "GSM2"),
        factor_levels = list(), n_replicates = 2L
      )
    ),
    comparisons = list(),
    design_kind = "treatment_vs_vehicle"
  )

  out <- multi_classify_study(
    series_id = "GSE0001",
    sample_facts_list = list(
      list(geo_accession = "GSM1"), list(geo_accession = "GSM2")
    ),
    study_summary = list(title = "T", summary = "S", overall_design = "O"),
    model_specs = model_specs,
    cache = NULL,
    .mock_response = fake_response
  )

  expect_named(out, c("model_1", "model_2"))
  expect_equal(out$model_1$series_id, "GSE0001")
  expect_equal(out$model_1$extraction$model, "mock:m1")
  expect_equal(out$model_2$extraction$model, "mock:m2")
})

test_that("multi_classify_study ritorna invalid_record per il modello che fallisce", {
  model_specs <- list(
    list(provider = "mock", model = "m_ok", label = "ok"),
    list(provider = "mock", model = "m_fail", label = "fail")
  )

  out <- multi_classify_study(
    series_id = "GSE0002",
    sample_facts_list = list(list(geo_accession = "GSM1")),
    study_summary = list(title = "T", summary = "S", overall_design = "O"),
    model_specs = model_specs,
    cache = NULL,
    .mock_adapter_factory = function(label) {
      if (label == "fail") {
        function(...) stop("simulated llm error")
      } else {
        function(...) list(
          series_id = "GSE0002",
          extraction = list(
            schema_version = "stage2.v1", model = "mock:m_ok",
            confidence = 0.5, ambiguity_flags = list(),
            input_sample_count = 1L, input_truncated = FALSE
          ),
          factors = list(),
          replicate_groups = list(list(
            group_id = "g1", design_role = "perturbed",
            sample_ids = list("GSM1"),
            factor_levels = list(), n_replicates = 1L
          )),
          comparisons = list(),
          design_kind = "treatment_vs_vehicle"
        )
      }
    }
  )

  expect_named(out, c("ok", "fail"))
  expect_null(out$ok$.invalid_reason)
  expect_equal(out$fail$.invalid_reason, "llm_call_failed")
})
```

- [ ] **Step 2: Esegui e verifica che falliscono**

```
OPENAI_API_KEY="" Rscript --vanilla -e 'devtools::test(filter = "p35c-multi-classify")'
```

Expected: errors "could not find function `multi_classify_study`".

- [ ] **Step 3: Implementa `multi_classify_study()` in `R/p35c-multi-classify.R`**

Crea `R/p35c-multi-classify.R`:

```r
#' Classifica uno studio con piu' modelli LLM e ritorna i risultati comparabili
#'
#' Wrapper su `classify_study()` che itera sui `model_specs` e ritorna una lista
#' nominata per `label`. Stage 1 fissato (sample_facts_list passato uguale a tutti).
#' La cache LLM esistente partitziona automaticamente per `(provider, model, messages)`,
#' quindi modelli diversi non si sovrascrivono.
#'
#' Gestione errori per-modello: se un modello solleva, il record diventa
#' `.invalid_reason='llm_call_failed'` (consistente con `classify_study()`).
#' I modelli che riescono restano disponibili nei loro slot.
#'
#' @param series_id GSE accession
#' @param sample_facts_list lista di sample_facts validati (stage1.v3)
#' @param study_summary list con title/summary/overall_design
#' @param model_specs lista di liste, ognuna con campi `provider`, `model`,
#'   `label` (slug univoco, usato come name di output), e opzionalmente
#'   `max_tokens`.
#' @param cache cache object (puo' essere NULL nei test mockati)
#' @param ... args extra passati a llm_call_structured (es. `.mock_response`)
#' @param .mock_adapter_factory (test only) funzione che riceve `label` e
#'   ritorna il `.mock_adapter` da iniettare per quel modello.
#'
#' @return list nominata per `label` (uno slot per modello), valore = output di
#'   `classify_study()` (study_design valido oppure invalid_record).
#'
#' @export
multi_classify_study <- function(series_id,
                                 sample_facts_list,
                                 study_summary,
                                 model_specs,
                                 cache,
                                 ...,
                                 .mock_adapter_factory = NULL) {
  stopifnot(is.list(model_specs), length(model_specs) >= 1L)

  out <- list()
  for (spec in model_specs) {
    stopifnot(all(c("provider", "model", "label") %in% names(spec)))

    extra_args <- list(...)
    if (!is.null(.mock_adapter_factory)) {
      extra_args$.mock_adapter <- .mock_adapter_factory(spec$label)
    }
    if (!is.null(spec$max_tokens)) {
      extra_args$max_tokens <- spec$max_tokens
    }

    out[[spec$label]] <- do.call(
      classify_study,
      c(
        list(
          series_id         = series_id,
          sample_facts_list = sample_facts_list,
          study_summary     = study_summary,
          provider          = spec$provider,
          model             = spec$model,
          cache             = cache
        ),
        extra_args
      )
    )
  }
  out
}
```

- [ ] **Step 4: Verifica che i test passano**

```
OPENAI_API_KEY="" Rscript --vanilla -e 'devtools::test(filter = "p35c-multi-classify")'
```

Expected: 2/2 PASS.

- [ ] **Step 5: Suite completa + document**

```
Rscript --vanilla -e 'devtools::document()'
OPENAI_API_KEY="" Rscript --vanilla -e 'devtools::test()'
```

Expected: zero failure.

- [ ] **Step 6: Commit**

```
git add R/p35c-multi-classify.R tests/testthat/test-p35c-multi-classify.R man/
git commit -m "P3.5-C Task 3: multi_classify_study dispatcher multi-modello"
```

---

## Task 4: Confidence score (`R/p35c-confidence.R`)

**Files:**
- Create: `R/p35c-confidence.R`
- Test: `tests/testthat/test-p35c-confidence.R`

Tre funzioni:
- `compute_pairwise_agreement(study_designs_per_model, sample_facts_list)`: per ogni coppia di modelli, calcola l'agreement su `design_kind`, `design_role` (per sample), `comparability_anchor` (per comparison).
- `aggregate_confidence_score(pairwise_agreements)`: media pesata pesi 0.3/0.5/0.2.
- `assign_difficulty_tier(score)`: easy ≥0.85, medium 0.6-0.85, hard <0.6.

- [ ] **Step 1: Scrivi i test failing**

Crea `tests/testthat/test-p35c-confidence.R`:

```r
.fake_design <- function(design_kind, role_per_sample, anchors = character()) {
  list(
    design_kind = design_kind,
    replicate_groups = lapply(seq_along(role_per_sample), function(i) {
      list(
        group_id = paste0("g", i),
        design_role = role_per_sample[[i]],
        sample_ids = list(paste0("GSM", i)),
        factor_levels = list(),
        n_replicates = 1L
      )
    }),
    comparisons = lapply(anchors, function(a) {
      list(
        comparison_id = paste0("c_", a),
        comparability_anchor = a
      )
    })
  )
}

test_that("compute_pairwise_agreement: identici -> agreement 1 su tutti i campi", {
  d1 <- .fake_design("treatment_vs_vehicle", c("perturbed", "vehicle_control"),
                    anchors = c("anchorA"))
  pa <- compute_pairwise_agreement(list(model_a = d1, model_b = d1))

  expect_equal(nrow(pa), 1L)
  expect_equal(pa$pair[1], "model_a__model_b")
  expect_equal(pa$design_kind_match[1], 1)
  expect_equal(pa$design_role_match_rate[1], 1)
  expect_equal(pa$anchor_match_rate[1], 1)
})

test_that("compute_pairwise_agreement: design_kind diversi -> 0 su quel campo", {
  d1 <- .fake_design("treatment_vs_vehicle", c("perturbed"))
  d2 <- .fake_design("case_control_disease", c("perturbed"))
  pa <- compute_pairwise_agreement(list(a = d1, b = d2))
  expect_equal(pa$design_kind_match[1], 0)
  expect_equal(pa$design_role_match_rate[1], 1)
})

test_that("compute_pairwise_agreement: design_role differente in 1 sample su 2 -> 0.5", {
  d1 <- .fake_design("treatment_vs_vehicle", c("perturbed", "vehicle_control"))
  d2 <- .fake_design("treatment_vs_vehicle", c("perturbed", "untreated_control"))
  pa <- compute_pairwise_agreement(list(a = d1, b = d2))
  expect_equal(pa$design_role_match_rate[1], 0.5)
})

test_that("aggregate_confidence_score: media pesata 0.3/0.5/0.2", {
  pa <- tibble::tibble(
    pair = "a__b",
    design_kind_match = 1,
    design_role_match_rate = 0.5,
    anchor_match_rate = 0.0
  )
  expect_equal(aggregate_confidence_score(pa),
               0.3 * 1 + 0.5 * 0.5 + 0.2 * 0.0)
})

test_that("aggregate_confidence_score: media tra le coppie quando ce ne sono 3", {
  pa <- tibble::tibble(
    pair = c("a__b", "a__c", "b__c"),
    design_kind_match = c(1, 1, 1),
    design_role_match_rate = c(1, 0.5, 0.5),
    anchor_match_rate = c(1, 1, 1)
  )
  pair_scores <- 0.3 * pa$design_kind_match +
                 0.5 * pa$design_role_match_rate +
                 0.2 * pa$anchor_match_rate
  expect_equal(aggregate_confidence_score(pa), mean(pair_scores))
})

test_that("assign_difficulty_tier: rispetta soglie 0.85 / 0.6", {
  expect_equal(assign_difficulty_tier(0.95), "easy")
  expect_equal(assign_difficulty_tier(0.85), "easy")
  expect_equal(assign_difficulty_tier(0.84), "medium")
  expect_equal(assign_difficulty_tier(0.6), "medium")
  expect_equal(assign_difficulty_tier(0.59), "hard")
  expect_equal(assign_difficulty_tier(0.0), "hard")
})

test_that("compute_pairwise_agreement gestisce study_designs invalidi (skip)", {
  d_ok <- .fake_design("treatment_vs_vehicle", c("perturbed"))
  d_bad <- list(.invalid_reason = "schema_validation_failed")
  pa <- compute_pairwise_agreement(list(a = d_ok, b = d_bad))
  expect_equal(nrow(pa), 0L)
})
```

- [ ] **Step 2: Verifica che falliscono**

```
OPENAI_API_KEY="" Rscript --vanilla -e 'devtools::test(filter = "p35c-confidence")'
```

Expected: errori "could not find function `compute_pairwise_agreement`".

- [ ] **Step 3: Implementa le tre funzioni in `R/p35c-confidence.R`**

Crea il file:

```r
#' Calcola pairwise agreement tra modelli su un singolo studio
#'
#' Per ogni coppia di modelli, confronta:
#' - `design_kind` (match esatto: 1/0)
#' - `design_role` per ogni sample condiviso (rate: frazione di sample con
#'   stesso design_role assegnato dai due modelli)
#' - `comparability_anchor` per comparison (rate: frazione di anchor in comune
#'   sul totale unione)
#'
#' Studi con `.invalid_reason` (LLM error / schema fail) vengono saltati: la
#' coppia che li include non produce nessuna riga.
#'
#' @param study_designs_per_model lista nominata per modello, ogni elemento
#'   e' un study_design valido stage2.v1 oppure un invalid_record.
#'
#' @return tibble con colonne pair, design_kind_match, design_role_match_rate,
#'   anchor_match_rate (una riga per coppia di modelli valida).
#'
#' @export
compute_pairwise_agreement <- function(study_designs_per_model) {
  labels <- names(study_designs_per_model)
  if (length(labels) < 2L) return(.empty_pairwise_agreement())

  pairs <- utils::combn(labels, 2L, simplify = FALSE)
  rows <- lapply(pairs, function(p) {
    a <- study_designs_per_model[[p[1]]]
    b <- study_designs_per_model[[p[2]]]
    if (.is_invalid_design(a) || .is_invalid_design(b)) return(NULL)

    tibble::tibble(
      pair                   = paste(p, collapse = "__"),
      design_kind_match      = as.integer(identical(a$design_kind, b$design_kind)),
      design_role_match_rate = .role_match_rate(a, b),
      anchor_match_rate      = .anchor_match_rate(a, b)
    )
  })
  rows <- Filter(Negate(is.null), rows)
  if (length(rows) == 0L) return(.empty_pairwise_agreement())
  dplyr::bind_rows(rows)
}

#' @noRd
.is_invalid_design <- function(d) {
  isTRUE(!is.null(d$.invalid_reason))
}

#' @noRd
.empty_pairwise_agreement <- function() {
  tibble::tibble(
    pair                   = character(),
    design_kind_match      = integer(),
    design_role_match_rate = double(),
    anchor_match_rate      = double()
  )
}

#' @noRd
.role_per_sample <- function(design) {
  out <- list()
  for (g in design$replicate_groups %||% list()) {
    role <- g$design_role
    for (sid in g$sample_ids %||% list()) {
      out[[as.character(sid)]] <- role
    }
  }
  out
}

#' @noRd
.role_match_rate <- function(a, b) {
  ra <- .role_per_sample(a)
  rb <- .role_per_sample(b)
  shared <- intersect(names(ra), names(rb))
  if (length(shared) == 0L) return(NA_real_)
  match_count <- sum(vapply(shared, function(s) identical(ra[[s]], rb[[s]]),
                            logical(1)))
  match_count / length(shared)
}

#' @noRd
.anchor_match_rate <- function(a, b) {
  anchors_a <- vapply(a$comparisons %||% list(),
                      function(c) as.character(c$comparability_anchor %||% NA),
                      character(1))
  anchors_b <- vapply(b$comparisons %||% list(),
                      function(c) as.character(c$comparability_anchor %||% NA),
                      character(1))
  if (length(anchors_a) == 0L && length(anchors_b) == 0L) return(1)
  union_n <- length(union(anchors_a, anchors_b))
  inter_n <- length(intersect(anchors_a, anchors_b))
  if (union_n == 0L) return(NA_real_)
  inter_n / union_n
}

#' Aggrega pairwise agreement in un singolo confidence score
#'
#' Per ogni coppia: pair_score = 0.3*design_kind + 0.5*role_rate + 0.2*anchor_rate.
#' Valori NA in role/anchor_rate ricevono peso effettivo 0.
#' Score finale = media dei pair_score sulle coppie disponibili.
#'
#' @param pairwise_agreements tibble da `compute_pairwise_agreement()`
#' @return numeric scalar in [0, 1], oppure NA_real_ se 0 coppie valide
#' @export
aggregate_confidence_score <- function(pairwise_agreements) {
  if (nrow(pairwise_agreements) == 0L) return(NA_real_)
  role <- pairwise_agreements$design_role_match_rate
  anch <- pairwise_agreements$anchor_match_rate
  role[is.na(role)] <- 0
  anch[is.na(anch)] <- 0
  pair_scores <- 0.3 * pairwise_agreements$design_kind_match +
                 0.5 * role +
                 0.2 * anch
  mean(pair_scores)
}

#' Assegna tier di difficolta' a un confidence score
#'
#' Soglie preliminari (spec sec.6.1): easy ge 0.85, medium 0.6-0.85, hard lt 0.6.
#'
#' @param score numeric in [0, 1]
#' @return character: "easy", "medium" o "hard"
#' @export
assign_difficulty_tier <- function(score) {
  if (is.na(score)) return(NA_character_)
  if (score >= 0.85) return("easy")
  if (score >= 0.6)  return("medium")
  "hard"
}
```

- [ ] **Step 4: Verifica i test**

```
OPENAI_API_KEY="" Rscript --vanilla -e 'devtools::test(filter = "p35c-confidence")'
```

Expected: 7/7 PASS.

- [ ] **Step 5: Document + suite completa + check**

```
Rscript --vanilla -e 'devtools::document()'
OPENAI_API_KEY="" Rscript --vanilla -e 'devtools::test()'
Rscript --vanilla -e 'devtools::check(error_on = "warning", document = FALSE)'
```

Expected: zero failure, zero check warnings.

- [ ] **Step 6: Commit**

```
git add R/p35c-confidence.R tests/testthat/test-p35c-confidence.R man/
git commit -m "P3.5-C Task 4: confidence score via cross-model agreement (3 funzioni)"
```

---

## Task 5: Mini-gold sampling stratificato (`R/p35c-minigold.R` parte 1)

**Files:**
- Create: `R/p35c-minigold.R`
- Test: `tests/testthat/test-p35c-minigold.R`

Funzione `sample_minigold_stratified()`: dato un tibble di GSE con `confidence_score` e `tier`, e un dataset di sample (con `geo_accession`, `series_id`, `string`), produce un sub-set di N sample stratificato 50/50 easy/hard, distribuito su almeno K GSE distinti per tier.

- [ ] **Step 1: Scrivi i test failing**

Crea `tests/testthat/test-p35c-minigold.R`:

```r
.mock_gse_tiers <- function() {
  tibble::tibble(
    series_id = c(paste0("GSE_E", 1:10), paste0("GSE_H", 1:10)),
    confidence_score = c(rep(0.95, 10), rep(0.4, 10)),
    tier = c(rep("easy", 10), rep("hard", 10))
  )
}

.mock_samples <- function() {
  # 5 sample per ogni GSE
  tidyr::expand_grid(
    series_id = c(paste0("GSE_E", 1:10), paste0("GSE_H", 1:10)),
    sample_idx = 1:5
  ) |>
    dplyr::mutate(
      geo_accession = paste0(series_id, "_GSM", sample_idx),
      string = paste("fake string for", geo_accession)
    ) |>
    dplyr::select(geo_accession, series_id, string)
}

test_that("sample_minigold_stratified: 50/50 easy/hard", {
  set.seed(1812)
  out <- sample_minigold_stratified(
    gse_tiers = .mock_gse_tiers(),
    samples_table = .mock_samples(),
    target_n = 100L,
    min_gse_per_tier = 5L
  )
  expect_equal(nrow(out), 100L)
  expect_equal(sum(out$tier == "easy"), 50L)
  expect_equal(sum(out$tier == "hard"), 50L)
})

test_that("sample_minigold_stratified: distribuisce su almeno min_gse_per_tier", {
  set.seed(1812)
  out <- sample_minigold_stratified(
    gse_tiers = .mock_gse_tiers(),
    samples_table = .mock_samples(),
    target_n = 100L,
    min_gse_per_tier = 5L
  )
  easy_gse <- length(unique(out$series_id[out$tier == "easy"]))
  hard_gse <- length(unique(out$series_id[out$tier == "hard"]))
  expect_gte(easy_gse, 5L)
  expect_gte(hard_gse, 5L)
})

test_that("sample_minigold_stratified e' deterministico col seed", {
  set.seed(1812)
  out1 <- sample_minigold_stratified(
    gse_tiers = .mock_gse_tiers(),
    samples_table = .mock_samples(),
    target_n = 50L,
    min_gse_per_tier = 5L
  )
  set.seed(1812)
  out2 <- sample_minigold_stratified(
    gse_tiers = .mock_gse_tiers(),
    samples_table = .mock_samples(),
    target_n = 50L,
    min_gse_per_tier = 5L
  )
  expect_equal(out1$geo_accession, out2$geo_accession)
})

test_that("sample_minigold_stratified: solleva se non ci sono abbastanza GSE per un tier", {
  small <- tibble::tibble(
    series_id = c("GSE_E1", "GSE_H1"),
    confidence_score = c(0.95, 0.4),
    tier = c("easy", "hard")
  )
  expect_error(
    sample_minigold_stratified(
      gse_tiers = small,
      samples_table = .mock_samples()[1:5, ],
      target_n = 100L,
      min_gse_per_tier = 5L
    ),
    class = "simulomicsr_minigold_insufficient_gse"
  )
})
```

- [ ] **Step 2: Verifica che falliscono**

```
OPENAI_API_KEY="" Rscript --vanilla -e 'devtools::test(filter = "p35c-minigold")'
```

Expected: errors "could not find function".

- [ ] **Step 3: Implementa `sample_minigold_stratified()`**

Crea `R/p35c-minigold.R`:

```r
#' Sampling stratificato per mini-gold design-aware
#'
#' Estrae N sample distribuiti 50/50 easy/hard, con almeno `min_gse_per_tier`
#' GSE distinti per tier (per evitare di studiare un singolo GSE anomalo).
#' Algoritmo: per ogni tier, seleziona min_gse_per_tier GSE casuali, e
#' campiona uniformemente i sample fino a target_n / 2.
#'
#' Errori tipizzati:
#' - `simulomicsr_minigold_insufficient_gse` se un tier ha meno di
#'   min_gse_per_tier GSE.
#'
#' @param gse_tiers tibble con colonne `series_id`, `confidence_score`, `tier`
#' @param samples_table tibble con `geo_accession`, `series_id`, `string`
#'   (tutti i sample candidati)
#' @param target_n totale sample (50/50 easy/hard)
#' @param min_gse_per_tier minimo numero GSE distinti da coprire per tier
#'
#' @return tibble con colonne geo_accession, series_id, string, tier,
#'   confidence_score
#'
#' @export
sample_minigold_stratified <- function(gse_tiers,
                                       samples_table,
                                       target_n = 100L,
                                       min_gse_per_tier = 15L) {
  stopifnot(target_n %% 2L == 0L)
  per_tier_n <- target_n %/% 2L

  out <- list()
  for (t in c("easy", "hard")) {
    pool <- gse_tiers[gse_tiers$tier == t, ]
    if (nrow(pool) < min_gse_per_tier) {
      rlang::abort(
        sprintf("Tier '%s' ha solo %d GSE (richiesti almeno %d).",
                t, nrow(pool), min_gse_per_tier),
        class = "simulomicsr_minigold_insufficient_gse",
        tier = t, n_gse = nrow(pool)
      )
    }
    selected_gse <- pool$series_id[sample.int(nrow(pool), min_gse_per_tier)]
    samples_pool <- samples_table[samples_table$series_id %in% selected_gse, ]
    take <- min(nrow(samples_pool), per_tier_n)
    picked_idx <- sample.int(nrow(samples_pool), take)
    picked <- samples_pool[picked_idx, ]
    picked$tier <- t
    picked <- dplyr::left_join(
      picked,
      gse_tiers[, c("series_id", "confidence_score")],
      by = "series_id"
    )
    out[[t]] <- picked
  }
  dplyr::bind_rows(out)
}
```

- [ ] **Step 4: Verifica i test**

```
OPENAI_API_KEY="" Rscript --vanilla -e 'devtools::test(filter = "p35c-minigold")'
```

Expected: 4/4 PASS.

- [ ] **Step 5: Commit**

```
git add R/p35c-minigold.R tests/testthat/test-p35c-minigold.R
git commit -m "P3.5-C Task 5: sample_minigold_stratified (50/50 easy/hard, K GSE distinti)"
```

---

## Task 6: Mini-gold export CSV + import + eval

**Files:**
- Modify: `R/p35c-minigold.R` (add 3 functions)
- Modify: `tests/testthat/test-p35c-minigold.R` (add tests)
- Create: `inst/extdata/p35c-minigold-template-empty.csv`

Tre funzioni nuove:
- `export_minigold_csv(minigold_pool, study_summaries, multi_classify_outputs, dest_path)`: produce un CSV con colonne pre-popolate per la review umana.
- `import_minigold_reviewed(csv_path)`: legge il CSV reviewato dall'utente, valida le colonne `design_role_gold` e `design_kind_gold`.
- `eval_against_minigold(reviewed, multi_classify_outputs)`: per ogni modello, accuracy globale + per tier sui sample reviewati.

- [ ] **Step 1: Crea `inst/extdata/p35c-minigold-template-empty.csv`**

Contenuto (header soltanto, riga vuota di esempio commentata via `#`):

```csv
geo_accession,series_id,string,study_title,study_summary,design_role_proposed_models,design_kind_proposed_models,design_role_gold,design_kind_gold,comment_optional,tier
```

Salva il file. È committato vuoto come riferimento dello schema CSV.

- [ ] **Step 2: Aggiungi i test failing per le 3 funzioni**

Aggiungi a `tests/testthat/test-p35c-minigold.R`:

```r
test_that("export_minigold_csv produce file con header atteso e righe per ogni sample", {
  pool <- tibble::tibble(
    geo_accession = c("GSE1_GSM1", "GSE1_GSM2"),
    series_id     = c("GSE1", "GSE1"),
    string        = c("s1", "s2"),
    tier          = c("easy", "easy"),
    confidence_score = c(0.95, 0.95)
  )
  study_summaries <- list(GSE1 = list(title = "T1", summary = "S1"))
  multi_outputs <- list(GSE1 = list(
    model_a = list(
      design_kind = "treatment_vs_vehicle",
      replicate_groups = list(
        list(group_id = "g1", design_role = "perturbed",  sample_ids = list("GSE1_GSM1")),
        list(group_id = "g2", design_role = "vehicle_control", sample_ids = list("GSE1_GSM2"))
      )
    ),
    model_b = list(
      design_kind = "case_control_disease",
      replicate_groups = list(
        list(group_id = "g1", design_role = "case", sample_ids = list("GSE1_GSM1")),
        list(group_id = "g2", design_role = "comparison", sample_ids = list("GSE1_GSM2"))
      )
    )
  ))
  dest <- tempfile(fileext = ".csv")
  export_minigold_csv(pool, study_summaries, multi_outputs, dest)

  csv <- readr::read_csv(dest, show_col_types = FALSE)
  expect_setequal(
    colnames(csv),
    c("geo_accession","series_id","string","study_title","study_summary",
      "design_role_proposed_models","design_kind_proposed_models",
      "design_role_gold","design_kind_gold","comment_optional","tier")
  )
  expect_equal(nrow(csv), 2L)
  expect_match(csv$design_role_proposed_models[1], "model_a=perturbed")
  expect_match(csv$design_role_proposed_models[1], "model_b=case")
  expect_true(all(is.na(csv$design_role_gold)))
})

test_that("import_minigold_reviewed valida colonne richieste e tipi", {
  good <- tibble::tibble(
    geo_accession = c("X1", "X2"),
    series_id     = c("S1", "S1"),
    string        = c("a", "b"),
    study_title   = c("T", "T"),
    study_summary = c("S", "S"),
    design_role_proposed_models = c("a=x", "b=y"),
    design_kind_proposed_models = c("a=x", "b=y"),
    design_role_gold = c("perturbed", "vehicle_control"),
    design_kind_gold = c("treatment_vs_vehicle", "treatment_vs_vehicle"),
    comment_optional = c("", ""),
    tier = c("easy", "easy")
  )
  csv_path <- tempfile(fileext = ".csv")
  readr::write_csv(good, csv_path)

  out <- import_minigold_reviewed(csv_path)
  expect_equal(nrow(out), 2L)
  expect_true("design_role_gold" %in% colnames(out))
})

test_that("import_minigold_reviewed solleva se design_role_gold contiene un valore fuori vocabolario", {
  bad <- tibble::tibble(
    geo_accession = "X", series_id = "S", string = "s",
    study_title = "t", study_summary = "u",
    design_role_proposed_models = "", design_kind_proposed_models = "",
    design_role_gold = "perturbatore_inventato",  # NON nei 13 valori
    design_kind_gold = "treatment_vs_vehicle",
    comment_optional = "", tier = "easy"
  )
  csv_path <- tempfile(fileext = ".csv")
  readr::write_csv(bad, csv_path)
  expect_error(
    import_minigold_reviewed(csv_path),
    class = "simulomicsr_minigold_invalid_value"
  )
})

test_that("eval_against_minigold calcola accuracy per modello e per tier", {
  reviewed <- tibble::tibble(
    geo_accession = c("X1", "X2", "X3", "X4"),
    series_id     = c("S1", "S1", "S2", "S2"),
    design_role_gold = c("perturbed", "vehicle_control", "case", "comparison"),
    tier = c("easy", "easy", "hard", "hard")
  )
  multi_outputs <- list(
    S1 = list(
      model_a = list(replicate_groups = list(
        list(design_role = "perturbed",       sample_ids = list("X1")),
        list(design_role = "vehicle_control", sample_ids = list("X2"))
      ))
    ),
    S2 = list(
      model_a = list(replicate_groups = list(
        list(design_role = "perturbed",       sample_ids = list("X3")),  # WRONG (gold=case)
        list(design_role = "comparison",      sample_ids = list("X4"))
      ))
    )
  )

  acc <- eval_against_minigold(reviewed, multi_outputs)
  # Per model_a: 3/4 corretti -> accuracy 0.75; easy 2/2; hard 1/2
  row_overall <- acc[acc$model == "model_a" & acc$tier == "overall", ]
  expect_equal(row_overall$accuracy, 0.75)
  row_easy <- acc[acc$model == "model_a" & acc$tier == "easy", ]
  expect_equal(row_easy$accuracy, 1.0)
  row_hard <- acc[acc$model == "model_a" & acc$tier == "hard", ]
  expect_equal(row_hard$accuracy, 0.5)
})
```

- [ ] **Step 3: Verifica che i 4 nuovi test falliscono**

```
OPENAI_API_KEY="" Rscript --vanilla -e 'devtools::test(filter = "p35c-minigold")'
```

Expected: 4 nuovi errori "could not find function".

- [ ] **Step 4: Implementa le 3 funzioni nello stesso file `R/p35c-minigold.R`**

Aggiungi in coda al file:

```r
.MINIGOLD_CSV_COLS <- c(
  "geo_accession", "series_id", "string", "study_title", "study_summary",
  "design_role_proposed_models", "design_kind_proposed_models",
  "design_role_gold", "design_kind_gold", "comment_optional", "tier"
)

# Vocabolario design_role v3 (13 valori) - duplica spec stage2.v1 per
# uso runtime senza dipendenza da schema JSON (validazione human-friendly).
.VALID_DESIGN_ROLES <- c(
  "perturbed", "vehicle_control", "untreated_control",
  "negative_genetic_control", "negative_inducer_control",
  "positive_control", "baseline_t0", "case", "comparison",
  "bystander", "secondary_arm", "excluded", "unclear"
)

.VALID_DESIGN_KINDS <- c(
  "case_control_disease", "treatment_vs_vehicle", "treatment_vs_untreated",
  "time_course", "dose_response", "knockdown_panel", "factorial",
  "differentiation_course", "multi_arm_treatment", "unclear"
)

#' Esporta il template CSV per la review umana del mini-gold
#'
#' Per ogni sample del pool, pre-popola: stringa GEO, study title/summary,
#' role/kind proposti dai modelli (concatenati), tier. Lascia vuote
#' design_role_gold e design_kind_gold (la review umana le compila).
#'
#' @param minigold_pool tibble da `sample_minigold_stratified()`
#' @param study_summaries lista nominata per series_id con title/summary
#' @param multi_classify_outputs lista nominata per series_id, ogni elemento
#'   e' la lista nominata per modello (output di `multi_classify_study()`)
#' @param dest_path path del CSV di destinazione
#'
#' @return invisible(dest_path)
#'
#' @export
export_minigold_csv <- function(minigold_pool,
                                study_summaries,
                                multi_classify_outputs,
                                dest_path) {
  rows <- lapply(seq_len(nrow(minigold_pool)), function(i) {
    row <- minigold_pool[i, ]
    sid <- row$series_id
    gsm <- row$geo_accession
    sumr <- study_summaries[[sid]] %||% list(title = "", summary = "")
    multi <- multi_classify_outputs[[sid]] %||% list()

    role_per_model <- vapply(names(multi), function(label) {
      d <- multi[[label]]
      if (.is_invalid_design(d)) return(paste0(label, "=ERROR"))
      role <- .lookup_role_for_sample(d, gsm)
      paste0(label, "=", role %||% "NA")
    }, character(1))

    kind_per_model <- vapply(names(multi), function(label) {
      d <- multi[[label]]
      if (.is_invalid_design(d)) return(paste0(label, "=ERROR"))
      paste0(label, "=", d$design_kind %||% "NA")
    }, character(1))

    tibble::tibble(
      geo_accession = gsm,
      series_id = sid,
      string = row$string %||% "",
      study_title = sumr$title %||% "",
      study_summary = sumr$summary %||% "",
      design_role_proposed_models = paste(role_per_model, collapse = "; "),
      design_kind_proposed_models = paste(kind_per_model, collapse = "; "),
      design_role_gold = NA_character_,
      design_kind_gold = NA_character_,
      comment_optional = NA_character_,
      tier = row$tier %||% NA_character_
    )
  })
  out <- dplyr::bind_rows(rows)
  readr::write_csv(out, dest_path, na = "")
  invisible(dest_path)
}

#' @noRd
.lookup_role_for_sample <- function(design, geo_accession) {
  for (g in design$replicate_groups %||% list()) {
    if (geo_accession %in% unlist(g$sample_ids %||% list())) {
      return(g$design_role)
    }
  }
  NULL
}

#' Importa il CSV reviewato dall'utente e valida i valori gold
#'
#' Errori tipizzati:
#' - `simulomicsr_minigold_invalid_value` se design_role_gold o design_kind_gold
#'   contengono valori fuori dal vocabolario v3.
#'
#' @param csv_path path del CSV reviewato
#' @return tibble con tutte le colonne del template, righe filtrate alle
#'   sole con design_role_gold non NA (la review puo' lasciare in bianco
#'   sample non review-abili).
#'
#' @export
import_minigold_reviewed <- function(csv_path) {
  reviewed <- readr::read_csv(csv_path, show_col_types = FALSE,
                              progress = FALSE)
  missing_cols <- setdiff(.MINIGOLD_CSV_COLS, colnames(reviewed))
  if (length(missing_cols) > 0L) {
    rlang::abort(
      sprintf("CSV reviewato manca delle colonne: %s",
              paste(missing_cols, collapse = ", ")),
      class = "simulomicsr_minigold_missing_cols"
    )
  }

  reviewed <- reviewed[!is.na(reviewed$design_role_gold), ]
  bad_roles <- setdiff(unique(reviewed$design_role_gold), .VALID_DESIGN_ROLES)
  if (length(bad_roles) > 0L) {
    rlang::abort(
      sprintf("design_role_gold ha valori fuori vocabolario: %s",
              paste(bad_roles, collapse = ", ")),
      class = "simulomicsr_minigold_invalid_value",
      bad_values = bad_roles
    )
  }
  bad_kinds <- setdiff(unique(reviewed$design_kind_gold), .VALID_DESIGN_KINDS)
  if (length(bad_kinds) > 0L) {
    rlang::abort(
      sprintf("design_kind_gold ha valori fuori vocabolario: %s",
              paste(bad_kinds, collapse = ", ")),
      class = "simulomicsr_minigold_invalid_value",
      bad_values = bad_kinds
    )
  }
  reviewed
}

#' Calcola accuracy di ogni modello vs mini-gold reviewato
#'
#' Per ogni (model, tier in {overall, easy, hard}), restituisce accuracy
#' del `design_role` predetto vs `design_role_gold`.
#'
#' @param reviewed tibble da `import_minigold_reviewed()`
#' @param multi_classify_outputs lista nominata per series_id (come in
#'   `export_minigold_csv()`)
#'
#' @return tibble con colonne model, tier, n, n_correct, accuracy
#'
#' @export
eval_against_minigold <- function(reviewed, multi_classify_outputs) {
  model_labels <- unique(unlist(lapply(multi_classify_outputs, names)))

  per_sample_rows <- lapply(seq_len(nrow(reviewed)), function(i) {
    row <- reviewed[i, ]
    sid <- row$series_id
    gsm <- row$geo_accession
    multi <- multi_classify_outputs[[sid]] %||% list()
    do.call(rbind, lapply(model_labels, function(label) {
      pred <- .lookup_role_for_sample(multi[[label]], gsm)
      tibble::tibble(
        geo_accession = gsm,
        tier = row$tier,
        model = label,
        gold = row$design_role_gold,
        pred = pred %||% NA_character_,
        correct = identical(pred, row$design_role_gold)
      )
    }))
  })
  per_sample <- dplyr::bind_rows(per_sample_rows)

  by_groups <- dplyr::bind_rows(
    per_sample |>
      dplyr::group_by(.data$model) |>
      dplyr::summarise(
        tier = "overall",
        n = dplyr::n(),
        n_correct = sum(.data$correct),
        accuracy = mean(.data$correct),
        .groups = "drop"
      ),
    per_sample |>
      dplyr::group_by(.data$model, tier = .data$tier) |>
      dplyr::summarise(
        n = dplyr::n(),
        n_correct = sum(.data$correct),
        accuracy = mean(.data$correct),
        .groups = "drop"
      )
  )
  by_groups |>
    dplyr::select(.data$model, .data$tier, .data$n, .data$n_correct,
                  .data$accuracy) |>
    dplyr::arrange(.data$model, .data$tier)
}
```

- [ ] **Step 5: Verifica i test**

```
OPENAI_API_KEY="" Rscript --vanilla -e 'devtools::test(filter = "p35c-minigold")'
```

Expected: 8/8 PASS (4 vecchi + 4 nuovi).

- [ ] **Step 6: Document + suite + check**

```
Rscript --vanilla -e 'devtools::document()'
OPENAI_API_KEY="" Rscript --vanilla -e 'devtools::test()'
Rscript --vanilla -e 'devtools::check(error_on = "warning", document = FALSE)'
```

Expected: zero failure.

- [ ] **Step 7: Commit**

```
git add R/p35c-minigold.R tests/testthat/test-p35c-minigold.R inst/extdata/p35c-minigold-template-empty.csv man/
git commit -m "P3.5-C Task 6: mini-gold export/import CSV + eval_against_minigold"
```

---

## Task 7: Estensione `analysis/_targets.R` — sub-set selection + multi-classify

**Files:**
- Modify: `analysis/_targets.R` (append new targets)

Aggiungiamo target:
- `curated_p35c_gse`: 50 GSE selezionati dai 100 di P3.5-A, stratificati su `design_kind`.
- `model_specs_p35c`: hard-coded list of 5 modelli (nuovi 4 + gpt-5.5 baseline).
- `study_designs_p35c_<label>`: una serie di target dinamici, uno per modello (escluso gpt-5.5 che riusiamo da P3.5-A).
- `multi_classify_outputs_p35c`: combina P3.5-A gpt-5.5 + nuovi modelli in una lista per GSE.

- [ ] **Step 1: Identifica il punto di inserimento**

Apri `analysis/_targets.R`, vai a fine file (riga 817). Aggiungi i nuovi target **prima** della parentesi chiusa finale `)` della list ritornata. Verifica che l'ultima riga del file pre-modifica sia `)` chiusa.

```
tail -5 analysis/_targets.R
```

Expected ultima riga: `)`.

- [ ] **Step 2: Scrivi i nuovi target nel file**

Inserisci, immediatamente prima dell'ultima `)`:

```r
  ,

  # ===========================================================================
  # P3.5-C: Confidence-aware classification (multi-model + mini-gold)
  # Spec: docs/superpowers/specs/2026-05-04-p3.5c-confidence-aware-design.md
  # ===========================================================================

  tar_target(
    curated_p35c_gse,
    {
      # Stratifica i 100 GSE P3.5-A per design_kind LLM-inferito
      validated <- study_designs_p35a_validated
      kinds <- vapply(validated, function(d) d$design_kind %||% "unclear",
                      character(1))
      sids  <- vapply(validated, function(d) d$series_id, character(1))
      tab <- tibble::tibble(series_id = sids, design_kind = kinds)

      easy_kinds <- c("treatment_vs_vehicle", "dose_response")
      hard_kinds <- c("case_control_disease", "time_course",
                      "treatment_vs_untreated")
      mid_kinds  <- c("factorial", "multi_arm_treatment")

      set.seed(1812)
      take <- function(sub_kinds, n_target) {
        pool <- tab[tab$design_kind %in% sub_kinds, ]
        if (nrow(pool) <= n_target) return(pool$series_id)
        pool$series_id[sample.int(nrow(pool), n_target)]
      }
      selected <- unique(c(
        take(easy_kinds, 15L),
        take(hard_kinds, 15L),
        take(mid_kinds, 10L)
      ))
      remaining <- setdiff(tab$series_id, selected)
      pad_n <- max(0L, 50L - length(selected))
      if (pad_n > 0L && length(remaining) > 0L) {
        pad <- remaining[sample.int(length(remaining), min(pad_n, length(remaining)))]
        selected <- c(selected, pad)
      }
      selected[seq_len(min(50L, length(selected)))]
    }
  ),

  tar_target(
    model_specs_p35c,
    list(
      list(provider = "openai",    model = "gpt-5.5",            label = "gpt_5_5"),
      list(provider = "openai",    model = "gpt-5.4-mini",       label = "gpt_5_4_mini"),
      list(provider = "openai",    model = "gpt-5.4-nano",       label = "gpt_5_4_nano"),
      list(provider = "anthropic", model = "claude-haiku-4-5",   label = "claude_haiku_4_5"),
      list(provider = "anthropic", model = "claude-sonnet-4-6",  label = "claude_sonnet_4_6")
    )
  ),

  tar_target(
    multi_classify_outputs_p35c,
    {
      sids <- curated_p35c_gse
      summaries <- study_summaries_p35a
      sample_facts_by_gse <- split(
        sample_facts_p35a_validated,
        vapply(sample_facts_p35a_validated,
               function(s) s$series_id %||% NA_character_, character(1))
      )

      out <- list()
      for (sid in sids) {
        sf <- sample_facts_by_gse[[sid]] %||% list()
        if (length(sf) == 0L) next
        summary <- summaries[[sid]] %||%
          list(title = "", summary = "", overall_design = "")
        out[[sid]] <- multi_classify_study(
          series_id         = sid,
          sample_facts_list = sf,
          study_summary     = summary,
          model_specs       = model_specs_p35c,
          cache             = llm_cache
        )
      }
      out
    },
    cue = targets::tar_cue(mode = "thorough")
  )
```

- [ ] **Step 3: Verifica che il manifest dei target sia parsabile**

```
cd analysis && Rscript --vanilla -e 'targets::tar_manifest(fields = c("name", "command"))[c("name")]' | tail -30
```

Expected: vedrai i nuovi target (`curated_p35c_gse`, `model_specs_p35c`, `multi_classify_outputs_p35c`) elencati.

- [ ] **Step 4: Esegui solo i target a basso costo (no LLM call)**

```
cd analysis && Rscript --vanilla -e 'targets::tar_make(c(curated_p35c_gse, model_specs_p35c))'
```

Expected: entrambi i target completati. Vedrai nel terminale: `▶ end pipeline` con 2 target costruiti.

Verifica risultati:

```
cd analysis && Rscript --vanilla -e 'cat("GSE selezionati:", length(targets::tar_read(curated_p35c_gse)), "\n"); cat("Modelli:", length(targets::tar_read(model_specs_p35c)), "\n")'
```

Expected: "GSE selezionati: 50", "Modelli: 5".

- [ ] **Step 5: Commit (senza il target costoso `multi_classify_outputs_p35c`)**

```
git add analysis/_targets.R
git commit -m "P3.5-C Task 7: target curated_p35c_gse + model_specs + multi_classify (no RUN)"
```

---

## Task 8: Estensione targets — confidence + minigold pool

**Files:**
- Modify: `analysis/_targets.R`

Aggiungi target a valle:
- `confidence_scores_p35c`: tibble con (series_id, confidence_score, tier).
- `minigold_pool_p35c`: 100 sample stratificati.
- `study_summaries_p35c`: riuso di `study_summaries_p35a`.
- `samples_table_p35c`: tibble di tutti i sample dei 50 GSE selezionati.

- [ ] **Step 1: Aggiungi i target dopo `multi_classify_outputs_p35c`**

Inserisci nel file (sempre prima dell'ultima `)`, sotto i target P3.5-C esistenti):

```r
  ,

  tar_target(
    confidence_scores_p35c,
    {
      out_rows <- list()
      for (sid in names(multi_classify_outputs_p35c)) {
        designs <- multi_classify_outputs_p35c[[sid]]
        pa <- compute_pairwise_agreement(designs)
        score <- aggregate_confidence_score(pa)
        tier  <- assign_difficulty_tier(score)
        out_rows[[sid]] <- tibble::tibble(
          series_id = sid,
          n_pairs = nrow(pa),
          confidence_score = score,
          tier = tier
        )
      }
      dplyr::bind_rows(out_rows)
    }
  ),

  tar_target(
    samples_table_p35c,
    {
      curated <- curated_p35c_gse
      full_xlsx <- read_samples_input(samples_input_path)
      full_xlsx |>
        dplyr::filter(.data$series_id %in% curated) |>
        dplyr::transmute(
          geo_accession = .data$geo_accession,
          series_id = .data$series_id,
          string = .data$string
        )
    }
  ),

  tar_target(
    minigold_pool_p35c,
    {
      set.seed(1812)
      sample_minigold_stratified(
        gse_tiers = confidence_scores_p35c,
        samples_table = samples_table_p35c,
        target_n = 100L,
        min_gse_per_tier = 15L
      )
    }
  ),

  tar_target(
    minigold_template_csv_p35c,
    {
      dest <- here::here("analysis", "eval", "p35c-minigold-template.csv")
      summaries <- study_summaries_p35a
      export_minigold_csv(
        minigold_pool          = minigold_pool_p35c,
        study_summaries        = summaries,
        multi_classify_outputs = multi_classify_outputs_p35c,
        dest_path              = dest
      )
      dest
    },
    format = "file"
  )
```

- [ ] **Step 2: Verifica manifest**

```
cd analysis && Rscript --vanilla -e 'targets::tar_manifest(fields = "name") |> tail(10)'
```

Expected: i 4 nuovi target compaiono.

- [ ] **Step 3: Verifica che il file _targets.R sia ancora syntax-valid**

```
Rscript --vanilla -e 'source("analysis/_targets.R")' 2>&1 | head -20
```

Expected: nessun errore di parse (output potrebbe essere vuoto se non ci sono side effects).

- [ ] **Step 4: Commit (senza esecuzione LLM)**

```
git add analysis/_targets.R
git commit -m "P3.5-C Task 8: target confidence_scores + minigold_pool + template_csv (no RUN)"
```

---

## Task 9: Smoke test 1 GSE × 5 modelli (validazione end-to-end con costo minimo)

**Files:** nessuna modifica al codice. Esecuzione manuale per validare l'integrazione end-to-end con tutti e 5 i modelli prima di lanciare il run completo.

Stima costo: ~$0.10 (1 GSE × 5 modelli × 1 call Stage 2 ciascuno).

- [ ] **Step 1: Identifica un GSE piccolo da P3.5-A per smoke test**

```
cd analysis && Rscript --vanilla -e '
v <- targets::tar_read(study_designs_p35a_validated)
sids <- vapply(v, function(d) d$series_id, character(1))
nsmp <- vapply(v, function(d) d$extraction$input_sample_count %||% NA_integer_, integer(1))
df <- tibble::tibble(series_id = sids, n_samples = nsmp) |> dplyr::arrange(n_samples)
head(df, 5)
'
```

Expected: tabella con i 5 GSE più piccoli. Scegli uno con 4-8 sample (es. il primo della tabella).

- [ ] **Step 2: Lancia smoke test inline (senza tar_make completo)**

Sostituisci `<GSE_SMOKE>` con il GSE scelto al punto 1:

```
cd analysis && OPENAI_API_KEY="$OPENAI_API_KEY" ANTHROPIC_API_KEY="$ANTHROPIC_API_KEY" Rscript --vanilla -e '
library(simulomicsr)
library(targets)

sid <- "<GSE_SMOKE>"
sf_all <- tar_read(sample_facts_p35a_validated)
sf <- Filter(function(s) (s$series_id %||% NA_character_) == sid, sf_all)
summary <- tar_read(study_summaries_p35a)[[sid]]
specs   <- tar_read(model_specs_p35c)
cache   <- tar_read(llm_cache)

cat("smoke test su", sid, "con", length(sf), "sample\n")
out <- multi_classify_study(
  series_id         = sid,
  sample_facts_list = sf,
  study_summary     = summary,
  model_specs       = specs,
  cache             = cache
)

for (label in names(out)) {
  d <- out[[label]]
  if (!is.null(d$.invalid_reason)) {
    cat(sprintf("[%s] INVALID: %s\n", label, d$.invalid_reason))
  } else {
    cat(sprintf("[%s] OK design_kind=%s n_groups=%d\n",
                label, d$design_kind %||% "NA",
                length(d$replicate_groups %||% list())))
  }
}
'
```

Expected:
- 5 righe di output, una per modello
- Almeno gpt-5.5 deve dare `OK` (cache hit P3.5-A)
- Gli altri 4 devono dare `OK` o `INVALID schema_validation_failed` (se un modello cheap fallisce, è un dato in sé — registra ma non fallire la fase)

Se 3+ modelli falliscono → STOP, investigare adapter o prompt. Se 0-2 falliscono → procedi.

- [ ] **Step 3: Salva un report dello smoke test in scratch (non committed)**

Annota in un file locale (NON committato, riferimento mentale):
- Quanti modelli hanno fallito
- Quali design_kind sono uscite
- Costo speso (controllabile dal dashboard OpenAI/Anthropic)

- [ ] **Step 4: Commit di un marker (no codice modificato — questo task è solo verifica)**

Skip il commit: questo task è una verifica manuale, non produce file. Procedi a Task 10.

---

## Task 10: Run completo multi_classify_outputs_p35c (50 GSE × 5 modelli)

**Files:** nessuna modifica codice. Esecuzione costosa.

Stima: ~$25-35, 60-90 minuti compute. **HARD LIMIT $50** — abort se la spesa supera.

- [ ] **Step 1: Verifica saldo OpenAI e Anthropic prima di lanciare**

Manuale: utente apre dashboard OpenAI e Anthropic e conferma saldo > $50 in entrambi.

- [ ] **Step 2: Lancia il target multi_classify_outputs_p35c in background**

```
cd analysis && OPENAI_API_KEY="$OPENAI_API_KEY" ANTHROPIC_API_KEY="$ANTHROPIC_API_KEY" Rscript --vanilla -e 'targets::tar_make(multi_classify_outputs_p35c, callr_function = NULL)' 2>&1 | tee /tmp/p35c-run.log
```

Expected: `▶ end pipeline` dopo 60-90 minuti. Se interrompi a metà, `tar_make` riprende dalla cache LLM esistente (cache hit per chiamate già fatte).

- [ ] **Step 3: Verifica dimensione output**

```
cd analysis && Rscript --vanilla -e '
out <- targets::tar_read(multi_classify_outputs_p35c)
cat("GSE classificati:", length(out), "\n")
cat("Primo GSE label:", names(out)[1], "\n")
cat("Modelli per GSE:", length(out[[1]]), "\n")
invalid_count <- 0L
total_classifications <- 0L
for (sid in names(out)) {
  for (label in names(out[[sid]])) {
    total_classifications <- total_classifications + 1L
    if (!is.null(out[[sid]][[label]]$.invalid_reason)) invalid_count <- invalid_count + 1L
  }
}
cat("Classificazioni totali:", total_classifications, "\n")
cat("Invalid:", invalid_count, sprintf("(%.1f%%)", 100 * invalid_count / total_classifications), "\n")
'
```

Expected: 50 GSE × 5 modelli = 250 classificazioni totali. Invalid rate <10% atteso (limite del rischio table 11).

Se invalid rate >10% per uno specifico modello → flag in report Task 13 ma procedi.

- [ ] **Step 4: Lancia anche i target dipendenti (confidence + minigold pool + template csv)**

```
cd analysis && OPENAI_API_KEY="$OPENAI_API_KEY" ANTHROPIC_API_KEY="$ANTHROPIC_API_KEY" Rscript --vanilla -e 'targets::tar_make(c(confidence_scores_p35c, minigold_pool_p35c, minigold_template_csv_p35c), callr_function = NULL)'
```

Expected: 3 target costruiti rapidamente (no LLM call).

Verifica:

```
cd analysis && Rscript --vanilla -e '
cs <- targets::tar_read(confidence_scores_p35c)
cat("Confidence score distribution:\n")
print(summary(cs$confidence_score))
cat("\nTier:\n")
print(table(cs$tier, useNA = "ifany"))
mg <- targets::tar_read(minigold_pool_p35c)
cat("\nMinigold pool:", nrow(mg), "sample\n")
cat("Per tier:\n")
print(table(mg$tier))
csv_path <- targets::tar_read(minigold_template_csv_p35c)
cat("\nCSV template:", csv_path, "\n")
cat("Size:", file.info(csv_path)$size, "bytes\n")
'
```

Expected:
- 50 GSE con confidence_score numerico
- Distribuzione tier ragionevole (non tutti `easy` o tutti `hard`)
- 100 sample nel minigold pool (50 easy + 50 hard)
- File CSV creato a `analysis/eval/p35c-minigold-template.csv`

- [ ] **Step 5: Restore meta dei target esistenti (lesson learned P3.5-A)**

Dopo tar_make, i file `analysis/_targets/.gitignore` e `analysis/_targets/meta/meta` sono modificati. Non committarli (sono auto-generati). Verifica:

```
git status analysis/_targets/
```

Se ci sono modifiche, lasciali untracked/ignored. **NON fare `git checkout`** mentre la pipeline è ancora in esecuzione.

- [ ] **Step 6: Commit del CSV template generato (con gli output dei modelli inclusi)**

Il CSV è committato perché contiene il pre-popolamento dei modelli (proposed_models columns) ed è il file che l'utente review-erà.

```
cp analysis/eval/p35c-minigold-template.csv inst/extdata/p35c-minigold-template-prepopulated.csv
git add inst/extdata/p35c-minigold-template-prepopulated.csv
git commit -m "P3.5-C Task 10: run multi-modello completato + minigold template prepopulated"
```

(Il file `inst/extdata/p35c-minigold-template-empty.csv` resta come riferimento dello schema; il `-prepopulated.csv` è l'artefatto del run.)

---

## Task 11: Review umana mini-gold (manuale, utente)

**Files:** modifica manuale di `analysis/eval/p35c-minigold-template.csv`.

Tempo stimato: 3-5 ore concentrate.

- [ ] **Step 1: Apri il CSV in editor**

L'utente apre `analysis/eval/p35c-minigold-template.csv` in Excel, Numbers, o un editor CSV. 100 righe.

- [ ] **Step 2: Per ogni riga, compila `design_role_gold` e `design_kind_gold`**

Lettura riga: la riga ha già pre-popolato `string`, `study_title`, `study_summary`, e le proposte dei 5 modelli (`design_role_proposed_models` colonna concatenata `gpt_5_5=perturbed; gpt_5_4_mini=...; ...`).

Per ogni sample, l'utente:
- legge `string` + `study_title` + `study_summary`
- guarda le proposte dei 5 modelli
- decide il `design_role_gold` (uno dei 13 valori v3) e `design_kind_gold` (uno dei 10 valori)
- compila la colonna corrispondente
- aggiunge un `comment_optional` se utile

Vocabolari di riferimento (da memorizzare o tenere a portata):
- `design_role`: perturbed, vehicle_control, untreated_control, negative_genetic_control, negative_inducer_control, positive_control, baseline_t0, case, comparison, bystander, secondary_arm, excluded, unclear
- `design_kind`: case_control_disease, treatment_vs_vehicle, treatment_vs_untreated, time_course, dose_response, knockdown_panel, factorial, differentiation_course, multi_arm_treatment, unclear

- [ ] **Step 3: Salva il CSV reviewato**

L'utente salva. Importante: mantenere il formato CSV (no `xlsx`).

- [ ] **Step 4: Copia il CSV reviewato come artefatto del progetto**

```
cp analysis/eval/p35c-minigold-template.csv inst/extdata/p35c-minigold-reviewed.csv
```

- [ ] **Step 5: Test dell'import (validazione vocabolari)**

```
OPENAI_API_KEY="" Rscript --vanilla -e '
library(simulomicsr)
out <- import_minigold_reviewed("inst/extdata/p35c-minigold-reviewed.csv")
cat("Sample reviewati:", nrow(out), "\n")
cat("Distribuzione design_role_gold:\n")
print(table(out$design_role_gold))
cat("\nDistribuzione design_kind_gold:\n")
print(table(out$design_kind_gold))
cat("\nDistribuzione tier:\n")
print(table(out$tier))
'
```

Expected: nessun errore di vocabolario. Sample reviewati ≥30 (soglia minima fase, vedi spec sec.7.3). Idealmente 100.

Se l'errore `simulomicsr_minigold_invalid_value` compare → l'utente ha digitato un valore fuori vocabolario. Fix nel CSV e ripeti.

- [ ] **Step 6: Commit del mini-gold reviewato**

```
git add inst/extdata/p35c-minigold-reviewed.csv
git commit -m "P3.5-C Task 11: mini-gold reviewato N=<NN> sample (utente)"
```

(Sostituisci `<NN>` con il numero effettivo riportato dal Step 5.)

---

## Task 12: Target eval contro mini-gold

**Files:**
- Modify: `analysis/_targets.R`

Aggiungi target che importano il mini-gold reviewato e calcolano l'accuracy per modello.

- [ ] **Step 1: Aggiungi target a fine sezione P3.5-C**

Inserisci nel file (sempre prima dell'ultima `)` finale):

```r
  ,

  tar_target(
    minigold_reviewed_csv_path_p35c,
    here::here("inst", "extdata", "p35c-minigold-reviewed.csv"),
    format = "file"
  ),

  tar_target(
    minigold_reviewed_p35c,
    import_minigold_reviewed(minigold_reviewed_csv_path_p35c)
  ),

  tar_target(
    eval_p35c_metrics,
    {
      acc <- eval_against_minigold(
        reviewed = minigold_reviewed_p35c,
        multi_classify_outputs = multi_classify_outputs_p35c
      )

      cm_per_model <- lapply(unique(acc$model), function(label) {
        per_sample <- list()
        for (i in seq_len(nrow(minigold_reviewed_p35c))) {
          row <- minigold_reviewed_p35c[i, ]
          d <- multi_classify_outputs_p35c[[row$series_id]][[label]]
          if (is.null(d) || isTRUE(!is.null(d$.invalid_reason))) next
          # Accuracy by design_kind_gold
          per_sample[[length(per_sample) + 1L]] <- tibble::tibble(
            kind_gold = row$design_kind_gold,
            kind_pred = d$design_kind %||% NA_character_,
            tier = row$tier,
            correct_kind = identical(d$design_kind, row$design_kind_gold)
          )
        }
        df <- dplyr::bind_rows(per_sample)
        df$model <- label
        df
      })

      list(
        accuracy_table = acc,
        confusion = dplyr::bind_rows(cm_per_model),
        n_reviewed = nrow(minigold_reviewed_p35c)
      )
    }
  )
```

- [ ] **Step 2: Esegui i target eval**

```
cd analysis && Rscript --vanilla -e 'targets::tar_make(c(minigold_reviewed_p35c, eval_p35c_metrics), callr_function = NULL)'
```

Expected: 2 target costruiti rapidamente.

- [ ] **Step 3: Visualizza i risultati**

```
cd analysis && Rscript --vanilla -e '
m <- targets::tar_read(eval_p35c_metrics)
cat("N sample reviewati:", m$n_reviewed, "\n\n")
cat("Accuracy table per modello x tier:\n")
print(m$accuracy_table)
'
```

Expected: tabella con 5 modelli × 3 tier (overall/easy/hard). Output qualcosa tipo:

```
 model              tier      n   n_correct  accuracy
 gpt_5_5            overall  100  85         0.85
 gpt_5_5            easy      50  47         0.94
 gpt_5_5            hard      50  38         0.76
 gpt_5_4_mini       overall  100  78         0.78
 ...
```

- [ ] **Step 4: Commit**

```
git add analysis/_targets.R
git commit -m "P3.5-C Task 12: target eval_p35c_metrics contro mini-gold reviewato"
```

---

## Task 13: Quarto report `p35c-confidence.Rmd`

**Files:**
- Create: `analysis/eval/p35c-confidence.Rmd`
- Modify: `analysis/_targets.R` (aggiungi `tar_render`)

Report 4 sezioni come da spec sec.8.

- [ ] **Step 1: Crea `analysis/eval/p35c-confidence.Rmd`**

Crea il file con questo contenuto (è lungo ma intero):

```rmd
---
title: "P3.5-C Confidence-aware classification — multi-model + mini-gold"
output:
  html_document:
    toc: true
    toc_depth: 2
    df_print: paged
params:
  eval_metrics: NULL
  confidence_scores: NULL
  multi_outputs: NULL
  minigold_reviewed: NULL
  curated_gse: NULL
  model_specs: NULL
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, message = FALSE, warning = FALSE)
library(dplyr)
library(tibble)
library(ggplot2)
library(knitr)

m  <- params$eval_metrics
cs <- params$confidence_scores
mc <- params$multi_outputs
mg <- params$minigold_reviewed
specs <- params$model_specs
```

# Sommario esecutivo

P3.5-C valida 5 modelli LLM (`r length(specs)`) su `r length(params$curated_gse)` GSE selezionati da P3.5-A, con un mini-gold design-aware di `r m$n_reviewed` sample reviewati a mano.

**Domande risposte in questo report:**
1. Quale modello e' P4-ready? (sec.1)
2. La cross-model agreement predice la difficolta'? (sec.2)
3. Quali sono gli errori sistematici per modello? (sec.3)
4. Decisione P4 + raccomandazione tier-aware filtering. (sec.4)

# 1. Cost-quality validation

```{r sec1-table}
acc_overall <- m$accuracy_table |>
  dplyr::filter(tier == "overall") |>
  dplyr::arrange(dplyr::desc(accuracy))

# Costi sub-set noti (Task 10 actual). Aggiornare se misurati diversi.
costs_subset <- tibble::tribble(
  ~model,                ~cost_usd,
  "gpt_5_5",              0.0,    # cache hit
  "gpt_5_4_mini",         3.0,    # placeholder, da aggiornare con valore reale
  "gpt_5_4_nano",         1.0,
  "claude_haiku_4_5",     5.0,
  "claude_sonnet_4_6",   17.0
)

acc_overall |>
  dplyr::left_join(costs_subset, by = "model") |>
  dplyr::mutate(
    cost_per_p4_estimate_usd = cost_usd / 50 * 700000  # 50 GSE -> 700k sample (P4)
  ) |>
  knitr::kable(digits = 3, caption = "Accuracy globale + costo sub-set + estrapolazione P4")
```

```{r sec1-wilson}
# Wilson 95% CI per ogni modello
acc_with_ci <- acc_overall |>
  dplyr::rowwise() |>
  dplyr::mutate(
    ci_low  = simulomicsr::wilson_ci(n_correct, n)$lower,
    ci_high = simulomicsr::wilson_ci(n_correct, n)$upper
  ) |>
  dplyr::ungroup()

ggplot(acc_with_ci, aes(x = reorder(model, accuracy), y = accuracy)) +
  geom_point() +
  geom_errorbar(aes(ymin = ci_low, ymax = ci_high), width = 0.2) +
  geom_hline(yintercept = max(acc_with_ci$accuracy), linetype = "dashed", alpha = 0.5) +
  labs(x = NULL, y = "Accuracy (Wilson 95% CI)",
       title = "Accuracy per modello sul mini-gold") +
  coord_flip() +
  theme_minimal()
```

```{r sec1-mcnemar}
# McNemar paired test: ogni modello vs gpt-5.5
baseline_label <- "gpt_5_5"
labels <- setdiff(unique(m$accuracy_table$model), baseline_label)

mcnemar_rows <- lapply(labels, function(other) {
  per_sample_a <- m$accuracy_table[FALSE, ]  # placeholder, ricalcolo da minigold
  # Ricostruisco i correct flag per (sample, model) dal eval_against_minigold
  # gold_per_sample indicizzato per geo_accession + model
  rows <- list()
  for (i in seq_len(nrow(mg))) {
    row <- mg[i, ]
    d_a <- mc[[row$series_id]][[baseline_label]]
    d_b <- mc[[row$series_id]][[other]]
    pred_a <- simulomicsr:::.lookup_role_for_sample(d_a, row$geo_accession)
    pred_b <- simulomicsr:::.lookup_role_for_sample(d_b, row$geo_accession)
    rows[[i]] <- tibble::tibble(
      a_correct = identical(pred_a, row$design_role_gold),
      b_correct = identical(pred_b, row$design_role_gold)
    )
  }
  df <- dplyr::bind_rows(rows)
  res <- simulomicsr::mcnemar_paired(df$a_correct, df$b_correct)
  tibble::tibble(
    other_model = other,
    n           = nrow(df),
    a_only_correct = sum(df$a_correct & !df$b_correct),
    b_only_correct = sum(!df$a_correct & df$b_correct),
    pvalue       = res$p_value,
    significant_05 = res$p_value < 0.05
  )
})
mcnemar_tbl <- dplyr::bind_rows(mcnemar_rows)

knitr::kable(mcnemar_tbl, digits = 4,
             caption = "McNemar paired test: ogni modello vs gpt-5.5")
```

**Bottom line — modello raccomandato per P4:** [da scrivere a mano dopo aver visto i risultati: il modello con accuracy non significativamente diversa da gpt-5.5 e costo P4 minimo].

# 2. Confidence calibration

```{r sec2-distribution}
ggplot(cs, aes(x = confidence_score, fill = tier)) +
  geom_histogram(binwidth = 0.05, alpha = 0.7) +
  labs(x = "Confidence score (cross-model agreement)",
       y = "N GSE",
       title = "Distribuzione confidence_score sul sub-set") +
  theme_minimal()
```

```{r sec2-tier-table}
cs |>
  dplyr::group_by(tier) |>
  dplyr::summarise(n = dplyr::n(), .groups = "drop") |>
  knitr::kable(caption = "Numero GSE per tier")
```

```{r sec2-calibration}
# Calibration plot: per ogni modello, accuracy vs tier
acc_by_tier <- m$accuracy_table |>
  dplyr::filter(tier %in% c("easy", "hard"))

ggplot(acc_by_tier, aes(x = tier, y = accuracy, color = model, group = model)) +
  geom_point(size = 3) +
  geom_line() +
  ylim(0, 1) +
  labs(x = "Tier (cross-model agreement)",
       y = "Accuracy vs mini-gold",
       title = "Calibrazione: accuracy ~ confidence tier",
       color = "Modello") +
  theme_minimal()
```

Lettura attesa: linee monotone discendenti (easy > hard). Se piatte → il difficulty score non discrimina.

# 3. Pattern di errore sistematici

```{r sec3-confusion}
m$confusion |>
  dplyr::group_by(model, kind_gold, kind_pred) |>
  dplyr::summarise(n = dplyr::n(), .groups = "drop") |>
  dplyr::filter(kind_gold != kind_pred) |>
  dplyr::arrange(model, dplyr::desc(n)) |>
  dplyr::group_by(model) |>
  dplyr::slice_head(n = 5) |>
  knitr::kable(caption = "Top-5 errori design_kind per modello (gold vs pred)")
```

```{r sec3-disagreement}
# Sample dove i modelli sono massimamente in disaccordo: quante volte il
# mini-gold coincide con la maggioranza?
disagreement_rows <- list()
for (i in seq_len(nrow(mg))) {
  row <- mg[i, ]
  preds <- vapply(names(mc[[row$series_id]]), function(label) {
    d <- mc[[row$series_id]][[label]]
    simulomicsr:::.lookup_role_for_sample(d, row$geo_accession) %||% NA_character_
  }, character(1))
  unique_preds <- unique(preds[!is.na(preds)])
  if (length(unique_preds) >= 3L) {
    majority <- names(sort(table(preds), decreasing = TRUE))[1]
    disagreement_rows[[length(disagreement_rows) + 1L]] <- tibble::tibble(
      geo_accession = row$geo_accession,
      gold = row$design_role_gold,
      majority_pred = majority,
      gold_eq_majority = identical(majority, row$design_role_gold),
      n_distinct_preds = length(unique_preds)
    )
  }
}
disagreement_tbl <- dplyr::bind_rows(disagreement_rows)

if (nrow(disagreement_tbl) > 0L) {
  cat(sprintf("\nSample con disaccordo forte (%d+ predizioni distinte): %d totali\n",
              3L, nrow(disagreement_tbl)))
  cat(sprintf("Gold = majority: %d (%.1f%%)\n",
              sum(disagreement_tbl$gold_eq_majority),
              100 * mean(disagreement_tbl$gold_eq_majority)))
  knitr::kable(head(disagreement_tbl, 10),
               caption = "Esempi (primi 10) di disaccordo forte")
}
```

# 4. Decisione P4-ready + limitations

**Modello scelto per P4:** [scrivere dopo aver visto sec.1].

**Soglia di confidence raccomandata per P5 meta-analisi:** [scrivere dopo aver visto sec.2 calibration plot].

**Budget aggiornato P4:** [estrapolazione costo sub-set × scaling factor].

## Limitations

- **Stage 1 fissato** a gpt-5.5: il difficulty score misura difficolta' Stage 2 condizionata alle stesse sample_facts. Stage 1 multi-modello e' fuori scope P3.5-C.
- **Mini-gold ridotto** (`r m$n_reviewed` sample, target ideale 100): statistica meno potente su slice rare.
- **Soglie tier preliminari** (0.85 / 0.6): da rivedere in caso di calibrazione piatta.
- **Cache hit di gpt-5.5** dal P3.5-A: nessuna re-classificazione del baseline (assunzione: il modello si comporta uguale a 1 settimana di distanza).

## Next step

P4 server-switch con modello scelto, tier-aware filtering integrato in P5 meta-analisi pipeline.
```

- [ ] **Step 2: Aggiungi `tar_render` per il report in `_targets.R`**

Inserisci, sempre prima dell'ultima `)`:

```r
  ,

  tarchetypes::tar_render(
    eval_p35c_report,
    here::here("analysis", "eval", "p35c-confidence.Rmd"),
    output_dir = here::here("analysis", "eval"),
    params = list(
      eval_metrics      = eval_p35c_metrics,
      confidence_scores = confidence_scores_p35c,
      multi_outputs     = multi_classify_outputs_p35c,
      minigold_reviewed = minigold_reviewed_p35c,
      curated_gse       = curated_p35c_gse,
      model_specs       = model_specs_p35c
    )
  )
```

- [ ] **Step 3: Lancia il render**

```
cd analysis && Rscript --vanilla -e 'targets::tar_make(eval_p35c_report, callr_function = NULL)'
```

Expected: target completato. File `analysis/eval/p35c-confidence.html` creato.

- [ ] **Step 4: Apri il report e leggi**

```
open analysis/eval/p35c-confidence.html
```

L'utente legge il report e:
- decide il modello P4-ready (riga finale sec.1 e sec.4)
- decide la soglia di confidence (sec.4)
- aggiorna i placeholder `[scrivere dopo aver visto...]` nel `Rmd` con i valori reali
- ri-renderizza una volta finito

- [ ] **Step 5: Aggiorna i placeholder e ri-render**

L'utente edita `analysis/eval/p35c-confidence.Rmd` per scrivere le tre frasi finali (modello P4, soglia confidence, budget P4 aggiornato).

```
cd analysis && Rscript --vanilla -e 'targets::tar_make(eval_p35c_report, callr_function = NULL)'
```

- [ ] **Step 6: Commit**

```
git add analysis/_targets.R analysis/eval/p35c-confidence.Rmd analysis/eval/p35c-confidence.html
git commit -m "P3.5-C Task 13: report Quarto p35c-confidence (4 sezioni) RUN completata"
```

---

## Task 14: Bump versione + NEWS + R CMD check finale

**Files:**
- Modify: `DESCRIPTION`
- Modify: `NEWS.md`
- Modify: `CLAUDE.md`

- [ ] **Step 1: Bump versione in `DESCRIPTION`**

Apri `DESCRIPTION`, trova la riga `Version:` e incrementa l'ultimo nibble:

Da:
```
Version: 0.0.0.9006
```

A:
```
Version: 0.0.0.9007
```

- [ ] **Step 2: Aggiungi entry in `NEWS.md`**

Inserisci sopra l'entry più recente, formato:

```markdown
# simulomicsr 0.0.0.9007 (P3.5-C — confidence-aware classification)

* Adapter Anthropic Messages API (`R/llm-client-anthropic.R`) con structured
  output via tool_use forzato. Supporta claude-haiku-4-5 e claude-sonnet-4-6.
* Dispatch `provider="anthropic"` in `llm_call_structured()`.
* `multi_classify_study()` per classificazione cross-modello su un singolo GSE.
* Confidence score via cross-model agreement: `compute_pairwise_agreement()`,
  `aggregate_confidence_score()`, `assign_difficulty_tier()` (R/p35c-confidence.R).
* Mini-gold design-aware: `sample_minigold_stratified()`, `export_minigold_csv()`,
  `import_minigold_reviewed()`, `eval_against_minigold()` (R/p35c-minigold.R).
* Pipeline `analysis/_targets.R`: target `_p35c` (50 GSE × 5 modelli, mini-gold
  100 sample stratificato per difficolta').
* Report Quarto `analysis/eval/p35c-confidence.html` 4 sezioni paper-ready.
* Tag: `p3.5c-confidence-complete`.

## Risultati P3.5-C

[Da compilare a fine fase con accuracy table, modello scelto P4, costo
totale, soglia di confidence raccomandata.]
```

- [ ] **Step 3: Aggiorna `CLAUDE.md` (sezione "Stato corrente")**

Apri `CLAUDE.md`, trova la sezione "Stato corrente" e aggiungi un blocco:

```markdown
### Cosa P3.5-C ha consegnato (confidence-aware classification)

- Adapter Anthropic Messages API + dispatch in llm_call_structured.
- Multi-model classifier (5 modelli: gpt-5.5, gpt-5.4-mini, gpt-5.4-nano,
  claude-haiku-4-5, claude-sonnet-4-6).
- Confidence score via cross-model agreement (3 funzioni in
  R/p35c-confidence.R).
- Mini-gold design-aware reviewato (N=<NN> sample, 50/50 easy/hard).
- Eval pipeline contro mini-gold + report Quarto 4 sezioni.

### Risultati P3.5-C

| Metrica | Valore |
|---|---|
| Modello P4-ready scelto | <da compilare> |
| Accuracy modello scelto vs mini-gold | <da compilare> |
| Costo P3.5-C run | <da compilare> $ |
| Estrapolazione P4 con modello scelto | <da compilare> $ |
| Soglia confidence raccomandata P5 | <da compilare> |
```

- [ ] **Step 4: R CMD check finale**

```
Rscript --vanilla -e 'devtools::document()'
Rscript --vanilla -e 'devtools::check(error_on = "warning", document = FALSE)'
```

Expected: 0 errors, 0 warnings, note pre-esistenti OK (NEWS title, package doc title).

- [ ] **Step 5: Suite completa**

```
OPENAI_API_KEY="" Rscript --vanilla -e 'devtools::test()'
```

Expected: tutti i test pass (370+ PASS).

- [ ] **Step 6: Commit + tag**

```
git add DESCRIPTION NEWS.md CLAUDE.md
git commit -m "P3.5-C Task 14: bump 0.0.0.9007 + NEWS + CLAUDE.md aggiornati"

git tag p3.5c-confidence-complete
```

NON fare `git push` (l'utente fa il push lui).

---

## Self-review

**Spec coverage check:**

- Spec sec.1 obj 1 (cost-quality validation) → Task 13 sec.1 (tabella cost-accuracy + Wilson + McNemar). ✓
- Spec sec.1 obj 2 (confidence/difficulty score) → Task 4 (3 funzioni) + Task 13 sec.2 (calibration plot). ✓
- Spec sec.1 obj 3 (mini-gold design-aware) → Task 5+6 (4 funzioni) + Task 11 (review) + Task 12 (eval). ✓
- Spec sec.3 architettura → Task 1 (Anthropic adapter), Task 2 (dispatch), Task 3 (multi-classify), Task 7-8 (target). ✓
- Spec sec.4 file → tutti coperti (Task 1, 3, 4, 6, 7, 13). ✓
- Spec sec.5.1 50 GSE → Task 7 (`curated_p35c_gse`). ✓
- Spec sec.5.2 5 modelli → Task 7 (`model_specs_p35c`). ✓
- Spec sec.5.3 Stage 1 fissato → Task 7 (`sample_facts_by_gse` da P3.5-A). ✓
- Spec sec.6.1 confidence definition → Task 4 + soglie 0.85/0.6 in `assign_difficulty_tier`. ✓
- Spec sec.6.2 calibrazione contro mini-gold → Task 13 sec.2. ✓
- Spec sec.7.1 mini-gold 100 sample 50/50 → Task 8 + Task 5. ✓
- Spec sec.7.2 workflow CSV review → Task 6 (export/import) + Task 11 (review umana). ✓
- Spec sec.7.3 fallback 30 sample minimo → Task 11 step 5 (controllo n>=30). ✓
- Spec sec.8 report 4 sezioni → Task 13. ✓
- Spec sec.9 test strategy → unit test in Task 1, 3, 4, 5, 6 + smoke in Task 9. ✓
- Spec sec.10 UAT criteria → coperti distribuiti (1: Task 13 sec.1; 2: Task 13 sec.2; 3: Task 13 sec.2; 4: Task 11; 5: Task 13 sec.4; 6: Task 13 sec.2/3; 7: Task 13 sec.4; 8: Task 14). ✓
- Spec sec.11 rischi → Task 9 (smoke test) cattura JSON invalido frequente; Task 10 hard limit $50; Task 11 fallback minimo 30. ✓
- Spec sec.12 out-of-scope → Task plan resta nei 3 obiettivi (no prompt iteration, no open-source, no ellmer migration). ✓

**Placeholder scan:** Nessun "TBD"/"TODO" nel plan. Le frasi "[scrivere dopo aver visto...]" nel Task 13 step 5 sono **azioni utente esplicite a fase quasi completata**, non placeholder del plan.

**Type consistency:**
- `compute_pairwise_agreement` ritorna tibble in tutti i test e nel codice ✓
- `aggregate_confidence_score` accetta quel tibble ✓
- `multi_classify_outputs_p35c[[sid]]` indicizzato per `series_id` poi per `label` modello ✓ (verificato in Task 7-8-12)
- `model_specs` ha campi `provider`/`model`/`label` consistente in Task 3 e Task 7 ✓
- CSV columns `.MINIGOLD_CSV_COLS` consistente tra `export_minigold_csv` e `import_minigold_reviewed` ✓

Self-review passed.

---

## Numero totale task: 14

Task pesi:
- Task 1-6, 12, 14: code-only, ~30-60 min ciascuno (TDD).
- Task 7-8: target pipeline integration, ~30 min ciascuno.
- Task 9: smoke test manuale, ~10 min.
- Task 10: run completo LLM, 60-90 min compute (autonomo dopo lancio).
- Task 11: review umana, 3-5 ore (utente).
- Task 13: report Quarto + render, ~1 ora.

Tempo totale calendario: 2-3 giornate (vedi spec sec.13).
