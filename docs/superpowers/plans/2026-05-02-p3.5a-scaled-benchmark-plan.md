# P3.5-A — Scaled benchmark Stadio 2 (100 GSE paper-ready) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Validare il classificatore Stadio 2 di simulomicsr su un set indipendente di 100 GSE freschi (no leakage rispetto ai 15 P3) selezionati da `RummaGEO official ∩ xlsx gold` con stratificazione hybrid (oversampling categorie rare critiche per il claim del paper). Produrre un Quarto report con 5 sezioni che fungano da evidence base citabile direttamente nel paper futuro: binary accuracy con Wilson CI, granularity disagreement enumeration scalata, anchor coverage cross-studio intra-100, RummaGEO head-to-head con McNemar paired e bootstrap delta CI, Sezione 5 investigation GSE145941 (chiusura della story P3.5-B).

**Architecture:** Estende l'infrastruttura eval costruita in P3.5-B riusando integralmente `R/eval-stage2.R`, `R/eval-rummageo.R` e i target eval pipeline. Aggiunge tre nuovi layer: (a) selezione GSE (`R/p35a-select-gse.R`) — fetch indice RummaGEO ufficiale via GraphQL, intersezione con xlsx gold, keyword proxy per `design_kind`, stratified sampling deterministico; (b) statistica inferenziale paper-grade (`R/eval-stats.R`) — Wilson CI, McNemar paired, bootstrap delta CI, Holm correction; (c) investigation GSE145941 (`R/p35a-investigate-gse145941.R`) — re-classify verbose-reasoning + side-by-side gold/LLM/ARCHS4. Il flusso P3.5-B (15 GSE) resta intatto in `_targets.R` per riproducibilità storica; P3.5-A aggiunge un parallel branch con suffisso `_p35a`.

**Tech Stack:** R 4.5+, P1-P3 stack invariato (`httr2`, `jsonlite`, `dplyr`, `tibble`, `tidyr`, `targets`, `tarchetypes`, `ggplot2`, `scales` — questi ultimi due già aggiunti in P3.5-B Task 1). Wilson CI / McNemar / Holm via base R (`qnorm`, `mcnemar.test`, `p.adjust`). Bootstrap manuale (loop con `sample()`). Zero nuove dipendenze.

---

## File Structure

| File | Stato | Responsabilità | LOC stimato |
|---|---|---|---|
| `R/eval-stats.R` | NUOVO | `wilson_ci()`, `mcnemar_paired()`, `bootstrap_delta_ci()`, `holm_adjust()` | ~150 |
| `R/p35a-select-gse.R` | NUOVO | `load_rummageo_index()` (GraphQL paginated + cache), `keyword_design_kind_proxy()` (regex-based), `intersect_with_xlsx_and_archs4()`, `stratified_sample_gse()` (deterministic seed 1812) | ~280 |
| `R/p35a-investigate-gse145941.R` | NUOVO | `reclassify_verbose()` (prompt extension chiede chain-of-thought), `compare_with_gold()` (tabella side-by-side per Sezione 5) | ~140 |
| `R/eval-stage2.R` | esistente | invariato (riusato) | — |
| `R/eval-rummageo.R` | esistente | invariato (riusato) | — |
| `analysis/_targets.R` (estensione) | modificato | nuovi target P3.5-A: `p35a_gse_csv`, `curated_p35a_gse`, `samples_input_p35a`, `sample_facts_p35a_raw`, `sample_facts_p35a_validated`, `sample_facts_p35a_invalid`, `study_designs_p35a_raw`, `study_designs_p35a_validated`, `study_designs_p35a_invalid`, `gold_table_subset_p35a`, `eval_stage2_p35a_gold_join`, `eval_stage2_p35a_metrics`, `rummageo_p35a_signatures`, `rummageo_p35a_metrics`, `comparisons_table_p35a`, `gse145941_reclassify`, `eval_p35a_report` (`tar_render`) | ~250 (estende ~480→730) |
| `analysis/eval/p35a-benchmark.Rmd` | NUOVO | Quarto report 5 sezioni: binary accuracy con Wilson CI, granularity, anchor coverage intra-100, RummaGEO head-to-head con McNemar+bootstrap+Holm, investigation GSE145941 | ~280 |
| `tests/testthat/test-eval-stats.R` | NUOVO | Test Wilson CI, McNemar, bootstrap su esempi noti (manuali analitici per Wilson; mcnemar.test ground truth per McNemar) | ~140 |
| `tests/testthat/test-p35a-select-gse.R` | NUOVO | Test mock RummaGEO index, keyword proxy su esempi rappresentativi, intersect, stratified sample (seeded determinism) | ~180 |
| `tests/testthat/test-p35a-investigate.R` | NUOVO | Test mocked `reclassify_verbose` + shape `compare_with_gold` | ~80 |
| `tests/testthat/fixtures/rummageo-index-mock.json` | NUOVO | Cassette mock per test offline | ~40 |
| `tests/testthat/fixtures/p35a-xlsx-mock.tsv` | NUOVO | Mini xlsx-like fixture per test intersect | ~50 righe |
| `inst/extdata/p35a-gse-selected.csv` | NUOVO (artefatto, committato) | Lista 100 GSE finali con `design_kind_proxy` (riproducibilità) | n/a |

Totale nuovo codice: ~850 LOC + ~400 LOC test. Tutto offline-buildable eccetto i task di RUN live (Task 5.6, 6.4, 7.4, 8.4, 9.3, 10.3 — esplicitamente segnalati).

Sono **NON** in scope di P3.5-A (vedono spec §11):
- Re-tuning prompt Stage 2 sulla base dei numeri P3.5-A — eventuale P3.5-A2 separato
- Effect-size pooling REM (ADR-0006 Output 3) — P5
- Gold "design-aware" su 200-300 sample — l'utente ha confermato gold binary `trtctr_EP` sufficiente
- Integrazione MetaHQ — deferred
- Run massivo ARCHS4 (700k sample) — P4

---

## Task 1: Branch + baseline check

**Files:**
- Solo verifica stato repo (no modifiche file)

- [ ] **Step 1.1: Verifica master pulito e tag P3.5-B presente**

```bash
git status
git tag --list 'p3.5b-*'
git rev-parse HEAD
```

Expected: working tree clean (esclusi `?? renv/settings.json`), `p3.5b-eval-complete` presente, HEAD su master `872f68b` o successivo.

- [ ] **Step 1.2: Verifica test suite baseline pulita**

```bash
Rscript --vanilla -e 'devtools::test()'
```

Expected: 328 PASS / 0 FAIL.

- [ ] **Step 1.3: Verifica targets P3.5-B funzionanti (cache già popolata)**

```bash
Rscript --vanilla -e 'targets::tar_make(callr_function = NULL, names = "eval_stage2_metrics")'
```

Expected: target up-to-date senza re-run; nessun errore.

- [ ] **Step 1.4: Crea branch `p3.5a-eval`**

```bash
git checkout -b p3.5a-eval
```

Expected: `Switched to a new branch 'p3.5a-eval'`.

- [ ] **Step 1.5: Verifica nessuna nuova dipendenza necessaria**

`ggplot2` + `scales` già in Suggests da P3.5-B Task 1. `httr2`, `jsonlite`, `targets`, `tarchetypes`, `dplyr`, `tibble`, `tidyr`, `readxl`, `rentrez`, `rmarkdown` già installati. Wilson/McNemar/Holm/bootstrap = base R. Nessun `renv::install` necessario.

```bash
grep -E 'Suggests|Imports' DESCRIPTION
```

Expected: `ggplot2`, `scales` presenti in Suggests.

- [ ] **Step 1.6: Nessun commit (solo verifica baseline)**

---

## Task 2: `R/eval-stats.R` — Wilson CI, McNemar paired, bootstrap delta, Holm

**Files:**
- Crea: `R/eval-stats.R`
- Crea: `tests/testthat/test-eval-stats.R`

`R/eval-stats.R` contiene 4 funzioni di statistica inferenziale paper-grade. Tutte pure (no I/O, no LLM), tutte deterministiche per `bootstrap_delta_ci()` con seed esplicito.

- [ ] **Step 2.1: Scrivi i test (failing) `tests/testthat/test-eval-stats.R`**

```r
test_that("wilson_ci ritorna intervalli noti per casi standard", {
  # Wilson CI per p=0.5, n=100, alpha=0.05 → analiticamente [~0.404, ~0.596]
  ci <- wilson_ci(50L, 100L, conf = 0.95)
  expect_equal(ci$estimate, 0.5)
  expect_equal(ci$lower, 0.4038, tolerance = 0.001)
  expect_equal(ci$upper, 0.5962, tolerance = 0.001)
})

test_that("wilson_ci gestisce p=0 e p=1 senza esplodere", {
  ci_zero <- wilson_ci(0L, 100L, conf = 0.95)
  expect_equal(ci_zero$estimate, 0)
  expect_true(ci_zero$lower >= 0)
  expect_true(ci_zero$upper > 0 && ci_zero$upper < 0.05)

  ci_one <- wilson_ci(100L, 100L, conf = 0.95)
  expect_equal(ci_one$estimate, 1)
  expect_true(ci_one$lower > 0.95 && ci_one$lower < 1)
  expect_equal(ci_one$upper, 1)
})

test_that("wilson_ci gestisce n=0", {
  ci <- wilson_ci(0L, 0L, conf = 0.95)
  expect_true(is.na(ci$estimate))
  expect_true(is.na(ci$lower))
  expect_true(is.na(ci$upper))
})

test_that("mcnemar_paired matcha mcnemar.test base R su esempio noto", {
  # Tabella canonica:
  #         B_correct  B_wrong
  # A_corr     30        20  (b)
  # A_wrong    10        40  (c)
  # b=20 c=10 → discrepanze 30. Senza correzione continuita': chi2 = (b-c)^2/(b+c) = 100/30 = 3.333
  pred_a <- c(rep("correct", 30), rep("correct", 20), rep("wrong", 10), rep("wrong", 40))
  pred_b <- c(rep("correct", 30), rep("wrong", 20),   rep("correct", 10), rep("wrong", 40))
  result <- mcnemar_paired(pred_a, pred_b, continuity = FALSE)
  expect_equal(result$statistic, 3.333, tolerance = 0.01)
  expect_equal(result$b, 20L)
  expect_equal(result$c, 10L)
  # Confronto vs base R
  tab <- table(pred_a, pred_b)
  base_result <- mcnemar.test(tab, correct = FALSE)
  expect_equal(result$statistic, as.numeric(base_result$statistic),
               tolerance = 0.01)
  expect_equal(result$p_value, base_result$p.value, tolerance = 0.001)
})

test_that("mcnemar_paired ritorna p_value alto quando b=c (no disagreement direzionale)", {
  pred_a <- c("correct", "wrong", "correct", "wrong")
  pred_b <- c("wrong", "correct", "wrong", "correct")
  # b=2, c=2 → no asimmetria
  result <- mcnemar_paired(pred_a, pred_b, continuity = FALSE)
  expect_equal(result$b, 2L)
  expect_equal(result$c, 2L)
  expect_true(result$p_value > 0.5)
})

test_that("bootstrap_delta_ci e' deterministico con seed", {
  set.seed(NULL)
  pred_a <- c(rep("correct", 80), rep("wrong", 20))
  pred_b <- c(rep("correct", 70), rep("wrong", 30))
  ci_1 <- bootstrap_delta_ci(pred_a, pred_b, n_iter = 500L, seed = 42L)
  ci_2 <- bootstrap_delta_ci(pred_a, pred_b, n_iter = 500L, seed = 42L)
  expect_equal(ci_1$lower, ci_2$lower)
  expect_equal(ci_1$upper, ci_2$upper)
  expect_equal(ci_1$delta, 0.1, tolerance = 0.001)
})

test_that("bootstrap_delta_ci CI contiene il delta vero", {
  pred_a <- c(rep("correct", 80), rep("wrong", 20))
  pred_b <- c(rep("correct", 70), rep("wrong", 30))
  ci <- bootstrap_delta_ci(pred_a, pred_b, n_iter = 1000L, seed = 1812L,
                           conf = 0.95)
  expect_true(ci$lower < 0.1 && 0.1 < ci$upper)
})

test_that("holm_adjust matcha p.adjust(method='holm') base R", {
  p_values <- c(0.01, 0.02, 0.03, 0.04)
  result <- holm_adjust(p_values)
  expected <- p.adjust(p_values, method = "holm")
  expect_equal(result, expected)
})
```

- [ ] **Step 2.2: Esegui test → DEVONO fallire (funzioni non esistono)**

```bash
Rscript --vanilla -e 'devtools::test(filter = "eval-stats")'
```

Expected: errore "could not find function 'wilson_ci'" (o equivalente).

- [ ] **Step 2.3: Implementa `R/eval-stats.R`**

```r
#' Wilson 95% CI per proporzione
#'
#' Implementa la formula di Wilson (1927) per intervallo di confidenza
#' su proporzione binomiale. Preferito a normal approximation per p
#' vicino ai bordi (0 o 1) o n piccolo. Ritorna NA se n=0.
#'
#' @param successes numero di successi (intero)
#' @param n numero totale di osservazioni (intero)
#' @param conf livello di confidenza (default 0.95)
#' @return list con campi estimate, lower, upper, n
#' @export
wilson_ci <- function(successes, n, conf = 0.95) {
  stopifnot(successes >= 0L, n >= 0L, successes <= n,
            conf > 0, conf < 1)
  if (n == 0L) {
    return(list(estimate = NA_real_, lower = NA_real_,
                upper = NA_real_, n = 0L))
  }
  z <- stats::qnorm(1 - (1 - conf) / 2)
  p <- successes / n
  denom <- 1 + z^2 / n
  center <- (p + z^2 / (2 * n)) / denom
  half_width <- (z * sqrt(p * (1 - p) / n + z^2 / (4 * n^2))) / denom
  list(
    estimate = p,
    lower = max(0, center - half_width),
    upper = min(1, center + half_width),
    n = n
  )
}

#' McNemar test paired su due classificatori
#'
#' Confronta due classificatori sulle stesse osservazioni rispetto a un
#' gold standard implicito (ognuno e' "correct" o "wrong"). Calcola b
#' (A correct, B wrong) e c (A wrong, B correct), poi statistica chi^2 =
#' (b-c)^2 / (b+c). Sample con NA in entrambi i predittori esclusi.
#'
#' Equivalente a `mcnemar.test(table(pred_a, pred_b), correct = continuity)`.
#'
#' @param pred_a character vector con valori "correct"/"wrong" del primo classificatore
#' @param pred_b character vector con valori "correct"/"wrong" del secondo classificatore
#' @param continuity applica correzione di continuita' (Yates), default FALSE
#' @return list con statistic, p_value, b, c, n
#' @export
mcnemar_paired <- function(pred_a, pred_b, continuity = FALSE) {
  stopifnot(length(pred_a) == length(pred_b))
  keep <- !is.na(pred_a) & !is.na(pred_b)
  a <- pred_a[keep]
  b_vec <- pred_b[keep]
  n <- length(a)
  b <- sum(a == "correct" & b_vec == "wrong")
  c <- sum(a == "wrong" & b_vec == "correct")
  if (b + c == 0L) {
    return(list(statistic = NA_real_, p_value = NA_real_,
                b = b, c = c, n = n))
  }
  chi2 <- if (continuity) {
    (abs(b - c) - 1)^2 / (b + c)
  } else {
    (b - c)^2 / (b + c)
  }
  p_value <- stats::pchisq(chi2, df = 1, lower.tail = FALSE)
  list(statistic = chi2, p_value = p_value, b = b, c = c, n = n)
}

#' Bootstrap CI sul delta accuracy tra due classificatori
#'
#' Resampling sample-level (non bootstrap pairs separati). A ogni iterazione
#' rifa il calcolo accuracy_a - accuracy_b sui sample resampled. Ritorna CI
#' percentile classico.
#'
#' @param pred_a character vector "correct"/"wrong"
#' @param pred_b character vector "correct"/"wrong"
#' @param n_iter numero di iterazioni bootstrap (default 1000)
#' @param seed seed per riproducibilita' (default 1812)
#' @param conf livello di confidenza (default 0.95)
#' @return list con delta, lower, upper, n_iter
#' @export
bootstrap_delta_ci <- function(pred_a, pred_b, n_iter = 1000L,
                                seed = 1812L, conf = 0.95) {
  stopifnot(length(pred_a) == length(pred_b),
            n_iter >= 100L, conf > 0, conf < 1)
  keep <- !is.na(pred_a) & !is.na(pred_b)
  a <- pred_a[keep]
  b <- pred_b[keep]
  n <- length(a)
  if (n == 0L) {
    return(list(delta = NA_real_, lower = NA_real_,
                upper = NA_real_, n_iter = n_iter))
  }
  delta_obs <- mean(a == "correct") - mean(b == "correct")
  set.seed(seed)
  deltas <- numeric(n_iter)
  for (i in seq_len(n_iter)) {
    idx <- sample.int(n, replace = TRUE)
    deltas[i] <- mean(a[idx] == "correct") - mean(b[idx] == "correct")
  }
  alpha <- 1 - conf
  ci <- stats::quantile(deltas, probs = c(alpha / 2, 1 - alpha / 2),
                        names = FALSE)
  list(delta = delta_obs, lower = ci[1], upper = ci[2], n_iter = n_iter)
}

#' Holm correction per p-values multipli
#'
#' Wrapper su `p.adjust(method = "holm")` per consistenza nel report.
#' Holm e' uniformly more powerful di Bonferroni a parita' di FWER.
#'
#' @param p_values numeric vector di p-values
#' @return numeric vector di p-values corretti
#' @export
holm_adjust <- function(p_values) {
  stats::p.adjust(p_values, method = "holm")
}
```

- [ ] **Step 2.4: Esegui test → DEVONO passare**

```bash
Rscript --vanilla -e 'devtools::test(filter = "eval-stats")'
```

Expected: 8 PASS / 0 FAIL.

- [ ] **Step 2.5: Aggiorna documentazione roxygen**

```bash
Rscript --vanilla -e 'devtools::document()'
```

Expected: 4 nuovi `.Rd` creati in `man/`.

- [ ] **Step 2.6: Commit**

```bash
git add R/eval-stats.R tests/testthat/test-eval-stats.R man/wilson_ci.Rd man/mcnemar_paired.Rd man/bootstrap_delta_ci.Rd man/holm_adjust.Rd NAMESPACE
git commit -m "P3.5a Task 2: eval-stats.R con Wilson CI + McNemar + bootstrap + Holm"
```

---

## Task 3: `R/p35a-select-gse.R` parte 1 — `load_rummageo_index()`

**Files:**
- Crea: `R/p35a-select-gse.R`
- Crea: `tests/testthat/test-p35a-select-gse.R`
- Crea: `tests/testthat/fixtures/rummageo-index-mock.json`

`load_rummageo_index(cache_dir, api_base)` scarica l'indice completo dei GSE in RummaGEO via paginazione GraphQL `gseInfos(first: 1000, offset: ...)`, salva un JSONL in cache, ritorna un tibble `tibble(gse, n_signatures)`. Se il GSE field contiene multi-GSE (`"GSE1,GSE2"` SuperSeries), splitta e produce una riga per GSE atomico.

- [ ] **Step 3.1: Crea cassetta mock `tests/testthat/fixtures/rummageo-index-mock.json`**

```json
{
  "data": {
    "gseInfos": {
      "nodes": [
        {"gse": "GSE100001", "sampleGroups": {"titles": {"1": "treated", "2": "ctrl"}, "samples": {"1": ["GSM1"], "2": ["GSM2"]}}},
        {"gse": "GSE100002", "sampleGroups": {"titles": {"1": "+drug", "2": "DMSO"}, "samples": {"1": ["GSM3", "GSM4"], "2": ["GSM5"]}}},
        {"gse": "GSE100003,GSE100004", "sampleGroups": {"titles": {"1": "ko", "2": "wt"}, "samples": {"1": ["GSM6"], "2": ["GSM7"]}}}
      ]
    }
  }
}
```

- [ ] **Step 3.2: Scrivi test (failing) per `load_rummageo_index()`**

Append a `tests/testthat/test-p35a-select-gse.R`:

```r
test_that("load_rummageo_index parsa cassetta mock con SuperSeries", {
  fixture_path <- testthat::test_path("fixtures", "rummageo-index-mock.json")
  # Mock helper inietta il path al posto del fetch live
  result <- .parse_rummageo_index_response(
    jsonlite::read_json(fixture_path, simplifyVector = FALSE)
  )
  expect_s3_class(result, "tbl_df")
  expect_setequal(result$gse,
                  c("GSE100001", "GSE100002", "GSE100003", "GSE100004"))
  expect_equal(nrow(result), 4L)
  # n_signatures = somma dei sample nei sampleGroups
  expect_equal(result$n_signatures[result$gse == "GSE100002"], 3L)
})

test_that("load_rummageo_index usa cache se presente", {
  tmp_dir <- withr::local_tempdir()
  fixture_path <- testthat::test_path("fixtures", "rummageo-index-mock.json")
  fixture_data <- jsonlite::read_json(fixture_path, simplifyVector = FALSE)
  # Pre-popola cache come ci si aspetta che l'helper cache scriva
  parsed <- .parse_rummageo_index_response(fixture_data)
  jsonlite::write_json(parsed, file.path(tmp_dir, "rummageo-index.json"),
                       auto_unbox = TRUE)
  # Hit cache: la chiamata non deve fare network. Stub api_base "" significa fail.
  result <- load_rummageo_index(cache_dir = tmp_dir, api_base = "http://localhost:0")
  expect_equal(nrow(result), 4L)
})
```

- [ ] **Step 3.3: Verifica che i test falliscano**

```bash
Rscript --vanilla -e 'devtools::test(filter = "p35a-select-gse")'
```

Expected: errore "could not find function 'load_rummageo_index'" (o "`.parse_rummageo_index_response`").

- [ ] **Step 3.4: Implementa `load_rummageo_index()` in `R/p35a-select-gse.R`**

Crea `R/p35a-select-gse.R` con:

```r
`%||%` <- function(x, y) if (is.null(x) || (length(x) == 1L && is.na(x))) y else x

#' @noRd
.parse_rummageo_index_response <- function(body) {
  nodes <- body$data$gseInfos$nodes %||% list()
  rows <- list()
  for (node in nodes) {
    gse_field <- node$gse %||% ""
    if (nchar(gse_field) == 0L) next
    n_sig <- sum(vapply(
      node$sampleGroups$samples %||% list(),
      function(s) length(s),
      integer(1)
    ))
    for (gse in strsplit(gse_field, ",")[[1L]]) {
      gse_clean <- trimws(gse)
      if (nchar(gse_clean) == 0L) next
      rows[[length(rows) + 1L]] <- tibble::tibble(
        gse = gse_clean,
        n_signatures = as.integer(n_sig)
      )
    }
  }
  if (length(rows) == 0L) {
    return(tibble::tibble(gse = character(0), n_signatures = integer(0)))
  }
  out <- dplyr::bind_rows(rows)
  out <- out[!duplicated(out$gse), ]
  out
}

#' @noRd
.fetch_rummageo_index_page <- function(api_base, page_size, offset) {
  url <- paste0(api_base, "/graphql")
  query <- sprintf(
    '{ gseInfos(first: %d, offset: %d) { nodes { gse sampleGroups } } }',
    page_size, offset
  )
  resp <- httr2::request(url) |>
    httr2::req_headers("Content-Type" = "application/json") |>
    httr2::req_body_json(list(query = query)) |>
    httr2::req_retry(max_tries = 3L,
                     backoff = function(n) 2^(n - 1L)) |>
    httr2::req_perform()
  if (httr2::resp_status(resp) >= 400L) {
    rlang::abort(
      sprintf("RummaGEO index fetch HTTP %d", httr2::resp_status(resp)),
      class = "simulomicsr_rummageo_index_unavailable"
    )
  }
  httr2::resp_body_json(resp, simplifyVector = FALSE)
}

#' Scarica indice completo GSE da RummaGEO con cache filesystem
#'
#' Pagina via GraphQL `gseInfos(first: 1000, offset: ...)` finche' i nodi
#' ritornati sono < page_size (ultimo batch). Salva il risultato come
#' `<cache_dir>/rummageo-index.json`. Se il file esiste, lo legge senza
#' rifare il fetch. Splitta GSE multipli (SuperSeries "GSE1,GSE2") in righe
#' separate per atomicita' di intersezione downstream.
#'
#' @param cache_dir directory cache (NULL = no cache, sempre fetch live)
#' @param api_base URL base API (default `"https://rummageo.com"`)
#' @param page_size dimensione pagina (default 1000)
#' @param max_pages limite paginazione difensivo (default 50, ~50k studi)
#' @return tibble con colonne `gse` (atomic GSE accession), `n_signatures` (somma sample nei sampleGroups)
#' @export
load_rummageo_index <- function(cache_dir = NULL,
                                 api_base = "https://rummageo.com",
                                 page_size = 1000L,
                                 max_pages = 50L) {
  if (!is.null(cache_dir)) {
    cache_path <- file.path(cache_dir, "rummageo-index.json")
    if (file.exists(cache_path)) {
      cached <- jsonlite::read_json(cache_path, simplifyVector = TRUE)
      return(tibble::as_tibble(cached))
    }
  }
  all_rows <- list()
  for (page in seq_len(max_pages)) {
    offset <- (page - 1L) * page_size
    body <- .fetch_rummageo_index_page(api_base, page_size, offset)
    parsed <- .parse_rummageo_index_response(body)
    if (nrow(parsed) == 0L) break
    all_rows[[length(all_rows) + 1L]] <- parsed
    nodes_returned <- length(body$data$gseInfos$nodes %||% list())
    if (nodes_returned < page_size) break
  }
  out <- if (length(all_rows) == 0L) {
    tibble::tibble(gse = character(0), n_signatures = integer(0))
  } else {
    dplyr::bind_rows(all_rows)
  }
  out <- out[!duplicated(out$gse), ]
  if (!is.null(cache_dir)) {
    fs::dir_create(cache_dir, recurse = TRUE)
    jsonlite::write_json(out, file.path(cache_dir, "rummageo-index.json"),
                         auto_unbox = TRUE, pretty = TRUE)
  }
  out
}
```

- [ ] **Step 3.5: Esegui test → DEVONO passare**

```bash
Rscript --vanilla -e 'devtools::test(filter = "p35a-select-gse")'
```

Expected: 2 PASS / 0 FAIL.

- [ ] **Step 3.6: Commit**

```bash
git add R/p35a-select-gse.R tests/testthat/test-p35a-select-gse.R tests/testthat/fixtures/rummageo-index-mock.json
git commit -m "P3.5a Task 3: load_rummageo_index() + cassetta mock + 2 test"
```

---

## Task 4: `R/p35a-select-gse.R` parte 2 — `keyword_design_kind_proxy()` + `intersect_with_xlsx_and_archs4()`

**Files:**
- Modifica: `R/p35a-select-gse.R`
- Modifica: `tests/testthat/test-p35a-select-gse.R`
- Crea: `tests/testthat/fixtures/p35a-xlsx-mock.tsv`

`keyword_design_kind_proxy(strings)` applica regex a un vettore di metadata strings (tipicamente concatenazione dei `string` xlsx di un GSE) e ritorna un design_kind candidato secondo la priorità: factorial > mediated_effect > time_course > inducible/knockdown > knockout_vs_wt > disease_vs_normal > treatment_vs_vehicle > treatment_vs_untreated > unknown.

`intersect_with_xlsx_and_archs4(rummageo_index, xlsx_df, archs4_studies)` intersection di tre liste di GSE; ritorna pool candidato.

- [ ] **Step 4.1: Crea fixture mock xlsx-like `tests/testthat/fixtures/p35a-xlsx-mock.tsv`**

```
geo_accession	series_id	string	trtctr_EP
GSM_A1	GSE_A	"si-GAPDH knockdown HEK293"	treated
GSM_A2	GSE_A	"non-targeting siNT control HEK293"	control
GSM_B1	GSE_B	"+Dox induced shRNA TP53 24h"	treated
GSM_B2	GSE_B	"-Dox no induction shRNA TP53 24h"	control
GSM_C1	GSE_C	"0Gy sham irradiation MCF7"	control
GSM_C2	GSE_C	"10Gy ionizing radiation MCF7"	treated
GSM_D1	GSE_D	"DMSO vehicle 24h HepG2"	control
GSM_D2	GSE_D	"trametinib 1uM 24h HepG2"	treated
GSM_E1	GSE_E	"baseline 0h time course"	control
GSM_E2	GSE_E	"6h timepoint"	treated
GSM_E3	GSE_E	"24h timepoint"	treated
GSM_F1	GSE_F	"glioblastoma patient sample"	treated
GSM_F2	GSE_F	"healthy control normal cortex"	control
GSM_G1	GSE_G	"conditioned media transwell coculture"	treated
GSM_G2	GSE_G	"direct contact bystander effect"	treated
GSM_H1	GSE_H	"WT mouse"	control
GSM_H2	GSE_H	"TP53 -/- knockout mouse"	treated
GSM_I1	GSE_I	"+drug +siRNA factorial design"	treated
GSM_I2	GSE_I	"-drug +siRNA factorial design"	treated
GSM_I3	GSE_I	"+drug -siRNA factorial design"	treated
GSM_I4	GSE_I	"-drug -siRNA control factorial design"	control
GSM_X1	GSE_X	"random nondescript metadata"	treated
```

- [ ] **Step 4.2: Scrivi test (failing) per `keyword_design_kind_proxy()`**

Append a `tests/testthat/test-p35a-select-gse.R`:

```r
test_that("keyword_design_kind_proxy classifica esempi rappresentativi", {
  cases <- list(
    list(string = "si-GAPDH knockdown HEK293",       expected = "knockdown_panel"),
    list(string = "+Dox induced shRNA TP53",         expected = "knockdown_panel"),
    list(string = "0Gy sham 10Gy irradiation MCF7",  expected = "treatment_vs_untreated"),
    list(string = "DMSO vehicle trametinib 1uM",     expected = "treatment_vs_vehicle"),
    list(string = "0h 6h 24h time course",           expected = "time_course"),
    list(string = "glioblastoma patient healthy normal", expected = "disease_vs_normal"),
    list(string = "conditioned media transwell coculture bystander", expected = "mediated_effect"),
    list(string = "WT mouse TP53 -/- knockout mouse", expected = "knockout_vs_wt"),
    list(string = "+drug +siRNA factorial",          expected = "factorial"),
    list(string = "random nondescript",              expected = "unknown")
  )
  for (case in cases) {
    expect_equal(
      keyword_design_kind_proxy(case$string),
      case$expected,
      info = paste("Failed on:", case$string)
    )
  }
})

test_that("keyword_design_kind_proxy applica priorita' factorial > altri", {
  # Stringa con keyword multipli: factorial vince
  expect_equal(
    keyword_design_kind_proxy("+drug +siRNA factorial DMSO vehicle 0h 6h 24h"),
    "factorial"
  )
})

test_that("keyword_design_kind_proxy e' vectorized", {
  strings <- c("si-GAPDH", "DMSO vehicle drug", "random")
  expected <- c("knockdown_panel", "treatment_vs_vehicle", "unknown")
  expect_equal(keyword_design_kind_proxy(strings), expected)
})

test_that("intersect_with_xlsx_and_archs4 produce intersect corretta", {
  rummageo_idx <- tibble::tibble(
    gse = c("GSE_A", "GSE_B", "GSE_C", "GSE_NOT_IN_XLSX"),
    n_signatures = c(2L, 2L, 2L, 5L)
  )
  xlsx_df <- tibble::tibble(
    geo_accession = c("GSM_A1", "GSM_A2", "GSM_B1", "GSM_C1", "GSM_D1"),
    series_id = c("GSE_A", "GSE_A", "GSE_B", "GSE_C", "GSE_D"),
    string = c("a", "a", "b", "c", "d"),
    trtctr_EP = c("treated", "control", "treated", "control", "treated")
  )
  archs4_studies <- c("GSE_A", "GSE_B", "GSE_NOT_IN_XLSX")
  pool <- intersect_with_xlsx_and_archs4(rummageo_idx, xlsx_df, archs4_studies)
  # Solo GSE_A e GSE_B sono in tutti e tre
  expect_setequal(pool$gse, c("GSE_A", "GSE_B"))
  expect_true(all(c("gse", "n_signatures", "n_samples_xlsx",
                    "in_archs4") %in% names(pool)))
  expect_equal(pool$n_samples_xlsx[pool$gse == "GSE_A"], 2L)
})

test_that("intersect_with_xlsx_and_archs4 con archs4_studies = NULL ammette tutto", {
  rummageo_idx <- tibble::tibble(gse = c("GSE_A", "GSE_B"), n_signatures = c(2L, 2L))
  xlsx_df <- tibble::tibble(
    geo_accession = c("GSM_A1", "GSM_B1"),
    series_id = c("GSE_A", "GSE_B"),
    string = c("a", "b"),
    trtctr_EP = c("treated", "treated")
  )
  pool <- intersect_with_xlsx_and_archs4(rummageo_idx, xlsx_df,
                                          archs4_studies = NULL)
  expect_setequal(pool$gse, c("GSE_A", "GSE_B"))
  expect_true(all(is.na(pool$in_archs4)))
})
```

- [ ] **Step 4.3: Verifica fail**

```bash
Rscript --vanilla -e 'devtools::test(filter = "p35a-select-gse")'
```

Expected: errori "could not find function 'keyword_design_kind_proxy'" / "intersect_with_xlsx_and_archs4".

- [ ] **Step 4.4: Implementa `keyword_design_kind_proxy()` e `intersect_with_xlsx_and_archs4()`**

Append a `R/p35a-select-gse.R`:

```r
# Pattern regex per keyword proxy. Ordine = priorita' (top wins).
.DESIGN_KIND_PATTERNS <- list(
  factorial = list(
    # Almeno 2 fattori incrociati: cerco "factorial" esplicito + presenza
    # di pattern "+X +Y" o "+X -Y" che suggerisca fattori multipli
    "factorial",
    "\\+\\w+\\s+\\+\\w+",
    "\\+\\w+\\s+-\\w+"
  ),
  mediated_effect = list(
    "conditioned\\s+media", "transwell", "co-?culture",
    "paracrine", "bystander\\s+effect"
  ),
  time_course = list(
    # Almeno 2 timepoint distinti tipo "0h 6h 24h" o "1d 3d 7d"
    "(\\d+\\s*[hd]\\b.*){2,}",
    "time\\s*course",
    "kinetic"
  ),
  knockdown_panel = list(
    "\\bsiRNA\\b", "\\bshRNA\\b", "knockdown", "knock-?down",
    "\\bsi[A-Z][A-Z0-9]+\\b", "\\bsh[A-Z][A-Z0-9]+\\b",
    "[+-]\\s*[Dd]ox\\b", "inducible.*[Dd]ox", "[Dd]ox.*induc",
    "tetracycline.*induc"
  ),
  knockout_vs_wt = list(
    "\\bknockout\\b", "\\bKO\\b", "-/-", "\\+/\\+",
    "\\bWT\\b", "wild-?type"
  ),
  disease_vs_normal = list(
    "\\bdisease\\b", "\\bcancer\\b", "\\btumor\\b", "\\btumour\\b",
    "patient", "carcinoma", "glioblastoma", "leukemia", "lymphoma",
    "(healthy|normal).*(control|tissue|cortex|donor)"
  ),
  treatment_vs_untreated = list(
    "(\\d+\\s*[Gg]y\\b.*){2,}",
    "irradiat",
    "(0\\s*[Gg]y).*(\\d+\\s*[Gg]y)",
    "untreated.*treated",
    "sham.*irradiat"
  ),
  treatment_vs_vehicle = list(
    "\\bDMSO\\b", "\\bvehicle\\b", "\\bdrug\\b", "compound",
    "\\b\\d+\\s*[uµμ]M\\b", "\\b\\d+\\s*nM\\b"
  )
)

.match_one_kind <- function(s) {
  s_low <- tolower(s)
  for (kind in names(.DESIGN_KIND_PATTERNS)) {
    patterns <- .DESIGN_KIND_PATTERNS[[kind]]
    for (p in patterns) {
      if (grepl(p, s_low, perl = TRUE, ignore.case = TRUE)) {
        return(kind)
      }
    }
  }
  "unknown"
}

#' Inferisce un design_kind candidato da metadata strings via keyword proxy
#'
#' Applica regex con priorita' fissa (factorial > mediated_effect > time_course
#' > knockdown_panel > knockout_vs_wt > disease_vs_normal > treatment_vs_untreated
#' > treatment_vs_vehicle > unknown). Usato SOLO per stratificazione del pool;
#' la classificazione vera e' Stage 2 LLM downstream.
#'
#' @param strings character vector (tipicamente concatenazione dei `string`
#'   xlsx dei sample di un GSE)
#' @return character vector di design_kind con stessa lunghezza
#' @export
keyword_design_kind_proxy <- function(strings) {
  vapply(strings, .match_one_kind, character(1), USE.NAMES = FALSE)
}

#' Intersezione tre-vie GSE: RummaGEO official, xlsx gold, ARCHS4 studies
#'
#' Produce il pool candidato per la selezione. Ogni GSE deve essere presente
#' in tutti e tre. Se `archs4_studies = NULL`, salta il filtro ARCHS4 (utile
#' per dev offline; la prod imposta ARCHS4 path).
#'
#' @param rummageo_index tibble da `load_rummageo_index()` (col: `gse`, `n_signatures`)
#' @param xlsx_df tibble da `read_samples_input()` (col: `geo_accession`,
#'   `series_id`, `string`, `trtctr_EP`)
#' @param archs4_studies character vector di GSE in ARCHS4 (NULL = no filter)
#' @return tibble con colonne `gse`, `n_signatures`, `n_samples_xlsx`,
#'   `in_archs4` (TRUE/FALSE/NA), `concat_strings` (concatenazione dei `string`
#'   xlsx per il GSE, usata downstream da `keyword_design_kind_proxy`)
#' @export
intersect_with_xlsx_and_archs4 <- function(rummageo_index, xlsx_df,
                                            archs4_studies = NULL) {
  stopifnot("gse" %in% names(rummageo_index),
            all(c("series_id", "string") %in% names(xlsx_df)))
  xlsx_per_gse <- xlsx_df |>
    dplyr::group_by(.data$series_id) |>
    dplyr::summarise(
      n_samples_xlsx = dplyr::n(),
      concat_strings = paste(.data$string, collapse = " || "),
      .groups = "drop"
    ) |>
    dplyr::rename(gse = "series_id")
  joined <- dplyr::inner_join(rummageo_index, xlsx_per_gse, by = "gse")
  joined$in_archs4 <- if (is.null(archs4_studies)) {
    NA
  } else {
    joined$gse %in% archs4_studies
  }
  if (!is.null(archs4_studies)) {
    joined <- joined[joined$in_archs4, ]
  }
  tibble::as_tibble(joined)
}
```

- [ ] **Step 4.5: Esegui test → DEVONO passare**

```bash
Rscript --vanilla -e 'devtools::test(filter = "p35a-select-gse")'
```

Expected: 6 PASS / 0 FAIL (cumulativo Task 3 + 4).

- [ ] **Step 4.6: Aggiorna documentazione**

```bash
Rscript --vanilla -e 'devtools::document()'
```

Expected: 3 nuovi `.Rd` per `load_rummageo_index`, `keyword_design_kind_proxy`, `intersect_with_xlsx_and_archs4`.

- [ ] **Step 4.7: Commit**

```bash
git add R/p35a-select-gse.R tests/testthat/test-p35a-select-gse.R tests/testthat/fixtures/p35a-xlsx-mock.tsv man/keyword_design_kind_proxy.Rd man/intersect_with_xlsx_and_archs4.Rd man/load_rummageo_index.Rd NAMESPACE
git commit -m "P3.5a Task 4: keyword_design_kind_proxy() + intersect_with_xlsx_and_archs4()"
```

---

## Task 5: `R/p35a-select-gse.R` parte 3 — `stratified_sample_gse()` + RUN selezione live

**Files:**
- Modifica: `R/p35a-select-gse.R`
- Modifica: `tests/testthat/test-p35a-select-gse.R`
- Crea (RUN live): `inst/extdata/p35a-gse-selected.csv`
- Crea (RUN live): `analysis/cache/rummageo/rummageo-index.json`

`stratified_sample_gse(pool_with_proxy, target_distribution, seed)` campiona deterministicamente dal pool verso la distribuzione target; fallback su `treatment_vs_vehicle` per categorie povere.

- [ ] **Step 5.1: Scrivi test (failing) `stratified_sample_gse()`**

Append:

```r
test_that("stratified_sample_gse rispetta target distribution con seed", {
  set.seed(NULL)
  # Pool sintetico con conteggi noti
  pool <- tibble::tibble(
    gse = sprintf("GSE%05d", 1:200),
    design_kind_proxy = c(
      rep("factorial", 20),
      rep("time_course", 20),
      rep("mediated_effect", 5),
      rep("treatment_vs_vehicle", 50),
      rep("knockdown_panel", 30),
      rep("disease_vs_normal", 30),
      rep("treatment_vs_untreated", 25),
      rep("unknown", 20)
    ),
    n_signatures = sample.int(20L, 200L, replace = TRUE),
    n_samples_xlsx = sample.int(40L, 200L, replace = TRUE)
  )
  target <- c(
    factorial = 15L, time_course = 15L, mediated_effect = 10L,
    treatment_vs_vehicle = 20L, knockdown_panel = 15L,
    disease_vs_normal = 15L, treatment_vs_untreated = 10L
  )
  result_1 <- stratified_sample_gse(pool, target, seed = 1812L)
  result_2 <- stratified_sample_gse(pool, target, seed = 1812L)
  # Determinismo
  expect_equal(result_1$gse, result_2$gse)
  # Totale = 100
  expect_equal(nrow(result_1), 100L)
  # Distribuzione: factorial < target → tutti disponibili (20 disponibili, target 15 → 15)
  expect_equal(sum(result_1$design_kind_proxy == "factorial"), 15L)
  # mediated_effect: 5 disponibili, target 10 → tutti i 5
  expect_equal(sum(result_1$design_kind_proxy == "mediated_effect"), 5L)
  # Deficit (10-5=5) ridistribuito su treatment_vs_vehicle: target 20 + 5 = 25
  expect_equal(sum(result_1$design_kind_proxy == "treatment_vs_vehicle"), 25L)
})

test_that("stratified_sample_gse non usa unknown nella stratificazione", {
  pool <- tibble::tibble(
    gse = sprintf("GSE%05d", 1:50),
    design_kind_proxy = c(rep("factorial", 30), rep("unknown", 20)),
    n_signatures = 10L,
    n_samples_xlsx = 10L
  )
  target <- c(factorial = 10L)
  result <- stratified_sample_gse(pool, target, seed = 1812L)
  expect_equal(nrow(result), 10L)
  expect_true(all(result$design_kind_proxy == "factorial"))
})
```

- [ ] **Step 5.2: Verifica fail**

```bash
Rscript --vanilla -e 'devtools::test(filter = "p35a-select-gse")'
```

Expected: errore "could not find function 'stratified_sample_gse'".

- [ ] **Step 5.3: Implementa `stratified_sample_gse()`**

Append a `R/p35a-select-gse.R`:

```r
#' Stratified sampling deterministico dal pool candidato
#'
#' Per ogni categoria in `target`, prende `min(target[k], n_disponibili)` GSE
#' (random uniform su quelli con `design_kind_proxy == k`). Categoria povera
#' (n_disponibili < target) → prendi tutti, deficit ridistribuito su
#' `treatment_vs_vehicle`. GSE con `design_kind_proxy = "unknown"` sono
#' esclusi dalla selezione (non aggiungono diversita' nota).
#'
#' Determinismo garantito da `seed` esplicito (default 1812 per consistenza
#' con P2/P3).
#'
#' @param pool tibble con colonne `gse`, `design_kind_proxy`, `n_signatures`,
#'   `n_samples_xlsx`, `in_archs4` (opzionale)
#' @param target named integer vector (es.
#'   `c(factorial = 15, time_course = 15, ...)`); somma = N totale desiderato
#' @param seed seed RNG (default 1812)
#' @return tibble con stessi campi di `pool` filtrato ai GSE selezionati
#' @export
stratified_sample_gse <- function(pool, target, seed = 1812L) {
  stopifnot(all(c("gse", "design_kind_proxy") %in% names(pool)),
            length(target) > 0L,
            !is.null(names(target)))
  pool <- pool[pool$design_kind_proxy != "unknown", ]
  set.seed(seed)
  selected <- list()
  deficit_total <- 0L
  for (kind in names(target)) {
    avail <- pool$gse[pool$design_kind_proxy == kind]
    want <- as.integer(target[[kind]])
    n_take <- min(want, length(avail))
    if (n_take > 0L) {
      take <- sample(avail, n_take)
      selected[[kind]] <- take
    }
    deficit_total <- deficit_total + max(0L, want - n_take)
  }
  if (deficit_total > 0L) {
    fallback_kind <- "treatment_vs_vehicle"
    already_taken <- unlist(selected, use.names = FALSE)
    extra_avail <- setdiff(
      pool$gse[pool$design_kind_proxy == fallback_kind],
      already_taken
    )
    if (length(extra_avail) > 0L) {
      n_extra <- min(deficit_total, length(extra_avail))
      selected[[paste0(fallback_kind, "_fallback")]] <-
        sample(extra_avail, n_extra)
    }
  }
  selected_gse <- unlist(selected, use.names = FALSE)
  out <- pool[pool$gse %in% selected_gse, ]
  out
}
```

- [ ] **Step 5.4: Esegui test → DEVONO passare**

```bash
Rscript --vanilla -e 'devtools::test(filter = "p35a-select-gse")'
```

Expected: 8 PASS / 0 FAIL.

- [ ] **Step 5.5: Aggiorna documentazione**

```bash
Rscript --vanilla -e 'devtools::document()'
```

- [ ] **Step 5.6: RUN live — selezione 100 GSE da pool reale**

> **STOP CHECKPOINT.** Run live richiede ~5-10 min wallclock per fetch RummaGEO index (29k+ studi paginati). Conferma prima di procedere.

```bash
Rscript --vanilla -e '
suppressMessages({
  library(simulomicsr)
  library(dplyr)
})

cache_dir <- here::here("analysis", "cache", "rummageo")
fs::dir_create(cache_dir)

message("Step 1/4: Carico indice RummaGEO official (paginated)...")
idx <- load_rummageo_index(cache_dir = cache_dir)
message(sprintf("  -> %d GSE in RummaGEO official", nrow(idx)))

message("Step 2/4: Carico xlsx gold...")
xlsx <- read_samples_input(here::here("data-raw", "relevant_sample_classified.xlsx"))
message(sprintf("  -> %d sample, %d GSE unici",
        nrow(xlsx), dplyr::n_distinct(xlsx$series_id)))

message("Step 3/4: Intersezione + keyword proxy...")
pool <- intersect_with_xlsx_and_archs4(idx, xlsx, archs4_studies = NULL)
pool$design_kind_proxy <- keyword_design_kind_proxy(pool$concat_strings)
message(sprintf("  -> %d GSE nel pool candidato", nrow(pool)))
message("  -> Distribuzione design_kind_proxy:")
print(table(pool$design_kind_proxy, useNA = "ifany"))

message("Step 4/4: Stratified sampling 100 GSE...")
target <- c(
  factorial = 15L, time_course = 15L, mediated_effect = 10L,
  treatment_vs_vehicle = 20L, knockdown_panel = 15L,
  disease_vs_normal = 15L, treatment_vs_untreated = 10L
)
selected <- stratified_sample_gse(pool, target, seed = 1812L)
message(sprintf("  -> %d GSE selezionati", nrow(selected)))
message("  -> Distribuzione finale:")
print(table(selected$design_kind_proxy))

# Output committed
out_path <- here::here("inst", "extdata", "p35a-gse-selected.csv")
selected_export <- selected |>
  dplyr::transmute(
    gse_id = gse,
    design_kind_proxy = design_kind_proxy,
    n_samples_xlsx = n_samples_xlsx,
    in_rummageo_official = TRUE,
    included_in_archs4 = ifelse(is.na(in_archs4), NA, in_archs4)
  ) |>
  dplyr::arrange(design_kind_proxy, gse_id)
write.csv(selected_export, out_path, row.names = FALSE, quote = TRUE)
message(sprintf("Scritto: %s", out_path))
'
```

Expected:
- `analysis/cache/rummageo/rummageo-index.json` creato (~10-30 MB).
- `inst/extdata/p35a-gse-selected.csv` creato con 100 righe.
- Distribuzione finale stampata che totalizza 100 GSE.

**Sanity check**:

```bash
wc -l inst/extdata/p35a-gse-selected.csv
```

Expected: 101 (header + 100 righe).

Se la distribuzione naturale del pool fa scattare il fallback massiccio su `treatment_vs_vehicle` (es. >15 deficit), accetta e annota nello spec/CLAUDE.md. Le categorie rare (`mediated_effect`, `factorial`, `time_course`) potrebbero avere n < target — è atteso.

- [ ] **Step 5.7: Commit**

```bash
git add R/p35a-select-gse.R tests/testthat/test-p35a-select-gse.R man/stratified_sample_gse.Rd inst/extdata/p35a-gse-selected.csv NAMESPACE
git commit -m "P3.5a Task 5: stratified_sample_gse() + RUN selezione live (100 GSE)"
```

> **NOTE:** `analysis/cache/rummageo/rummageo-index.json` non viene committato (cache, già `.gitignore` da P3.5-B).

---

## Task 6: Estensione `_targets.R` Stadio 1 P3.5-A + RUN Stadio 1 live

**Files:**
- Modifica: `analysis/_targets.R`

Aggiunge target per Stadio 1 P3.5-A: legge il CSV selezione, filtra xlsx ai sample dei 100 GSE, dynamic-branch `classify_sample_row` su ogni sample (~1500-2500 totali), validazione partition pass/fail.

Riusa `cache_init(stage1_cache_dir, namespace = "stage1")` per condividere cache LLM con P3 (se per caso ci sono sample di GSE già toccati prima — improbabile ma gratuito).

- [ ] **Step 6.1: Modifica `analysis/_targets.R` — aggiungi target P3.5-A Stadio 1**

Inserisci (PRIMA del blocco `# P3.5-B eval benchmark` esistente, OPPURE in un nuovo blocco `# P3.5-A scaled benchmark` alla fine prima del `# Stadio 1 eval`):

```r
  # ============================================================
  # P3.5-A scaled benchmark (100 GSE freschi paper-ready)
  # ============================================================

  tar_target(
    p35a_gse_csv,
    system.file("extdata", "p35a-gse-selected.csv", package = "simulomicsr"),
    format = "file"
  ),

  tar_target(
    curated_p35a_gse,
    {
      df <- utils::read.csv(p35a_gse_csv, stringsAsFactors = FALSE)
      sort(unique(df$gse_id))
    }
  ),

  tar_target(
    samples_input_p35a,
    {
      full <- read_samples_input(samples_input_path)
      full[full$series_id %in% curated_p35a_gse, ]
    }
  ),

  # Dynamic branching: una invocazione per riga (classify_sample_row riceve
  # tibble di 1 riga). Riusa cache namespace "stage1" per max share.
  tar_target(
    sample_facts_p35a_raw,
    classify_sample_row(
      samples_input_p35a,
      provider = "openai",
      model    = "gpt-5.5",
      cache    = cache_init(stage1_cache_dir, namespace = "stage1")
    ),
    pattern   = map(samples_input_p35a),
    iteration = "list"
  ),

  tar_target(
    sample_facts_p35a_validated,
    {
      validator <- compile_schema(sample_facts_validator)
      keep <- vapply(sample_facts_p35a_raw, function(f) {
        if (!is.null(f$.invalid_reason)) return(FALSE)
        f$.invalid_reason <- NULL
        f$.invalid_detail <- NULL
        validate_json(f, validator = validator)$valid
      }, logical(1))
      sample_facts_p35a_raw[keep]
    }
  ),

  tar_target(
    sample_facts_p35a_invalid,
    {
      validator <- compile_schema(sample_facts_validator)
      drop <- vapply(sample_facts_p35a_raw, function(f) {
        if (!is.null(f$.invalid_reason)) return(TRUE)
        f$.invalid_reason <- NULL
        f$.invalid_detail <- NULL
        !validate_json(f, validator = validator)$valid
      }, logical(1))
      sample_facts_p35a_raw[drop]
    }
  ),
```

- [ ] **Step 6.2: Build target lazy (no LLM call ancora)**

```bash
Rscript --vanilla -e 'targets::tar_make(callr_function = NULL, names = c("p35a_gse_csv", "curated_p35a_gse", "samples_input_p35a"))'
```

Expected: target costruiti senza LLM call. `samples_input_p35a` ha ~1500-2500 righe (verifica con `tar_read`).

```bash
Rscript --vanilla -e 'message("samples_input_p35a rows: ", nrow(targets::tar_read(samples_input_p35a)))'
```

- [ ] **Step 6.3: Verifica curated GSE corrispondano al CSV**

```bash
Rscript --vanilla -e 'message("curated_p35a_gse: ", length(targets::tar_read(curated_p35a_gse)), " GSE")'
```

Expected: 100.

- [ ] **Step 6.4: RUN Stadio 1 live**

> **STOP CHECKPOINT.** Walltime atteso 30-90 min. Cost ~$5-10 (gpt-5.5). API key richiesta in `.Renviron.local`.

```bash
OPENAI_API_KEY="$(grep OPENAI_API_KEY .Renviron.local | cut -d'=' -f2 | tr -d '\"')" \
  Rscript --vanilla -e '
targets::tar_make(callr_function = NULL,
                  names = c("sample_facts_p35a_raw",
                            "sample_facts_p35a_validated",
                            "sample_facts_p35a_invalid"))
'
```

Expected: ~1500-2500 LLM call, walltime 30-90 min, cost log mostrato a fine. Validazione: ~95-100% validity (soglia P2 era 1.0; aspetta ≥0.95 su pool più diversificato).

**Sanity check post-run**:

```bash
Rscript --vanilla -e '
v <- targets::tar_read(sample_facts_p35a_validated)
i <- targets::tar_read(sample_facts_p35a_invalid)
message("Validated: ", length(v), " | Invalid: ", length(i),
        " | Validity rate: ",
        round(length(v) / (length(v) + length(i)), 3))
'
```

Expected: validity ≥ 0.95.

- [ ] **Step 6.5: Commit estensione `_targets.R` (cache LLM `analysis/cache/` rimane gitignored)**

```bash
git add analysis/_targets.R
git commit -m "P3.5a Task 6: target Stadio 1 P3.5-A + RUN ~2000 sample gpt-5.5"
```

---

## Task 7: Estensione `_targets.R` Stadio 2 P3.5-A + RUN Stadio 2 live

**Files:**
- Modifica: `analysis/_targets.R`

Stadio 2 richiede `study_summary` (rentrez) + `sample_facts_list` per GSE. Per i 100 nuovi: dynamic-branch `fetch_study_summary` su `curated_p35a_gse`, poi dynamic-branch `classify_study` per GSE usando i `sample_facts_p35a_validated` filtrati.

- [ ] **Step 7.1: Modifica `analysis/_targets.R` — aggiungi target P3.5-A Stadio 2**

Append dopo i target Stadio 1 P3.5-A. `fetch_study_summary()` ha cache filesystem in `geo_summary_cache_dir` (riusato da P3) → fetch e classify nello stesso branch, no target intermedio:

```r
  # Stadio 2 P3.5-A: dynamic-branch su curated_p35a_gse, fetch summary +
  # classify_study insieme (fetch_study_summary cache su disco).
  tar_target(
    study_designs_p35a_raw,
    {
      gse <- curated_p35a_gse
      summary_obj <- fetch_study_summary(
        gse, cache_dir = geo_summary_cache_dir
      )
      facts_list <- Filter(
        function(f) (f$series_id %||% NA_character_) == gse,
        sample_facts_p35a_validated
      )
      if (length(facts_list) == 0L) {
        return(list(
          .invalid_reason = "no_validated_sample_facts",
          series_id = gse
        ))
      }
      classify_study(
        series_id = gse,
        sample_facts_list = facts_list,
        study_summary = summary_obj,
        provider = "openai", model = "gpt-5.5",
        cache = cache_init(stage2_cache_dir, namespace = "stage2")
      )
    },
    pattern = map(curated_p35a_gse),
    iteration = "list"
  ),

  tar_target(
    study_designs_p35a_validated,
    {
      validator <- compile_schema(study_designs_validator)
      keep <- vapply(study_designs_p35a_raw, function(d) {
        if (!is.null(d$.invalid_reason)) return(FALSE)
        d$.invalid_reason <- NULL
        d$.invalid_detail <- NULL
        validate_json(d, validator = validator)$valid
      }, logical(1))
      study_designs_p35a_raw[keep]
    }
  ),

  tar_target(
    study_designs_p35a_invalid,
    {
      validator <- compile_schema(study_designs_validator)
      drop <- vapply(study_designs_p35a_raw, function(d) {
        if (!is.null(d$.invalid_reason)) return(TRUE)
        d$.invalid_reason <- NULL
        d$.invalid_detail <- NULL
        !validate_json(d, validator = validator)$valid
      }, logical(1))
      study_designs_p35a_raw[drop]
    }
  ),
```

- [ ] **Step 7.2: RUN Stadio 2 live**

> **STOP CHECKPOINT.** Walltime atteso 20-60 min (5-15 min fetch GEO via rentrez + 15-45 min Stadio 2 LLM). Cost ~$2-4 (gpt-5.5).

```bash
OPENAI_API_KEY="$(grep OPENAI_API_KEY .Renviron.local | cut -d'=' -f2 | tr -d '\"')" \
  Rscript --vanilla -e '
targets::tar_make(callr_function = NULL,
                  names = c("study_designs_p35a_raw",
                            "study_designs_p35a_validated",
                            "study_designs_p35a_invalid"))
'
```

Expected: 100 fetch GEO (cached) + 100 LLM call, walltime 20-60 min, validity ≥ 0.90.

**Sanity check post-run**:

```bash
Rscript --vanilla -e '
v <- targets::tar_read(study_designs_p35a_validated)
i <- targets::tar_read(study_designs_p35a_invalid)
message("Validated: ", length(v), " | Invalid: ", length(i),
        " | Validity rate: ",
        round(length(v) / (length(v) + length(i)), 3))
'
```

- [ ] **Step 7.3: Commit**

```bash
git add analysis/_targets.R
git commit -m "P3.5a Task 7: target Stadio 2 P3.5-A + RUN 100 GSE gpt-5.5"
```

---

## Task 8: `R/p35a-investigate-gse145941.R` + RUN reclassify_verbose

**Files:**
- Crea: `R/p35a-investigate-gse145941.R`
- Crea: `tests/testthat/test-p35a-investigate.R`

`reclassify_verbose(series_id, sample_facts_list, study_summary, ...)` chiama `classify_study()` con un prompt aggiuntivo che chiede esplicitamente al modello di esplicitare la chain-of-thought. `compare_with_gold(verbose_design, gold_xlsx, original_design)` produce tabella side-by-side per Sezione 5.

- [ ] **Step 8.1: Scrivi test (failing) `tests/testthat/test-p35a-investigate.R`**

```r
test_that("compare_with_gold produce shape attesa", {
  verbose_design <- list(
    series_id = "GSE145941",
    replicate_groups = list(
      list(group_id = "g1", sample_ids = c("GSM1", "GSM2"),
           design_role = "perturbed"),
      list(group_id = "g2", sample_ids = c("GSM3", "GSM4"),
           design_role = "untreated_control")
    ),
    design_kind = "treatment_vs_untreated",
    .reasoning = "extended chain-of-thought from gpt-5.5"
  )
  gold_xlsx <- tibble::tibble(
    geo_accession = c("GSM1", "GSM2", "GSM3", "GSM4"),
    series_id = "GSE145941",
    string = c("10Gy", "10Gy", "0Gy", "0Gy"),
    gold_binary = c("treated", "treated", "control", "control")
  )
  original_design <- list(
    series_id = "GSE145941",
    replicate_groups = list(
      list(group_id = "g1", sample_ids = c("GSM1"),
           design_role = "perturbed"),
      list(group_id = "g2", sample_ids = c("GSM2", "GSM3", "GSM4"),
           design_role = "untreated_control")
    )
  )
  out <- compare_with_gold(verbose_design, gold_xlsx, original_design)
  expect_true(all(c("geo_accession", "gold_binary",
                    "simulomicsr_p3_role", "simulomicsr_reclassify_role",
                    "p3_predicted_binary", "reclassify_predicted_binary",
                    "agreement") %in% names(out)))
  expect_equal(nrow(out), 4L)
  # GSM1: P3 dice perturbed (treated), reclassify dice perturbed (treated),
  # gold dice treated → agreement=TRUE
  expect_true(out$agreement[out$geo_accession == "GSM1"])
})

test_that("reclassify_verbose mocked ritorna struttura design+reasoning", {
  # Mock di classify_study via testthat::with_mocked_bindings
  fake_design <- list(
    series_id = "GSE145941",
    replicate_groups = list(),
    .reasoning = "fake CoT"
  )
  testthat::with_mocked_bindings(
    classify_study = function(...) fake_design,
    .package = "simulomicsr",
    {
      result <- reclassify_verbose(
        series_id = "GSE145941",
        sample_facts_list = list(),
        study_summary = list(),
        cache = NULL
      )
      expect_equal(result$series_id, "GSE145941")
      expect_true(!is.null(result$.reasoning))
    }
  )
})
```

- [ ] **Step 8.2: Verifica fail**

```bash
Rscript --vanilla -e 'devtools::test(filter = "p35a-investigate")'
```

Expected: errori "could not find function".

- [ ] **Step 8.3: Implementa `R/p35a-investigate-gse145941.R`**

```r
#' Re-classify uno studio con prompt verbose-reasoning
#'
#' Chiama `classify_study()` con un'istruzione aggiuntiva (`extra_instruction`)
#' che chiede esplicitamente al modello di esplicitare la chain-of-thought
#' per (a) assegnazione GSM -> replicate_group, (b) identificazione control/
#' treatment arm, (c) inferenza design_kind. Il reasoning risultante e' inserito
#' nel campo `.reasoning` del design object.
#'
#' Usato per investigation GSE145941 (P3.5-A Sezione 5).
#'
#' @param series_id GSE accession
#' @param sample_facts_list list di sample_facts validati per il GSE
#' @param study_summary list ritornato da `fetch_study_summary()`
#' @param cache cache_init() per Stadio 2 (se NULL nessuna cache)
#' @param provider/model passati a `classify_study()`
#' @return design object con campo aggiuntivo `.reasoning`
#' @export
reclassify_verbose <- function(series_id, sample_facts_list, study_summary,
                                cache = NULL,
                                provider = "openai", model = "gpt-5.5") {
  extra <- paste(
    "ISTRUZIONE INVESTIGATION: oltre all'output JSON canonico,",
    "popola il campo opzionale `.reasoning` con una chain-of-thought",
    "esplicita che spieghi (1) come hai assegnato GSM ai replicate_groups,",
    "(2) come hai identificato il control vs treatment arm,",
    "(3) come hai inferito il design_kind. Massimo 300 parole.",
    sep = " "
  )
  classify_study(
    series_id = series_id,
    sample_facts_list = sample_facts_list,
    study_summary = study_summary,
    provider = provider,
    model = model,
    cache = cache,
    extra_instruction = extra
  )
}

#' Tabella side-by-side gold xlsx vs simulomicsr P3 vs reclassify verbose
#'
#' @param verbose_design design object da `reclassify_verbose()`
#' @param gold_xlsx tibble con colonne geo_accession, series_id, string,
#'   gold_binary (treated/control/NA)
#' @param original_design design object dal run P3 originale
#' @return tibble con colonne geo_accession, gold_binary,
#'   simulomicsr_p3_role, simulomicsr_reclassify_role,
#'   p3_predicted_binary, reclassify_predicted_binary, agreement
#' @export
compare_with_gold <- function(verbose_design, gold_xlsx, original_design) {
  flatten_design <- function(d) {
    rows <- list()
    for (g in d$replicate_groups %||% list()) {
      role <- g$design_role
      for (sid in (g$sample_ids %||% list())) {
        rows[[length(rows) + 1L]] <- tibble::tibble(
          geo_accession = as.character(sid),
          design_role = role
        )
      }
    }
    if (length(rows) == 0L) {
      return(tibble::tibble(geo_accession = character(0),
                            design_role = character(0)))
    }
    dplyr::bind_rows(rows)
  }
  p3 <- flatten_design(original_design) |>
    dplyr::rename(simulomicsr_p3_role = "design_role")
  rev <- flatten_design(verbose_design) |>
    dplyr::rename(simulomicsr_reclassify_role = "design_role")
  out <- gold_xlsx |>
    dplyr::select("geo_accession", "gold_binary") |>
    dplyr::left_join(p3, by = "geo_accession") |>
    dplyr::left_join(rev, by = "geo_accession") |>
    dplyr::mutate(
      p3_predicted_binary = design_role_to_binary(.data$simulomicsr_p3_role),
      reclassify_predicted_binary = design_role_to_binary(.data$simulomicsr_reclassify_role),
      agreement = !is.na(.data$gold_binary) &
                  !is.na(.data$reclassify_predicted_binary) &
                  .data$gold_binary == .data$reclassify_predicted_binary
    )
  tibble::as_tibble(out)
}
```

- [ ] **Step 8.4: Esegui test → DEVONO passare**

```bash
Rscript --vanilla -e 'devtools::test(filter = "p35a-investigate")'
```

Expected: 2 PASS / 0 FAIL.

> **NOTA:** se `classify_study()` non supporta il param `extra_instruction`, va aggiunto. Verifica:

```bash
Rscript --vanilla -e 'args(classify_study)'
```

Se manca, aggiungi parametro a `R/llm-stage2.R::classify_study()` e a `build_prompt_stage2()` (concatena `extra_instruction` al prompt user). Test mockato deve sempre passare.

- [ ] **Step 8.5: Aggiungi target `gse145941_reclassify` in `_targets.R`**

```r
  tar_target(
    gse145941_reclassify,
    {
      facts_list <- Filter(
        function(f) (f$series_id %||% NA_character_) == "GSE145941",
        sample_facts_validated
      )
      summary_obj <- fetch_study_summary("GSE145941",
                                          cache_dir = geo_summary_cache_dir)
      reclassify_verbose(
        series_id = "GSE145941",
        sample_facts_list = facts_list,
        study_summary = summary_obj,
        cache = cache_init(stage2_cache_dir, namespace = "stage2_verbose")
      )
    }
  ),
```

- [ ] **Step 8.6: RUN reclassify_verbose live**

> **STOP CHECKPOINT.** Walltime ~30 sec, cost ~$0.30-0.50.

```bash
OPENAI_API_KEY="$(grep OPENAI_API_KEY .Renviron.local | cut -d'=' -f2 | tr -d '\"')" \
  Rscript --vanilla -e '
targets::tar_make(callr_function = NULL, names = "gse145941_reclassify")
'
```

**Sanity check**:

```bash
Rscript --vanilla -e '
r <- targets::tar_read(gse145941_reclassify)
message("series_id: ", r$series_id)
message("design_kind: ", r$design_kind)
message("reasoning length: ", nchar(r$.reasoning %||% ""), " chars")
'
```

Expected: `series_id = GSE145941`, `.reasoning` non vuoto.

- [ ] **Step 8.7: Commit**

```bash
git add R/p35a-investigate-gse145941.R tests/testthat/test-p35a-investigate.R analysis/_targets.R man/reclassify_verbose.Rd man/compare_with_gold.Rd NAMESPACE
git commit -m "P3.5a Task 8: reclassify_verbose() + compare_with_gold() + RUN GSE145941"
```

---

## Task 9: Estensione `_targets.R` eval P3.5-A + RUN eval pipeline

**Files:**
- Modifica: `analysis/_targets.R`

Aggiunge target eval P3.5-A che riusano integralmente le funzioni di `R/eval-stage2.R` e `R/eval-rummageo.R`. Solo i nomi dei target cambiano (suffisso `_p35a`).

- [ ] **Step 9.1: Modifica `analysis/_targets.R` — aggiungi target eval P3.5-A**

Append dopo i target Stadio 2 P3.5-A:

```r
  # Eval P3.5-A: gold join + metrics + RummaGEO

  tar_target(
    gold_table_subset_p35a,
    {
      curated <- curated_p35a_gse
      full_xlsx <- read_samples_input(samples_input_path)
      full_xlsx |>
        dplyr::filter(.data$series_id %in% curated) |>
        dplyr::transmute(
          geo_accession = .data$geo_accession,
          series_id = .data$series_id,
          string = .data$string,
          gold_raw = .data$trtctr_EP,
          gold_binary = dplyr::case_when(
            tolower(.data$trtctr_EP) %in% c("treated") ~ "treated",
            tolower(.data$trtctr_EP) %in% c("control") ~ "control",
            TRUE ~ NA_character_
          )
        )
    }
  ),

  tar_target(
    eval_stage2_p35a_gold_join,
    {
      rows <- list()
      for (design in study_designs_p35a_validated) {
        sid <- design$series_id
        for (g in design$replicate_groups) {
          for (gsm in g$sample_ids) {
            rows[[length(rows) + 1L]] <- tibble::tibble(
              series_id = sid,
              geo_accession = gsm,
              design_role = g$design_role,
              design_kind = design$design_kind,
              predicted_binary = design_role_to_binary(g$design_role)
            )
          }
        }
      }
      pred_df <- dplyr::bind_rows(rows)
      out <- dplyr::left_join(
        pred_df,
        gold_table_subset_p35a[, c("geo_accession", "series_id",
                                    "string", "gold_raw", "gold_binary")],
        by = c("geo_accession", "series_id")
      )
      out
    }
  ),

  tar_target(
    eval_stage2_p35a_metrics,
    {
      list(
        overall = eval_binary_accuracy(
          eval_stage2_p35a_gold_join$gold_binary,
          eval_stage2_p35a_gold_join$predicted_binary
        ),
        per_kind = eval_per_design_kind(eval_stage2_p35a_gold_join),
        granularity = flag_granularity_disagreement(eval_stage2_p35a_gold_join)
      )
    }
  ),

  tar_target(
    rummageo_p35a_signatures,
    {
      out_per_gse <- lapply(curated_p35a_gse, function(gse) {
        sub <- gold_table_subset_p35a[
          gold_table_subset_p35a$series_id == gse, ]
        if (nrow(sub) == 0L) return(NULL)
        labels <- tryCatch({
          data <- fetch_rummageo_signatures(gse, cache_dir = rummageo_cache_dir)
          parsed <- parse_rummageo_labels(data)
          if (nrow(parsed) == 0L) {
            parsed <- rummageo_baseline_internal(sub)
            attr(parsed, "source") <- "internal_fallback_empty_signatures"
          } else {
            attr(parsed, "source") <- "rummageo_official"
          }
          parsed
        }, simulomicsr_rummageo_unavailable = function(e) {
          parsed <- rummageo_baseline_internal(sub)
          attr(parsed, "source") <- "internal_fallback_unavailable"
          parsed
        }, error = function(e) {
          parsed <- rummageo_baseline_internal(sub)
          attr(parsed, "source") <- "internal_fallback_error"
          parsed
        })
        labels$series_id <- gse
        labels$source <- attr(labels, "source") %||% "unknown"
        labels
      })
      dplyr::bind_rows(Filter(Negate(is.null), out_per_gse))
    }
  ),

  tar_target(
    rummageo_p35a_metrics,
    {
      joined <- dplyr::left_join(
        eval_stage2_p35a_gold_join,
        rummageo_p35a_signatures[, c("geo_accession", "series_id",
                                      "rummageo_label", "source")],
        by = c("geo_accession", "series_id")
      )
      list(
        rummageo_vs_gold = eval_binary_accuracy(
          joined$gold_binary, joined$rummageo_label
        ),
        simulomicsr_vs_gold = eval_binary_accuracy(
          joined$gold_binary, joined$predicted_binary
        ),
        rummageo_vs_simulomicsr = eval_binary_accuracy(
          joined$predicted_binary, joined$rummageo_label
        ),
        joined_table = joined,
        source_summary = table(joined$source, useNA = "ifany")
      )
    }
  ),
```

- [ ] **Step 9.2: Build target lazy + RUN**

```bash
OPENAI_API_KEY="$(grep OPENAI_API_KEY .Renviron.local | cut -d'=' -f2 | tr -d '\"')" \
  Rscript --vanilla -e '
targets::tar_make(callr_function = NULL,
                  names = c("gold_table_subset_p35a",
                            "eval_stage2_p35a_gold_join",
                            "eval_stage2_p35a_metrics",
                            "rummageo_p35a_signatures",
                            "rummageo_p35a_metrics"))
'
```

Expected: walltime 5-30 min (RummaGEO fetch live per i 100 GSE; cache filesystem velocizza re-run).

- [ ] **Step 9.3: Sanity check metriche**

```bash
Rscript --vanilla -e '
m <- targets::tar_read(eval_stage2_p35a_metrics)
message("Overall accuracy: ", round(m$overall$accuracy, 3),
        " (n=", m$overall$n, ")")
print(m$per_kind)
r <- targets::tar_read(rummageo_p35a_metrics)
message("RummaGEO vs gold accuracy: ", round(r$rummageo_vs_gold$accuracy, 3))
message("Source summary:")
print(r$source_summary)
'
```

Expected: overall accuracy ≥ 0.80, RummaGEO source_summary mostra distribuzione tra `rummageo_official` e fallback variants. Se accuracy < 0.80, stop checkpoint per investigation case-by-case prima di proseguire.

- [ ] **Step 9.4: Commit**

```bash
git add analysis/_targets.R
git commit -m "P3.5a Task 9: target eval P3.5-A (gold join + metrics + RummaGEO)"
```

---

## Task 10: `analysis/eval/p35a-benchmark.Rmd` Quarto 5 sezioni + RUN report

**Files:**
- Crea: `analysis/eval/p35a-benchmark.Rmd`
- Modifica: `analysis/_targets.R` (aggiunge `tar_render`)

5 sezioni: binary accuracy con Wilson CI; granularity; anchor coverage intra-100; RummaGEO head-to-head con McNemar+bootstrap+Holm; investigation GSE145941.

- [ ] **Step 10.1: Crea `analysis/eval/p35a-benchmark.Rmd`**

```rmd
---
title: "P3.5-A — Scaled benchmark Stadio 2 (100 GSE paper-ready)"
author: "simulomicsr"
date: "`r Sys.Date()`"
output:
  html_document:
    toc: true
    toc_depth: 3
    theme: cosmo
    code_folding: hide
params:
  stage2_metrics: NULL
  rummageo_metrics: NULL
  comparisons_table: NULL
  curated_gse: NULL
  gse145941_reclassify: NULL
  gold_table_subset: NULL
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, message = FALSE, warning = FALSE)
library(dplyr)
library(tibble)
library(ggplot2)
library(simulomicsr)
```

# Executive summary

Benchmark scaled del classificatore Stadio 2 di simulomicsr su **100 GSE freschi** selezionati da `RummaGEO official ∩ xlsx gold`, stratificati per `design_kind` con oversampling delle categorie rare (factorial, time_course, mediated_effect). Test set indipendente dai 15 GSE di P3.

```{r exec-summary}
m <- params$stage2_metrics
r <- params$rummageo_metrics
overall_ci <- wilson_ci(
  successes = round(m$overall$accuracy * m$overall$n),
  n = m$overall$n
)
rumma_ci <- wilson_ci(
  successes = round(r$rummageo_vs_gold$accuracy * r$rummageo_vs_gold$n),
  n = r$rummageo_vs_gold$n
)
cat(sprintf(
  "- **n GSE**: %d  \n- **n sample valutati**: %d  \n- **simulomicsr accuracy**: %.1f%% (95%% Wilson CI [%.1f%%, %.1f%%])  \n- **RummaGEO accuracy**: %.1f%% (95%% Wilson CI [%.1f%%, %.1f%%])  \n",
  length(params$curated_gse), m$overall$n,
  100 * overall_ci$estimate, 100 * overall_ci$lower, 100 * overall_ci$upper,
  100 * rumma_ci$estimate, 100 * rumma_ci$lower, 100 * rumma_ci$upper
))
```

# Sezione 1 — Stage 2 binary accuracy

## Overall

```{r sec1-overall}
m <- params$stage2_metrics
ci <- wilson_ci(
  successes = round(m$overall$accuracy * m$overall$n),
  n = m$overall$n
)
tibble::tibble(
  n = m$overall$n,
  accuracy = sprintf("%.3f", m$overall$accuracy),
  wilson_ci = sprintf("[%.3f, %.3f]", ci$lower, ci$upper),
  sensitivity = sprintf("%.3f", m$overall$sensitivity),
  specificity = sprintf("%.3f", m$overall$specificity),
  f1 = sprintf("%.3f", m$overall$f1)
) |> knitr::kable(caption = "Overall binary accuracy con Wilson 95% CI")
```

## Confusion matrix

```{r sec1-cm}
print(m$overall$confusion_matrix)
```

## Breakdown per design_kind

```{r sec1-per-kind}
per_kind <- m$per_kind
per_kind$wilson_lower <- vapply(seq_len(nrow(per_kind)), function(i) {
  ci <- wilson_ci(round(per_kind$accuracy[i] * per_kind$n[i]),
                  per_kind$n[i])
  ci$lower
}, numeric(1))
per_kind$wilson_upper <- vapply(seq_len(nrow(per_kind)), function(i) {
  ci <- wilson_ci(round(per_kind$accuracy[i] * per_kind$n[i]),
                  per_kind$n[i])
  ci$upper
}, numeric(1))
per_kind |>
  dplyr::mutate(
    accuracy_ci = sprintf("%.3f [%.3f, %.3f]",
                          accuracy, wilson_lower, wilson_upper)
  ) |>
  dplyr::select(design_kind, n, accuracy_ci, sensitivity, specificity, f1) |>
  knitr::kable(caption = "Per design_kind con Wilson CI")
```

```{r sec1-plot, fig.width=8, fig.height=5}
ggplot(per_kind, aes(x = reorder(design_kind, accuracy), y = accuracy)) +
  geom_col(fill = "steelblue") +
  geom_errorbar(aes(ymin = wilson_lower, ymax = wilson_upper),
                width = 0.2) +
  geom_hline(yintercept = 0.85, linetype = "dashed", colour = "red") +
  geom_hline(yintercept = 0.80, linetype = "dashed", colour = "darkgreen") +
  coord_flip() +
  scale_y_continuous(limits = c(0, 1), labels = scales::percent) +
  labs(x = NULL, y = "Accuracy",
       title = "Stage 2 accuracy per design_kind con Wilson 95% CI",
       subtitle = "Soglie: 80% globale (verde), 85% binari semplici (rosso)")
```

# Sezione 2 — Granularity disagreement enumeration

```{r sec2}
gran <- params$stage2_metrics$granularity
if (nrow(gran) == 0L) {
  cat("Nessun caso granulare nei 100 GSE.")
} else {
  gran |>
    dplyr::count(granularity_kind, sort = TRUE) |>
    knitr::kable(caption = "Conteggi per granularity_kind")
  cat("\n\n**Esempi rappresentativi (max 2 per categoria):**\n")
  gran |>
    dplyr::group_by(granularity_kind) |>
    dplyr::slice_head(n = 2L) |>
    dplyr::ungroup() |>
    dplyr::select(geo_accession, series_id, granularity_kind, string) |>
    knitr::kable()
}
```

# Sezione 3 — Anchor coverage cross-studio (intra-100)

```{r sec3}
ct <- params$comparisons_table
if (is.null(ct) || nrow(ct) == 0L) {
  cat("Nessuna comparison nei 100 GSE.")
} else {
  by_anchor <- ct |>
    dplyr::group_by(comparability_anchor) |>
    dplyr::summarise(
      n_gse = dplyr::n_distinct(series_id),
      n_comparisons = dplyr::n(),
      .groups = "drop"
    ) |>
    dplyr::arrange(dplyr::desc(n_gse))
  multi_gse <- by_anchor |> dplyr::filter(n_gse >= 2L)
  cat(sprintf("- **Anchor unici**: %d  \n", nrow(by_anchor)))
  cat(sprintf("- **Anchor con N >= 2 GSE**: %d  \n", nrow(multi_gse)))
  cat(sprintf("- **Comparison totali**: %d  \n", nrow(ct)))
  if (nrow(multi_gse) > 0L) {
    knitr::kable(head(multi_gse, 30L),
                 caption = "Top anchor cross-GSE (potential meta-analyses)")
  }

  ggplot(by_anchor, aes(x = n_gse)) +
    geom_histogram(binwidth = 1L, fill = "darkgreen") +
    labs(x = "N GSE per anchor", y = "Conteggio anchor",
         title = "Distribuzione anchor cross-studio (intra-100)")
}
```

# Sezione 4 — RummaGEO head-to-head

## Source summary (RummaGEO official vs fallback interno)

```{r sec4-source}
print(params$rummageo_metrics$source_summary)
```

## Confusion matrices

### simulomicsr × gold
```{r sec4-cm-sim}
print(params$rummageo_metrics$simulomicsr_vs_gold$confusion_matrix)
```

### RummaGEO × gold
```{r sec4-cm-rumma}
print(params$rummageo_metrics$rummageo_vs_gold$confusion_matrix)
```

## Test inferenziale: McNemar + bootstrap delta

```{r sec4-stats}
joined <- params$rummageo_metrics$joined_table
sim_correct <- ifelse(is.na(joined$gold_binary) | is.na(joined$predicted_binary),
                      NA,
                      ifelse(joined$predicted_binary == joined$gold_binary,
                             "correct", "wrong"))
rumma_correct <- ifelse(is.na(joined$gold_binary) | is.na(joined$rummageo_label),
                        NA,
                        ifelse(joined$rummageo_label == joined$gold_binary,
                               "correct", "wrong"))
mc <- mcnemar_paired(sim_correct, rumma_correct, continuity = FALSE)
boot <- bootstrap_delta_ci(sim_correct, rumma_correct,
                            n_iter = 1000L, seed = 1812L)
tibble::tibble(
  metric = c("McNemar chi^2", "McNemar p-value",
             "Bootstrap delta (sim - rumma)",
             "Bootstrap CI lower", "Bootstrap CI upper",
             "n paired"),
  value = c(sprintf("%.3f", mc$statistic),
            sprintf("%.4f", mc$p_value),
            sprintf("%.3f", boot$delta),
            sprintf("%.3f", boot$lower),
            sprintf("%.3f", boot$upper),
            mc$n)
) |> knitr::kable(caption = "Statistica inferenziale globale")
```

## Per design_kind con Holm correction

```{r sec4-perkind}
per_kind_stats <- joined |>
  dplyr::filter(!is.na(gold_binary), !is.na(predicted_binary),
                !is.na(rummageo_label)) |>
  dplyr::group_by(design_kind) |>
  dplyr::summarise(
    n = dplyr::n(),
    sim_acc = mean(predicted_binary == gold_binary),
    rumma_acc = mean(rummageo_label == gold_binary),
    .groups = "drop"
  )
# McNemar per ogni design_kind
mc_per <- joined |>
  dplyr::filter(!is.na(gold_binary), !is.na(predicted_binary),
                !is.na(rummageo_label)) |>
  dplyr::group_by(design_kind) |>
  dplyr::summarise(
    n = dplyr::n(),
    p_value = {
      sa <- ifelse(predicted_binary == gold_binary, "correct", "wrong")
      ra <- ifelse(rummageo_label == gold_binary, "correct", "wrong")
      mcnemar_paired(sa, ra, continuity = FALSE)$p_value
    },
    .groups = "drop"
  )
mc_per$p_holm <- holm_adjust(mc_per$p_value)
out <- dplyr::left_join(per_kind_stats, mc_per, by = c("design_kind", "n"))
out |>
  dplyr::mutate(
    delta = sprintf("%+.3f", sim_acc - rumma_acc),
    p_value = sprintf("%.4f", p_value),
    p_holm = sprintf("%.4f", p_holm)
  ) |>
  dplyr::select(design_kind, n, sim_acc, rumma_acc, delta, p_value, p_holm) |>
  knitr::kable(caption = "Per design_kind: McNemar p-value + Holm-corrected")
```

# Sezione 5 — Investigation GSE145941

```{r sec5-data}
verbose <- params$gse145941_reclassify
gold <- params$gold_table_subset
gold_145941 <- gold[gold$series_id == "GSE145941", ]
```

## Re-classify chain-of-thought (gpt-5.5 verbose-reasoning)

```{r sec5-cot, results='asis'}
cat("```\n")
cat(verbose$.reasoning %||% "(no reasoning emitted)")
cat("\n```\n")
```

## Side-by-side: gold xlsx vs P3 originale vs reclassify verbose

```{r sec5-table}
# Carica original_design dal target P3 study_designs_validated
v_designs <- targets::tar_read(study_designs_validated)
original <- Filter(function(d) d$series_id == "GSE145941", v_designs)[[1]]
cmp <- compare_with_gold(verbose, gold_145941, original)
knitr::kable(cmp, caption = "GSE145941 side-by-side")
```

## Conclusione

```{r sec5-conclusion}
agreement_rate <- mean(cmp$agreement, na.rm = TRUE)
cat(sprintf("- **Agreement rate reclassify vs gold**: %.1f%%  \n",
            100 * agreement_rate))
if (agreement_rate >= 0.85) {
  cat("- **Diagnosi**: il P3 originale ha sbagliato; il modello con verbose-reasoning ricostruisce correttamente. Probabile **`llm_inference_error`** sul run originale (deterministico solo dopo cache miss).\n")
} else if (agreement_rate < 0.50) {
  cat("- **Diagnosi**: anche il reclassify non concorda. Probabile **`gold_xlsx_error`** o **`metadata_mismatch`**: il gold xlsx ha labels controintuitive, oppure i metadati testuali divergono da ARCHS4. Verifica manuale richiesta.\n")
} else {
  cat("- **Diagnosi**: agreement parziale. Probabile **`ambiguous_design`**: il dataset ha caratteristiche border-line che ammettono multiple interpretazioni legittime.\n")
}
```

---
*Generato con `tar_make(eval_p35a_report)`. RUN reproducibility seed = 1812.*
```

- [ ] **Step 10.2: Aggiungi `tar_render` in `_targets.R`**

```r
  tarchetypes::tar_render(
    eval_p35a_report,
    here::here("analysis", "eval", "p35a-benchmark.Rmd"),
    output_dir = here::here("analysis", "eval"),
    params = list(
      stage2_metrics       = eval_stage2_p35a_metrics,
      rummageo_metrics     = rummageo_p35a_metrics,
      comparisons_table    = comparisons_table,  # nota: questo e' il P3.5-B legacy; per P3.5-A serve un comparisons_table_p35a
      curated_gse          = curated_p35a_gse,
      gse145941_reclassify = gse145941_reclassify,
      gold_table_subset    = gold_table_subset_p35a
    )
  ),
```

> **NOTA:** se Sezione 3 deve essere intra-100 (non intra-15), serve un `comparisons_table_p35a` parallelo. Aggiungi:

```r
  tar_target(
    comparisons_table_p35a,
    {
      rows <- list()
      facts_idx_per_gse <- list()
      for (f in sample_facts_p35a_validated) {
        sid <- f$series_id %||% NA_character_
        if (is.na(sid)) next
        if (is.null(facts_idx_per_gse[[sid]])) facts_idx_per_gse[[sid]] <- list()
        facts_idx_per_gse[[sid]][[f$geo_accession]] <- f
      }
      for (design in study_designs_p35a_validated) {
        sid <- design$series_id
        facts_idx <- facts_idx_per_gse[[sid]] %||% list()
        groups_idx <- list()
        for (g in design$replicate_groups) {
          groups_idx[[g$group_id]] <- g
        }
        for (cmp in design$comparisons) {
          treated_grp <- groups_idx[[cmp$treated_group]]
          if (is.null(treated_grp) || length(treated_grp$sample_ids) == 0L) next
          repr_id <- treated_grp$sample_ids[[1L]]
          repr_facts <- facts_idx[[repr_id]]
          if (is.null(repr_facts)) next
          anchor <- tryCatch(
            make_anchor(repr_facts, stage2_role = treated_grp$design_role),
            error = function(e) NA_character_
          )
          control_grp <- groups_idx[[cmp$control_group]]
          n_ctrl <- if (is.null(control_grp)) 0L else length(control_grp$sample_ids)
          rows[[length(rows) + 1L]] <- tibble::tibble(
            series_id            = sid,
            comparison_id        = cmp$comparison_id,
            treated_group        = cmp$treated_group,
            control_group        = cmp$control_group,
            varying_factor       = cmp$varying_factor %||% NA_character_,
            study_internal_score = cmp$study_internal_score %||% NA_real_,
            comparability_anchor = anchor,
            anchor_version       = "v3",
            design_kind          = design$design_kind,
            n_samples_treated    = length(treated_grp$sample_ids),
            n_samples_control    = n_ctrl
          )
        }
      }
      if (length(rows) == 0L) {
        return(tibble::tibble(
          series_id = character(0), comparison_id = character(0),
          treated_group = character(0), control_group = character(0),
          varying_factor = character(0), study_internal_score = numeric(0),
          comparability_anchor = character(0), anchor_version = character(0),
          design_kind = character(0), n_samples_treated = integer(0),
          n_samples_control = integer(0)
        ))
      }
      dplyr::bind_rows(rows)
    }
  ),
```

E sostituisci nel `tar_render`:

```r
      comparisons_table    = comparisons_table_p35a,
```

- [ ] **Step 10.3: RUN report**

> **STOP CHECKPOINT.** Walltime ~30 sec.

```bash
Rscript --vanilla -e '
targets::tar_make(callr_function = NULL,
                  names = c("comparisons_table_p35a", "eval_p35a_report"))
'
```

Expected: `analysis/eval/p35a-benchmark.html` creato.

```bash
ls -la analysis/eval/p35a-benchmark.html
```

Expected: file presente, dimensione ≥ 500 KB.

- [ ] **Step 10.4: Sanity check report**

Apri il file in browser (manuale) e verifica:
- Sezione 1: tabella accuracy + grafico per_design_kind con CI
- Sezione 2: granularity ha conteggi
- Sezione 3: anchor con N≥2 GSE elencati
- Sezione 4: McNemar p-value + bootstrap CI + tabella per_design_kind
- Sezione 5: chain-of-thought GSE145941 + tabella side-by-side + diagnosi

Se Sezione 4 mostra simulomicsr peggiore di RummaGEO o McNemar non significativo: **STOP**, decisione utente prima di proseguire (eventuale iter prompt → P3.5-A2).

- [ ] **Step 10.5: Commit**

```bash
git add analysis/eval/p35a-benchmark.Rmd analysis/eval/p35a-benchmark.html analysis/_targets.R
git commit -m "P3.5a Task 10: Quarto p35a-benchmark.Rmd 5 sezioni + RUN report"
```

---

## Task 11: Pre-merge — R CMD check + bump version + NEWS

**Files:**
- Modifica: `DESCRIPTION`
- Modifica: `NEWS.md`
- Modifica: `inst/extdata/p35a-gse-selected.csv` (no ASCII fix se serve)

- [ ] **Step 11.1: R CMD check**

```bash
Rscript --vanilla -e 'devtools::check(args = "--no-manual")'
```

Expected: 0 ERROR / 0 WARNING / ≤2 NOTE (note pre-esistenti). Se nuove warnings → fix prima di merge:
- non-ASCII chars in `R/eval-stats.R`, `R/p35a-select-gse.R`, `R/p35a-investigate-gse145941.R` (es. ², µ, °) → sostituisci con ASCII o `²`.
- man pages: tutte presenti per export.

- [ ] **Step 11.2: Bump versione in `DESCRIPTION`**

```
Version: 0.0.0.9006
```

(da 0.0.0.9005)

- [ ] **Step 11.3: Aggiorna `NEWS.md`**

Append in cima (dopo l'ultima entry P3.5-B):

```markdown
# simulomicsr 0.0.0.9006

## P3.5-A — Scaled benchmark Stadio 2 (100 GSE paper-ready)

### Funzionalita' nuove

* `wilson_ci()`, `mcnemar_paired()`, `bootstrap_delta_ci()`, `holm_adjust()` —
  statistica inferenziale per benchmark paper-grade in `R/eval-stats.R`.
* `load_rummageo_index()` — scarica indice completo GSE da RummaGEO via GraphQL paginated con cache filesystem.
* `keyword_design_kind_proxy()` — inferisce design_kind candidato da metadata
  strings via regex per stratificazione del pool di selezione.
* `intersect_with_xlsx_and_archs4()` — intersezione tre-vie GSE.
* `stratified_sample_gse()` — campionamento stratificato deterministico
  (seed 1812) con fallback su categoria abbondante per categorie povere.
* `reclassify_verbose()` — re-classify Stadio 2 con prompt verbose-reasoning
  per investigation casi specifici (introdotto per GSE145941).
* `compare_with_gold()` — tabella side-by-side gold xlsx vs run P3 vs reclassify.

### Artefatti committati

* `inst/extdata/p35a-gse-selected.csv` — lista 100 GSE finali per
  riproducibilita' del set di test.
* `analysis/eval/p35a-benchmark.html` — report Quarto 5 sezioni
  (binary accuracy + Wilson CI, granularity, anchor coverage,
  RummaGEO head-to-head con McNemar + bootstrap + Holm,
  investigation GSE145941).

### Risultati run live

[Da popolare dopo Task 10 con i numeri concreti.]

### Cost LLM

* Stadio 1 (~2000 sample gpt-5.5): ~$X
* Stadio 2 (100 GSE gpt-5.5): ~$Y
* Reclassify GSE145941 verbose: ~$Z
* Totale P3.5-A: ~$T (stima ~$10-15)
```

> **NOTA:** popola i numeri reali dopo Task 10.

- [ ] **Step 11.4: Test suite finale**

```bash
Rscript --vanilla -e 'devtools::test()'
```

Expected: ≥328 PASS (aggiungono ~20 nuovi test) / 0 FAIL.

- [ ] **Step 11.5: Commit**

```bash
git add DESCRIPTION NEWS.md
git commit -m "P3.5a Task 11: bump 0.0.0.9006 + NEWS aggiornato + R CMD check"
```

---

## Task 12: Merge + tag + CLAUDE.md

**Files:**
- Modifica: `CLAUDE.md`

- [ ] **Step 12.1: Verifica branch pulita**

```bash
git status
git log --oneline master..p3.5a-eval
```

Expected: 11 commit P3.5-A, working tree pulito (eccetto `?? renv/settings.json`).

- [ ] **Step 12.2: Merge fast-forward su master**

```bash
git checkout master
git merge --ff-only p3.5a-eval
```

Expected: `Fast-forward` a HEAD del branch p3.5a-eval.

- [ ] **Step 12.3: Tag**

```bash
git tag p3.5a-eval-complete
```

- [ ] **Step 12.4: Aggiorna `CLAUDE.md` — Stato corrente, Cosa P3.5-A ha consegnato, Risultati run, Next step**

Sostituisci la sezione "Stato corrente":

```markdown
## Stato corrente (2026-MM-DD fine sessione `simulomicsr_p3_2`)

- **Master HEAD:** `<sha>` (P3.5a Task 12: tag p3.5a-eval-complete + CLAUDE.md update).
- **Tag:** `p1-infra-llm-complete` (P1), `p2-stage1-complete` (P2), `p3-stage2-complete` (P3), `p3.5b-eval-complete` (P3.5-B), **`p3.5a-eval-complete`** (P3.5-A).
- **Master locale e' ahead di `origin/master`** — l'utente fa il push lui (mai automaticamente).
- **R CMD check:** 0E / 0W / 2N (note pre-esistenti).
- **Test suite:** ~348 PASS / 0 FAIL.
```

Aggiungi nuova sezione dopo "Cosa P3.5-B ha consegnato":

```markdown
### Cosa P3.5-A ha consegnato (scaled benchmark paper-ready)

- `R/eval-stats.R`: `wilson_ci()`, `mcnemar_paired()`, `bootstrap_delta_ci()`, `holm_adjust()` — statistica inferenziale paper-grade (tutti export).
- `R/p35a-select-gse.R`: `load_rummageo_index()`, `keyword_design_kind_proxy()`, `intersect_with_xlsx_and_archs4()`, `stratified_sample_gse()` — pipeline selezione GSE da pool RummaGEO inter xlsx.
- `R/p35a-investigate-gse145941.R`: `reclassify_verbose()`, `compare_with_gold()` — investigation chain-of-thought GSE145941.
- `inst/extdata/p35a-gse-selected.csv`: 100 GSE finali con design_kind_proxy (riproducibilita' set test).
- `analysis/_targets.R` esteso con ~15 nuovi target P3.5-A (suffisso `_p35a`).
- `analysis/eval/p35a-benchmark.html`: report Quarto 5 sezioni paper-ready.

### Risultati P3.5-A benchmark (100 GSE, ~XXXX sample)

[Compila dopo Task 10 con i numeri concreti.]

| Metrica | Valore |
|---|---|
| Stage 2 binary accuracy globale | **XX.X%** (Wilson CI [a, b]) |
| RummaGEO accuracy globale | YY.Y% (Wilson CI [c, d]) |
| Delta simulomicsr - RummaGEO (bootstrap CI) | +Z.Z pp (95% CI [e, f]) |
| McNemar p-value globale | p = 0.XXXX |
| McNemar p-value su factorial (Holm-corrected) | p = 0.XXXX |
| GSE145941 investigation diagnosi | <gold_xlsx_error / llm_inference_error / metadata_mismatch / ambiguous_design> |
| Costo cumulativo P1+P2+P3+P3.5-B+P3.5-A | ~$15-22 |
```

Aggiorna sezione "Next step (per la prossima sessione)":

```markdown
## Next step (per la prossima sessione — P4 server-switch o P3.5-A2 prompt iter)

**P3.5-A completato (2026-MM-DD).** Decisione condizionale sulle metriche:

### Se accuracy globale >= 80% e McNemar p < 0.05 vs RummaGEO

Procedi a **P4 — server-switch + run massivo ARCHS4**. Vedi ADR-0005 per la procedura. Trigger raggiunto: paper-ready evidence base ottenuta su 100 GSE indipendenti.

### Se accuracy globale < 80% o McNemar non significativo

Apri **P3.5-A2** (prompt iter): brainstorm + plan dedicato per re-tuning prompt Stadio 2 sulla base dei failure mode emersi. Riusa l'eval pipeline P3.5-A per regression testing post-fix.

### Roadmap aggiornata

1. P4 server-switch (ADR-0005) o P3.5-A2 prompt iter (decisione post-numbers).
2. Migrazione a `ellmer` come ADR separato pre-P4 (multi-provider + batch API ergonomic).
3. Rename pacchetto (ADR-0003) prima del primo `install_github` pubblico.
4. P5 — Stadio 4+5: DE per-studio (DESeq2/limma) + meta-analisi REM (`metafor`) — Output 3 ADR-0006.
```

- [ ] **Step 12.5: Commit CLAUDE.md**

```bash
git add CLAUDE.md
git commit -m "P3.5a chiusura: aggiorna CLAUDE.md Stato corrente con risultati benchmark + tag"
```

- [ ] **Step 12.6: Pulisci branch locale**

```bash
git branch -d p3.5a-eval
```

Expected: branch eliminato (merge confermato).

- [ ] **Step 12.7: Verifica finale**

```bash
git log --oneline -15
git tag --list 'p3.5*'
git status
```

Expected:
- Top commit: "P3.5a chiusura: aggiorna CLAUDE.md..."
- Tag `p3.5a-eval-complete` presente.
- Working tree pulito (eccetto `?? renv/settings.json` ignored).

---

## Self-review checklist (post-stesura)

Spec coverage rispetto a `2026-05-02-p3.5a-scaled-benchmark-design.md`:

- [x] §1 Obiettivo paper-ready → coperto da Task 1-12 end-to-end
- [x] §3 Architettura layer eval esteso → Task 6-10
- [x] §4 Componenti file → tabella File Structure ↑
- [x] §5.1 Pool candidato (RummaGEO ∩ xlsx ∩ ARCHS4) → Task 4
- [x] §5.2 Keyword proxy → Task 4 con tabella regex completa
- [x] §5.3 Stratified sampling distribuzione 15/15/10/20/15/15/10 → Task 5
- [x] §5.4 Output `inst/extdata/p35a-gse-selected.csv` committato → Task 5.7
- [x] §6 Investigation GSE145941 livello B → Task 8
- [x] §7.1 Wilson CI → Task 2 + Sezione 1, 4 del report
- [x] §7.2 McNemar paired + Holm → Task 2 + Sezione 4
- [x] §7.2 Bootstrap CI delta → Task 2 + Sezione 4
- [x] §7.3 Soglie di success globale ≥ 80%, binari ≥ 85% → Sanity check Task 9.3 + Task 10.4
- [x] §8 Quarto 5 sezioni → Task 10
- [x] §9 Error handling → catch nei target (`Filter(Negate(is.null), ...)`, `tryCatch(simulomicsr_rummageo_unavailable, ...)` riusato da P3.5-B)
- [x] §10 Testing → test files creati Task 2, 3, 4, 5, 8
- [x] §13 Done criteria → Task 11 + 12

Placeholder scan: nessun "TBD/TODO" nei task; due placeholder `[Da popolare dopo Task 10 con i numeri concreti]` in NEWS.md/CLAUDE.md sono ESPLICITAMENTE marker di run output da popolare a runtime, non plan failure.

Type consistency check:
- `wilson_ci()` ritorna `list(estimate, lower, upper, n)` — usato uguale in Sezione 1, 4
- `mcnemar_paired()` ritorna `list(statistic, p_value, b, c, n)` — usato uguale in Sezione 4
- `bootstrap_delta_ci()` ritorna `list(delta, lower, upper, n_iter)` — usato uguale in Sezione 4
- `keyword_design_kind_proxy()` vectorized, sempre character vector
- `stratified_sample_gse()` ritorna sottinsieme del `pool` con stesse colonne
- `reclassify_verbose()` ritorna design object con `.reasoning` aggiunto
- `compare_with_gold()` ritorna tibble con 7 colonne fisse

Tutto consistent.

---

## Riferimenti

- Spec P3.5-A: `docs/superpowers/specs/2026-05-02-p3.5a-scaled-benchmark-design.md`
- Spec P3.5-B: `docs/superpowers/specs/2026-05-02-p3.5-eval-benchmark-design.md`
- Plan P3.5-B: `docs/superpowers/plans/2026-05-02-p3.5b-eval-benchmark-plan.md` (template di stile)
- ADR-0006: `docs/decisions/0006-stato-arte-vs-simulomicsr.md`
- Spec classificatore v5: `docs/superpowers/specs/2026-04-29-classificatore-llm-design.md`
- Report P3.5-B: `analysis/eval/p35-benchmark.html` (numeri di reference 75.1% globale)
