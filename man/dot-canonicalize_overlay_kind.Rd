% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage3-name-cleanup-overlay.R
\name{.canonicalize_overlay_kind}
\alias{.canonicalize_overlay_kind}
\title{Canonicalizza il \code{new_kind} GREZZO di Mistral a un kind-anchor (o lo scarta).}
\usage{
.canonicalize_overlay_kind(kind)
}
\arguments{
\item{kind}{character(1) il \code{new_kind} grezzo (puo' essere \code{NA}/\code{""}).}
}
\value{
character(1): la forma canonica, \code{NA_character_} se scartato, o il
valore originale se gia' valido / non-(b).
}
\description{
Mistral emette \code{new_kind} in vocabolario libero (\code{cytokine}, \code{pathogen},
\code{genetic_mutation}, ...), non nell'enum canonico. Il branch (b) di
\code{.extract_anchor_segments} sovrascrive \code{kind_effective} SOLO se il kind
inizia per \code{genetic_} o e' in \code{.biological_override_kinds}
(\code{cytokine_stim}, \code{pathogen_or_aggregate_exposure}). Questo helper:
\itemize{
\item traduce i biologici grezzi alla forma canonica (\code{cytokine} ->
\code{cytokine_stim}, \code{pathogen} -> \code{pathogen_or_aggregate_exposure}) cosi'
(b) scatta e i frammenti si fondono;
\item scarta (\code{NA}) i \verb{genetic_*} NON in \code{.OVERLAY_ANCHOR_GENETIC_KINDS}
(es. \code{genetic_variant}/\code{genetic_mutation}): (b) non scatta -> resta il
kind deterministico (il consumer ha il guard \code{!is.na(recovery$kind)});
\item lascia invariato tutto il resto (kind gia' anchor-validi, e i kind
non-(b) come \code{disease}/\code{small_molecule}/\verb{gene_*}, che non toccano (b) e
per cui il kind deterministico governa comunque).
}
NB: NON usare \code{.canonicalize_resolver_kind} (mapperebbe \verb{genetic_*} ->
\code{genetic_perturbation}, che NON e' un kind-anchor -> reintrodurrebbe il
problema).
}
\keyword{internal}
