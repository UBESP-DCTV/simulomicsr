% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage4-loo-influence.R
\name{compute_pooling_influence}
\alias{compute_pooling_influence}
\title{Di quanto si sposta una meta-analisi: le tre statistiche dell'influenza}
\usage{
compute_pooling_influence(pieno, ridotto, geni, top_n = 30L)
}
\arguments{
\item{pieno}{data.frame poolato completo (output di
\code{pool_cluster_leaving_out} senza esclusioni).}

\item{ridotto}{data.frame poolato dopo la rimozione.}

\item{geni}{character: l'insieme di geni FISSO su cui misurare (di norma i
significativi del pieno). Dichiararlo prima e' un vincolo, non un dettaglio.}

\item{top_n}{quanti geni, in cima al pieno, per il massimo scarto di logFC
(default 30, come le tabelle del Layer B).}
}
\value{
data.frame di una riga: \code{n_geni_richiesti}, \code{n_geni_confrontati},
\code{n_geni_spariti}, \code{spearman}, \code{n_sig_pieno}, \code{n_sig_ridotto},
\code{n_sig_persi}, \code{n_sig_guadagnati}, \code{max_abs_delta_logFC}, \code{gene_max_delta},
\code{k_pieno}, \code{k_ridotto}.
}
\description{
Un solo numero non basta: sullo stesso gruppo si osservano Spearman ~ 0,99 e
contemporaneamente scarti di 2-3 unita' di logFC sui primi geni. Si riportano
tutte e tre:
\itemize{
\item \strong{Spearman} del ranking dei geni — se cambia \emph{quali} geni si mostrano;
\item \strong{geni significativi persi/guadagnati} — se cambia la \emph{quantita'} di
risultato;
\item \strong{massimo |delta logFC| fra i primi N} — se cambia la \emph{grandezza} di
un effetto dichiarato.
}
}
\details{
Il ranking usa \code{p_value_pool} e non \code{FDR_BH_within_cluster}: BH e' monotona in
p, quindi l'ordine e' lo stesso, ma l'FDR si ricalcola su un denominatore
diverso a ogni rimozione e ne farebbe una misura del denominatore. Il conteggio
dei significativi, che l'FDR lo usa davvero, resta su \code{FDR_BH_within_cluster}.

I primi N si fissano sul pooling PIENO: riordinare anche il ridotto
misurerebbe un insieme di geni diverso a ogni rimozione.
}
