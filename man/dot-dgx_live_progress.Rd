% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgx-utils.R
\name{.dgx_live_progress}
\alias{.dgx_live_progress}
\title{Live progress di un run P4 (mid-run reale)}
\usage{
.dgx_live_progress(cfg, run_id)
}
\arguments{
\item{cfg}{\code{simulomicsr_dgx_config}.}

\item{run_id}{stringa run id.}
}
\value{
list con \code{records_done} (intero), \code{per_worker} (data.frame
con colonne worker_id, n_records), \code{last_modified} (POSIXct UTC,
timestamp dell'ultimo file modificato) o NULL.
}
\description{
Conta via SSH le righe dei file \code{predictions.worker_*.jsonl} nella run
dir remota e restituisce il progress aggregato + per-worker. Usato come
fallback affidabile quando \code{status.json} e' fermo a \code{state="starting"}
(run_p4_vllm.py aggiorna status.json solo a inizio/fine, non per
microbatch). Se nessun file \code{predictions.worker_*.jsonl} esiste ancora
(fase boot pre-prima-microbatch), ritorna NULL.
}
\keyword{internal}
