% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage3-suspect-triage.R
\name{.build_suspect_triage}
\alias{.build_suspect_triage}
\title{Triage sospetti/canary/skip da \code{clusters.rds} (v9-pre re-cluster, Fase C).}
\usage{
.build_suspect_triage(clusters, min_k = 1L)
}
\arguments{
\item{clusters}{tibble \code{clusters.rds}. Richiede le colonne \verb{cluster_id, anchor_key, kind_effective_resolved, canonical_name, agent_id_resolved, k, n_studies, kind_confidence, kind_chebi_zero_roles}; le altre colonne
sono ignorate (robusto a schema piu' ampio).}

\item{min_k}{integer(1), soglia minima di \code{k} per includere un cluster nel
triage (default \code{1L} = nessun filtro). Cluster con \code{k < min_k} sono
esclusi del tutto (non compaiono in output, nemmeno come skip).}
}
\value{
tibble \verb{cluster_id, name, kind, k, n_studies, homog, top_theme, name_ok, cls, role}. \code{name} = \code{canonical_name} ripulito dal markup
(\code{\link{.strip_name_markup}}). \code{homog} = \code{NA_character_} (placeholder
di compatibilita' schema: la coerenza-membri richiede H5, non disponibile
qui). \code{name_ok} = \code{TRUE} solo sui cluster classificati canary.
}
\description{
Classifica ogni cluster in candidato alla pulizia-nomi Mistral
(\code{role = "candidate"}), canary (nome gia' noto-buono, controllo di
non-regressione, \code{role = "canary"}) o skip (non materia di pulizia-nomi,
\code{role = NA}). Opera SOLO sulle colonne di \code{clusters} passate in input —
NESSUN accesso H5 (il triage e' pre-recluster; "portata generosa" per
design: include anche frammenti piccoli con nome-spazzatura, meglio un
falso sospetto in piu' — costa un giro Mistral in piu', precision-gated a
valle — che un vero mal-nominato lasciato fuori).
}
\details{
\strong{Classificazione} (in ordine di precedenza):
\enumerate{
\item \strong{skip per kind} (precede TUTTO): \code{kind_effective_resolved} in
\code{c("vehicle_only", "none")} non e' mai una perturbazione da
nome-pulire, a prescindere dagli altri segnali.
\item \strong{sospetto} (batte canary se entrambi scatterebbero): \code{agent_id_resolved}
e' \code{NA}, inizia per \code{"STR:"} o \code{"UNK"}; oppure \code{kind_chebi_zero_roles};
oppure \code{kind_confidence} in \code{c("NONE", "WEAK")}; oppure il nome e'
"garbage/formale" (\code{\link{.is_garbage_name}}, valutata sul
\code{canonical_name} GREZZO — vedi nota su markup HTML li').
\item \strong{canary}: non sospetto, \code{agent_id_resolved} ha un prefisso ontologico
forte (\code{HGNC|CHEBI|NCBITaxon|CHEMBL|MeSH}), \code{kind_confidence == "STRONG"}
e \code{k >= 3}.
\item \strong{skip} (default): tutto il resto (es. nome forte ma \code{k < 3}: non
abbastanza studi per fidarsi come canary, ma nemmeno un segnale di
sospetto — resta fuori dal batch).
}

Mantiene lo SCHEMA CSV legacy atteso da \code{.load_name_cleanup_candidates()}
(\verb{cluster_id,name,kind,k,n_studies,homog,top_theme,name_ok,cls}),
incluso il vocabolario di \code{cls} (\code{OMOGENEO+ben_nominato},
\code{OMOGENEO+MAL_nominato}, \code{vehicle/none}), cosi' che \code{run_name_cleanup()}
(INVARIATA) la consumi senza modifiche e un round-trip CSV attraverso
\code{.load_name_cleanup_candidates()} riproduca lo stesso \code{role} calcolato qui
— e' il meccanismo di sicurezza dei canary (validato in T13).
}
\keyword{internal}
