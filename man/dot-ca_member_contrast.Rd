% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage3-contrast-anchor.R
\name{.ca_member_contrast}
\alias{.ca_member_contrast}
\title{Verdetto di contrasto per un singolo membro (un confronto trattato-vs-controllo)}
\usage{
.ca_member_contrast(
  treated_label,
  control_label,
  treated_fl,
  control_fl,
  anchor_name = NA_character_,
  anchor_id = NA_character_,
  ontology_env = NULL,
  caches = NULL
)
}
\arguments{
\item{treated_label, control_label}{etichette leggibili dei due bracci}

\item{treated_fl, control_fl}{factor_levels dei due bracci (lista o stringa)}

\item{anchor_name, anchor_id}{nome canonico e ID gia' risolti nell'anchor del
campione trattato di QUESTO record (ramo on-contrast). \code{NA} = ramo
disattivato. Nel build il nome del CLUSTER non esiste ancora: l'analogo
disponibile e' l'anchor del record (decisione utente 2026-07-27).}

\item{ontology_env}{environment dei dizionari}

\item{caches}{list opzionale di environment di memoizzazione
(\code{agent}, \code{token})}
}
\value{
list con \code{entity}, \code{direction}, \code{control_key},
\code{contrast_class}, \code{entity_source}, \code{drop_reason}.
\code{drop_reason == ""} = membro tenuto.
}
\description{
Applica, NELL'ORDINE misurato dal gate v11
(\code{analysis/audit/2026-07-24-anchor-coherence-sim/109-fase1-v11-gate.R}),
le regole del gate (\code{.cg_*}) e dell'appaiamento della riga
(\code{.rp_row_defect}), e restituisce l'identita' del contrasto oppure la
ragione dello scarto. L'ordine non e' arbitrario: cambiarlo cambia le ragioni
di scarto e quindi i conteggi, anche a parita' di esito.
}
\keyword{internal}
