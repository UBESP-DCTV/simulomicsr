% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage4-pooling-effectiveness.R
\name{compute_pooling_effectiveness}
\alias{compute_pooling_effectiveness}
\title{Efficacia del pooling per cluster: quanti studi contano davvero}
\usage{
compute_pooling_effectiveness(per_arm, tau2, soglia_dominanza = 0.5)
}
\arguments{
\item{per_arm}{data.frame con \code{cluster_id}, \code{gene_id}, \code{study_id}, \code{SE}
(una riga per braccio: il collasso per studio lo fa questa funzione).}

\item{tau2}{data.frame con \code{cluster_id}, \code{gene_id}, \code{tau2}. Passare qui il
sottoinsieme dei geni su cui si vuole la misura (di norma i significativi).}

\item{soglia_dominanza}{quota di peso del primo studio oltre la quale il
cluster e' marcato \code{dominato} (default 0,5).}
}
\value{
data.frame, una riga per cluster: \code{cluster_id}, \code{k_studies} (studi
distinti, mediana sui geni), \code{k_kish}, \code{quota_top1}, \code{frazione_efficace}
(\code{k_kish / k_studies}), \code{dominato}, \code{studio_dominante} (lo studio con la
quota mediana piu' alta), \code{n_geni}.
}
\description{
Per ogni gene si calcolano i pesi del random-effects \code{w = 1/(SE_studio^2 + tau^2)} e da questi il \strong{numero efficace di studi} di Kish
\eqn{(\sum w)^2 / \sum w^2}: vale \code{k} se tutti pesano uguale, tende a 1 se
uno domina. Si aggrega per cluster con la MEDIANA sui geni (robusta alle
code), non con la media.
}
\details{
Un gene presente nei pesi ma privo di \code{tau2} viene \strong{escluso}, non trattato
come \code{tau2 = 0}: assumere zero abbasserebbe artificialmente il numero
efficace, cioe' spingerebbe verso la conclusione piu' allarmante. Le esclusioni
si leggono in \code{n_geni}.
}
