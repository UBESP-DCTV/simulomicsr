% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm-client-openrouter.R
\name{.openrouter_build_request}
\alias{.openrouter_build_request}
\title{Costruisce (senza inviare) la \code{httr2} request per OpenRouter chat.
OpenRouter e' OpenAI-compatible: stessa shape di body, ma response_format
usa "json_object" (universale, supportato da tutti i modelli backend) invece
di "json_schema" strict (supportato solo da OpenAI). Lo schema viene incluso
nel system prompt come istruzione testuale e validato client-side.}
\usage{
.openrouter_build_request(
  model,
  messages,
  response_schema,
  schema_name,
  temperature = NULL,
  max_tokens = NULL,
  api_key = NULL
)
}
\description{
Costruisce (senza inviare) la \code{httr2} request per OpenRouter chat.
OpenRouter e' OpenAI-compatible: stessa shape di body, ma response_format
usa "json_object" (universale, supportato da tutti i modelli backend) invece
di "json_schema" strict (supportato solo da OpenAI). Lo schema viene incluso
nel system prompt come istruzione testuale e validato client-side.
}
\keyword{internal}
