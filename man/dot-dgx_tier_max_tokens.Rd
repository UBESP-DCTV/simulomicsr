% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgx-utils.R
\name{.dgx_tier_max_tokens}
\alias{.dgx_tier_max_tokens}
\title{Tier mapping size-based per \code{max_tokens} per-record}
\usage{
.dgx_tier_max_tokens(input_bytes)
}
\arguments{
\item{input_bytes}{intero (numero di byte della riga JSONL).}
}
\value{
list con \code{max_tokens} (intero) e \code{tier} (character "S","M","L","XL").
}
\description{
Ritorna il \code{max_tokens} appropriato per un record stage2 in base alla
dimensione in byte della sua riga JSONL (input record). Le soglie sono
calibrate sull'osservazione α stage2 cs25 (2026-05-09): 90.6\% pass a
4096, 9.4\% truncation a 4096 (di cui 95.5\% recovery a 8192), 0.4\%
truncation a 8192 (recovery parziale a 32768). Tier mapping mira a
coprire ~99\% dei record in single pass evitando rescue cycle.
}
\details{
Tier:
\itemize{
\item \code{S}  — input < 15 KB → max_tokens = 4096  (~70\% atteso, 90\%+ valid)
\item \code{M}  — 15 ≤ input < 25 KB → max_tokens = 8192  (~20\% atteso, 95\%+ valid)
\item \code{L}  — 25 ≤ input < 35 KB → max_tokens = 16384 (~8\% atteso)
\item \code{XL} — input ≥ 35 KB → max_tokens = 32768 (~2\% atteso)
}

Soglie hard-coded nel codice (non parametrizzate) — se servono altri
mapping in futuro creare un secondo helper o estendere con \code{breakpoints}
arg. Mantenuto semplice per chiarezza ADR-0011 (da scrivere).
}
\keyword{internal}
