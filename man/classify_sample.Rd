% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm-stage1.R
\name{classify_sample}
\alias{classify_sample}
\title{Classifica un sample RNAseq nello Stadio 1 (sample_facts schema v3)}
\usage{
classify_sample(
  sample_string,
  geo_accession,
  series_id,
  provider = "openai",
  model = "gpt-5.5",
  cache = NULL,
  organism_hint = NULL,
  ...
)
}
\arguments{
\item{sample_string}{stringa di metadati GEO concatenati (input)}

\item{geo_accession}{GSM id}

\item{series_id}{GSE id}

\item{provider}{\code{"openai"} (default) o \code{"mock"} (test)}

\item{model}{nome del modello (default \code{"gpt-5.5"} — spec §5.3.1 dev set)}

\item{cache}{oggetto \code{cache} (\code{cache_init()}), oppure \code{NULL} per bypass}

\item{organism_hint}{hint opzionale per lo user message}

\item{...}{inoltrato all'adapter (es. \code{temperature}, \code{max_tokens},
\code{.mock_adapter} per test)}
}
\value{
lista come da \code{llm_call_structured()}: \code{value} (sample_fact post
enrichment), \code{provider}, \code{model}, \code{validated}, \code{cache_hit}, \code{raw_response}.
}
\description{
Orchestra: build_prompt_stage1 -> llm_call_structured (con cache + Structured
Outputs strict) -> parse_stage1_response (enrichment deterministico).
}
