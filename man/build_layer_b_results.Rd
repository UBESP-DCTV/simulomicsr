% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer-b-build.R
\name{build_layer_b_results}
\alias{build_layer_b_results}
\title{Build Layer B results (orchestrator)}
\usage{
build_layer_b_results(
  stage4_dir,
  selection,
  counts_cache_manifest,
  per_cluster_samples_provider,
  stage3_metadata = NULL,
  config = layer_b_default_config(),
  out_dir = NULL,
  h5_path = NULL
)
}
\arguments{
\item{stage4_dir}{character path al dir Layer A output.}

\item{selection}{character path-to-CSV o data.frame.}

\item{counts_cache_manifest}{list nested \code{cache[[cluster_id]][[study_id]] = path}.
Tipicamente derivato dal counts cache di Stage 4.}

\item{per_cluster_samples_provider}{function(cluster_id) -> tibble
\verb{sample_id, study_id, treatment} per il cluster (assemblata upstream
dal join Stage 3 assignment + Stage 2 design_role).}

\item{stage3_metadata}{tibble opzionale con \verb{cluster_id, kind_effective, agent_id, tissue, safety_min} (per summary card). NULL -> summary card
senza Stage 3 fields.}

\item{config}{list (vedi \code{\link[=layer_b_default_config]{layer_b_default_config()}}).}

\item{out_dir}{character path output dir. Se NULL, genera dir versionata
in \verb{analysis/p4-output/<ts>-layer-b-<run_id>/}.}

\item{h5_path}{character path al H5 ARCHS4 (per re-fetch counts in caso
di cache miss; NULL = nessun fetch fallback).}
}
\value{
Oggetto S3 \code{layer_b_result} (list con \code{cluster_bundles},
\code{selection_resolved}, \code{run_metadata}, \code{dir}).
}
\description{
Entry-point pubblico. Esegue i 3 sub-stage Layer B:
B.1 Loader+QC (validate selection + fetch_layer_a_subset),
B.2 Asset generation (per cluster: 8 plot conditional + summary card +
narrative template),
B.3 (delegato a \code{render_layer_b_report()} chiamato dal caller).
}
