% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer-b-plot-corpus-overview.R
\name{.corpus_overview_from_bundle}
\alias{.corpus_overview_from_bundle}
\title{"Il corpus" letto dal bundle: la figura se il deliverable annotato e'
raggiungibile, altrimenti il perche' non lo e'}
\usage{
.corpus_overview_from_bundle(lb, out_dir = lb$dir)
}
\arguments{
\item{lb}{oggetto \code{layer_b_result} (output di \code{\link[=load_layer_b]{load_layer_b()}}).}

\item{out_dir}{character path dove scrivere \code{corpus_overview.png}/\code{.svg}
quando la figura viene generata. Default \code{lb$dir} (la cartella del
bundle: stessa convenzione dei path relativi usata dal template per le
immagini per-cluster).}
}
\value{
list con \code{disponibile} (\code{logical(1)}) e, quando \code{TRUE}, \code{plot}
(l'output di \code{\link[=.build_corpus_overview]{.build_corpus_overview()}}); quando \code{FALSE}, \code{motivo}
(\code{character(1)}) e \code{plot = NULL}.
}
\description{
Punto di ingresso usato dal template Quarto (\code{inst/templates/layer-b-report.qmd}).
Il deliverable annotato (\code{deliverable-annotato.rds}) NON e' copiato dentro
il bundle Layer B -- vive nella directory dello Stadio 4 -- ma il suo path
e' registrato in \code{run_metadata.json$input_files$deliverable_annotato}
(collegato al build dal Task 7bis). Questa funzione lo rilegge AL MOMENTO
DEL RENDER: se il file e' li' e ha le colonne giuste, produce la figura a
tre pannelli con \code{\link[=.build_corpus_overview]{.build_corpus_overview()}}; altrimenti dichiara il motivo
invece di lasciare un buco silenzioso o far fallire il render.
}
\details{
Nessun taglio silenzioso: OGNI ramo che porta a \code{disponibile = FALSE} mette
una frase in \code{motivo} -- chiave assente da \code{run_metadata} (bundle
costruito prima del Task 7bis), file non trovato (spostato, disco esterno
non montato, mai stato disponibile al build), file illeggibile, o colonne
mancanti.
}
\keyword{internal}
