% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage3-metadata.R
\name{.build_biosample_lookup}
\alias{.build_biosample_lookup}
\title{Pre-build named character vector lookup geo_accession -> BioSample SAMN}
\usage{
.build_biosample_lookup(archs4_metadata)
}
\arguments{
\item{archs4_metadata}{tibble/data.frame con colonne \code{sample_id} e
\code{biosample_id}. Tollerato \code{NULL} (caller passa NULL quando metadata
non disponibile -> fallback senza dedupe).}
}
\value{
named character vector (\code{names = sample_id}, \code{values = biosample_id} con possibili NA) oppure NULL se input incompatibile.
NA preservato by-design: il chiamante in \code{.enrich_cluster_metadata()}
tratta NA come "identita' biologica ignota -> sample distinto".
}
\description{
Implementa il lookup primario per il dedupe cross-studio (FASE E0,
ADR-0019 D9). Costruito UNA volta in \code{.summarize_clusters()} e passato
come \code{biosample_lookup} a \code{.enrich_cluster_metadata()}. Costo per
lookup: O(1) named-vector subscripting.
}
\details{
La fonte canonica della colonna \code{biosample_id} e' lo schema esteso di
\code{load_archs4_metadata()} (P5 audit RED_ALERT C3 + E0) che legge
\code{meta/samples/relation} da H5 e applica \code{parse_biosample_id()}.
}
\keyword{internal}
