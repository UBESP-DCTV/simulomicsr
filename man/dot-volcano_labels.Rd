% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer-b-plot-volcano.R
\name{.volcano_labels}
\alias{.volcano_labels}
\title{Etichette del volcano: top-N per |logFC| x -log10(FDR), un simbolo una volta}
\usage{
.volcano_labels(cp, config)
}
\arguments{
\item{cp}{data.frame poolato del cluster, con \code{is_sig} gia' calcolata.}

\item{config}{list di configurazione Layer B.}
}
\value{
character della stessa lunghezza di \code{nrow(cp)}: l'etichetta dove va
mostrata, \code{NA} altrove.
}
\description{
Nella regione MHC lo stesso simbolo ha piu' ID Ensembl su aplotipi
alternativi (\code{UBD} ne ha sei nel gruppo SARS-CoV-2, tre con valori identici):
senza deduplica la stessa etichetta occupava piu' posti fra i top-N. Tabella
e heatmap deduplicavano gia' via \code{.rank_and_dedup_genes()}; qui si chiude il
terzo pannello, cosi' la stessa figura non racconta due cose diverse.
}
\details{
I geni \strong{senza} simbolo restano etichettati col proprio \code{gene_id} e non
vengono fusi fra loro: sono geni diversi, e collassarli sarebbe peggio del
problema che si sta risolvendo.
}
\keyword{internal}
