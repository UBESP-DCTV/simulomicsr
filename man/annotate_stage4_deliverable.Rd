% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage4-deliverable-annotation.R
\name{annotate_stage4_deliverable}
\alias{annotate_stage4_deliverable}
\title{Annota il deliverable dello Stadio 4}
\usage{
annotate_stage4_deliverable(
  cluster_pooled,
  per_study_de,
  cluster_meta,
  member_labels = NULL,
  coherence_verdicts = NULL,
  coherence_source = NA_character_,
  fdr_threshold = 0.05,
  entity_env = NULL
)
}
\arguments{
\item{cluster_pooled}{data.frame/tibble delle righe poolate (\code{cluster_id},
\code{gene_id}, \code{k_effective}, \code{FDR_BH_within_cluster}, \code{I2}, \code{tau2}).}

\item{per_study_de}{data.frame per-braccio (\code{cluster_id}, \code{gene_id},
\code{study_id}, \code{SE}). Il collasso dei bracci per studio lo fa
\code{\link[=compute_pooling_effectiveness]{compute_pooling_effectiveness()}}.}

\item{cluster_meta}{data.frame con \code{cluster_id}, \code{contrast_entity},
\code{contrast_direction}, \code{contrast_control_key} e (opzionale)
\code{canonical_name}. \strong{Deve coprire tutti i cluster poolati}: un gruppo senza
identita' del contrasto uscirebbe con entita' \code{NA} e nessuno se ne
accorgerebbe leggendo il CSV, quindi la funzione si ferma.}

\item{member_labels}{data.frame opzionale con \code{cluster_id}, \code{study_id},
\code{label} (una riga per braccio). Serve solo all'asse del materiale: se NULL,
quelle colonne escono \code{NA} \strong{dichiarate}, non indovinate. Puo' contenere
anche studi non poolati: la funzione li \strong{scarta da sola} tenendo le
coppie (cluster, studio) presenti in \code{per_study_de}.}

\item{coherence_verdicts}{data.frame opzionale con \code{ckey} e \code{motivo}, passato
a \code{.annotate_coherence()}. Un verdetto che non trova il suo gruppo \strong{ferma}
la funzione: lasciarlo cadere marcherebbe un gruppo incoerente come
coerente, cioe' il fallimento silenzioso a favore della conclusione comoda.}

\item{coherence_source}{character(1) provenienza dei verdetti (la marcatura e'
una lettura umana: la sua provenienza viaggia nel dato).}

\item{fdr_threshold}{soglia per \code{n_sig} e per i geni su cui si misura
l'efficacia (default 0,05).}

\item{entity_env}{ambiente dei dizionari ontologici; NULL = caricato al volo.}
}
\value{
data.frame, una riga per meta-analisi, ordinato per \code{k_effective}
decrescente e poi per \code{cluster_id} (ordine \strong{deterministico}: due
esecuzioni sullo stesso input devono dare lo stesso file).
}
\description{
Da una riga per gene-cluster a \strong{una riga per meta-analisi}, con le metriche
del pool, l'etichetta risolta dall'ID, il verdetto di coerenza e i due assi
misurati il 2026-07-31: quanto il pooling e' efficace e su che materiale.
}
\details{
Perche' i due assi nuovi: \code{k_effective} dice \strong{quanti} studi entrano e non
quanto \strong{contano}. Sui 191 gruppi di v13, 105 (55\%) hanno uno studio sopra il
50\% del peso e 66 (35\%) valgono meno di due studi efficaci — e \strong{tutti sono
marcati coerenti}, perche' coerenza e dominanza sono assi indipendenti.
}
