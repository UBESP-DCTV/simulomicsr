% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm-client.R
\name{llm_call_structured}
\alias{llm_call_structured}
\title{Chiama un LLM con output strutturato validato e cache opzionale}
\usage{
llm_call_structured(
  provider,
  model,
  messages,
  response_schema,
  cache = NULL,
  cache_namespace_version = "v0",
  ...,
  .mock_response = NULL,
  .mock_adapter = NULL
)
}
\arguments{
\item{provider}{stringa: \code{"openai"} o \code{"mock"} (per i test). Altri provider
in plan futuri.}

\item{model}{nome del modello (es. \code{"gpt-5.4-mini"})}

\item{messages}{lista di messaggi nello schema OpenAI (\code{role} + \code{content})}

\item{response_schema}{path a un file JSON Schema; la risposta dell'LLM
viene validata contro questo schema}

\item{cache}{oggetto ritornato da \code{cache_init()}, o \code{NULL} per bypass}

\item{cache_namespace_version}{stringa che entra nella cache key (es.
\code{"stage1.v3"}); cambiare questo invalida la cache}

\item{...}{parametri provider-specifici inoltrati all'adapter}

\item{.mock_response}{(test only) risposta da iniettare se \code{provider="mock"}}

\item{.mock_adapter}{(test only) function da chiamare al posto dell'adapter
reale; ha precedenza su \code{.mock_response}}
}
\value{
lista con: \code{value} (oggetto R parsed dal JSON), \code{provider}, \code{model},
\code{validated} (logico), \code{cache_hit} (logico), \code{raw_response} (lista grezza).
}
\description{
Punto d'ingresso pubblico del client LLM. Dispatcha sul provider corretto,
valida la risposta contro \code{response_schema}, e (se \code{cache} e' fornita)
serve dalla cache su hit.
}
