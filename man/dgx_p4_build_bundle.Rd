% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgx-bundle.R
\name{dgx_p4_build_bundle}
\alias{dgx_p4_build_bundle}
\title{Costruisce un bundle locale per un run P4 sulla DGX}
\usage{
dgx_p4_build_bundle(
  input_jsonl,
  stage,
  config,
  metadata = list(),
  bundle_dir_root = "analysis/p4-bundles"
)
}
\arguments{
\item{input_jsonl}{path locale a un file JSONL (una riga per record).
Per stage1: campi \code{record_id}, \code{geo_accession}, \code{series_id},
\code{string}.
Per stage2: campi \code{record_id}, \code{study_summary}, \code{samples}
(lista).}

\item{stage}{\code{"stage1"} o \code{"stage2"}.}

\item{config}{\code{simulomicsr_dgx_config} da \code{dgx_config()}.}

\item{metadata}{list opzionale con \code{slug} (default: stage), aggiunta al
manifest e usata per costruire il \code{run_id}.}

\item{bundle_dir_root}{directory parent in cui creare il bundle. Default
\code{"analysis/p4-bundles/"}.}
}
\value{
oggetto \code{simulomicsr_dgx_bundle} con campi \code{run_id},
\code{bundle_dir}, \code{stage}, \code{config}, \code{record_count}.
}
\description{
Un bundle e' una directory autocontenuta che raccoglie tutto cio' che
serve a un job SLURM remoto: manifest, input records, prompt template
(system message), schema JSON, parametri di generazione, status iniziale.
Verra' rsync-ato sul login node da \code{dgx_p4_submit()}.
}
