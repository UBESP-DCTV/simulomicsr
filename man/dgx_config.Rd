% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgx-config.R
\name{dgx_config}
\alias{dgx_config}
\title{Configurazione per i run P4 sulla DGX UniPD HPC}
\usage{
dgx_config(
  login_user = "u0044",
  login_host = "logindgx.hpc.ict.unipd.it",
  mail_user = "luca.vedovelli@unipd.it",
  partition = "dgx12cluster",
  account = "dctv_dgx",
  nodelist = "poddgx02",
  remote_root = NULL,
  ssh_key_path = NULL,
  ...
)
}
\arguments{
\item{login_user}{user SSH sul login node DGX. Default \code{"u0044"}.}

\item{login_host}{hostname login node. Default
\code{"logindgx.hpc.ict.unipd.it"}.}

\item{mail_user}{mail per notifiche SLURM (\verb{#SBATCH --mail-user}).
Default \code{"luca.vedovelli@unipd.it"}.}

\item{partition}{partizione SLURM. Default \code{"dgx12cluster"}.}

\item{account}{account SLURM. Default \code{"dctv_dgx"}.}

\item{nodelist}{nodelist SLURM. Default \code{"poddgx02"} (nodo dell'utente
nel pool \code{dgx12cluster}). Validato col job 19724 il 2026-05-07.
Pass \code{NULL} se vuoi che lo scheduler scelga automaticamente
(utile se poddgx02 e' DRAIN/DOWN; gli altri nodi del pool sono
poddgx01 e poddgx03).}

\item{remote_root}{root remoto del workspace P4. Default
\code{"/home/<login_user>/simulomicsr-dgx"}. NOTA: \verb{/home/<user>/} (NON
\verb{/mnt/home/<user>/}) perche' i compute node UniPD HPC non montano
\verb{/mnt/home/}. Verificato col probe job 19720 il 2026-05-07 su
poddgx03: \verb{/mnt/home/u0044/} non esiste lato compute.}

\item{ssh_key_path}{path opzionale a private key SSH. \code{NULL} significa
usa la default (id_rsa o ssh-agent).}

\item{...}{interno, intercetta argomenti non riconosciuti per
produrre un errore esplicito invece del default R "argument unused".}
}
\value{
oggetto \code{simulomicsr_dgx_config}.
}
\description{
Restituisce un oggetto \code{simulomicsr_dgx_config} con i parametri di accesso
al cluster (login, partizione SLURM, account, nodelist) e le path remote
usate da \code{dgx_p4_submit()} / \code{dgx_p4_collect()}. I default sono cuciti per
il setup attuale dell'utente (UniPD HPC, account \code{dctv_dgx}, nodelist
\code{poddgx02}, login user \code{u0044}); ogni campo e' pero' overridable.
}
