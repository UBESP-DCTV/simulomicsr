% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage2-normalize.R
\name{.reassemble_stage2_chunks}
\alias{.reassemble_stage2_chunks}
\title{Riassembla i chunk Stadio 2 in un record per studio}
\usage{
.reassemble_stage2_chunks(stage2_master)
}
\arguments{
\item{stage2_master}{list di study records (output di \code{.load_stage2_master}).}
}
\value{
\verb{list(stage2_master = <riassemblato>, report = list(n_records_in, n_records_out, n_series_multichunk))}.
}
\description{
Uno studio grande viene classificato dallo Stadio 2 in piu' chunk (cs50, per
il limite di contesto LLM): nel master compare come N record con la stessa
\code{series_id}. La pipeline a valle assume un record per studio (record_id =
\code{series__group}, indice Stadio 4 per \code{series_id} last-wins) -> di uno studio
chunked sopravvive solo l'ultimo chunk, gli altri campioni vengono persi o
misrisolti (vedi finding 2026-06-01). Questa funzione ripristina l'invariante:
fonde i record con la stessa \code{series_id} in UN record.
}
\details{
Strategia \strong{namespacing-per-chunk}, non union-per-id: sui dati reali 639
group_id omonimi tra chunk hanno \code{primary_role} in conflitto e 831
comparison_id riferiscono treated/control diversi. Unire per id li
corromperebbe. Invece ogni chunk mantiene i suoi gruppi/comparison con id resi
unici (suffisso \verb{#chunk<k>}); i riferimenti delle comparison vengono riscritti
coerentemente entro lo stesso chunk. Il merge biologico (due chunk dello stesso
gruppo) lo fa il clustering per-anchor a valle, non questa funzione.

Idempotente: applicata a un master gia' riassemblato (un record per series) e'
un no-op. Le series a chunk singolo restano invariate (nessun rename).
}
\keyword{internal}
