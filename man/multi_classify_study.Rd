% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/p35c-multi-classify.R
\name{multi_classify_study}
\alias{multi_classify_study}
\title{Classifica uno studio con piu' modelli LLM e ritorna i risultati comparabili}
\usage{
multi_classify_study(
  series_id,
  sample_facts_list,
  study_summary,
  model_specs,
  cache,
  ...,
  .mock_adapter_factory = NULL
)
}
\arguments{
\item{series_id}{GSE accession}

\item{sample_facts_list}{lista di sample_facts validati (stage1.v3)}

\item{study_summary}{list con title/summary/overall_design}

\item{model_specs}{lista di liste, ognuna con campi \code{provider}, \code{model},
\code{label} (slug univoco, usato come name di output), e opzionalmente
\code{max_tokens}.}

\item{cache}{cache object (puo' essere NULL nei test mockati)}

\item{...}{args extra passati a llm_call_structured (es. \code{.mock_response})}

\item{.mock_adapter_factory}{(test only) funzione che riceve \code{label} e
ritorna il \code{.mock_adapter} da iniettare per quel modello.}
}
\value{
list nominata per \code{label} (uno slot per modello), valore = output di
\code{classify_study()} (study_design valido oppure invalid_record).
}
\description{
Wrapper su \code{classify_study()} che itera sui \code{model_specs} e ritorna una lista
nominata per \code{label}. Stage 1 fissato (sample_facts_list passato uguale a tutti).
La cache LLM esistente partiziona automaticamente per \verb{(provider, model, messages)},
quindi modelli diversi non si sovrascrivono.
}
\details{
Gestione errori per-modello: se un modello solleva, il record diventa
\code{.invalid_reason='llm_call_failed'} (consistente con \code{classify_study()}).
I modelli che riescono restano disponibili nei loro slot.
}
