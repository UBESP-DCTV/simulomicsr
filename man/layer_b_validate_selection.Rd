% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer-b-selection.R
\name{layer_b_validate_selection}
\alias{layer_b_validate_selection}
\title{Valida selection Layer B contro Layer A output (pre-check pubblico)}
\usage{
layer_b_validate_selection(selection, stage4_dir, fdr_threshold = 0.05)
}
\arguments{
\item{selection}{character path-to-CSV o data.frame (passato a \link{.load_layer_b_selection}).}

\item{stage4_dir}{character path al dir output di Layer A (contiene \code{cluster_pooled.parquet}).}

\item{fdr_threshold}{numeric soglia per \code{n_sig_FDR05} (default 0.05).}
}
\value{
tibble con \verb{cluster_id, label_paper, priority, notes, exists_in_stage4, method, k_effective, n_sig_FDR05}.
}
\description{
Verifica che ogni \code{cluster_id} esista in \code{cluster_pooled.parquet} di Layer A
(fail-fast su typo o drift Stage 4 re-run). Restituisce tibble arricchita
con \code{exists_in_stage4}, \code{method}, \code{k_effective}, \code{n_sig_FDR05} per ogni
cluster --- usabile come smoke check pre-batch da R interactive.
}
\examples{
\dontrun{
val <- layer_b_validate_selection(
  "analysis/layer-b-selection.csv",
  "analysis/p4-output/20260523T032601Z-stage4-96c43acb"
)
print(val)
}
}
