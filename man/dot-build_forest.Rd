% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer-b-plot-forest.R
\name{.build_forest}
\alias{.build_forest}
\title{Forest plot top-N geni (REM + MEGA-AUG dispatch)}
\usage{
.build_forest(
  per_study_de_subset,
  cluster_pooled_subset,
  method,
  out_dir,
  config
)
}
\arguments{
\item{per_study_de_subset}{tibble subset per il cluster (puo' essere 0 rows per mega).}

\item{cluster_pooled_subset}{tibble subset per il cluster.}

\item{method}{character "rem", "rem_group", "mega", o "mega_aug".}

\item{out_dir}{character dir output.}

\item{config}{list config.}
}
\value{
list con \code{png_path} (NA se skip), \code{svg_path} (NA se skip o save_svg=FALSE),
\code{caption} (skip-explanation se applicable).
}
\description{
Per \code{method \%in\% c("rem", "rem_group")}: usa \code{metafor::forest()} su per-study yi/vi.
Per \code{method == "mega_aug"}: custom ggplot con 2 studi del pair + pooled diamond.
Per \code{method == "mega"}: skip-graceful con caption esplicativa (no per-study DE
nel Layer A output).
}
\keyword{internal}
