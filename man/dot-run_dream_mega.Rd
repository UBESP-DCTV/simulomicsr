% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage4-dream-mega.R
\name{.run_dream_mega}
\alias{.run_dream_mega}
\title{Run dream mixed-effect model per MEGA / MEGA-AUG cluster}
\usage{
.run_dream_mega(
  counts,
  metadata,
  cluster_id,
  workers = 1L,
  n_baseline_studies_augmented = NA_integer_,
  method_label = "mega"
)
}
\arguments{
\item{counts}{integer matrix (geni x sample) assembled cross-study.}

\item{metadata}{data.frame con (sample_id, study factor, treatment factor).
Treatment deve avere levels \code{c("control", "treated")}.}

\item{cluster_id}{string cluster_id per output column.}

\item{workers}{integer numero di worker BiocParallel (default 1L, serial).}

\item{n_baseline_studies_augmented}{integer NA per MEGA, n studi baseline
pool per MEGA-AUG.}

\item{method_label}{string label di metodo per la colonna \code{method}
(default \code{"mega"}; usato \code{"mega_aug"} dal chiamante MEGA-AUG).}
}
\value{
tibble gene-level con cluster_pooled.parquet schema:
\code{cluster_id}, \code{gene}, \code{method}, \code{logFC_pool},
\code{SE_pool}, \code{p_value_pool}, \code{tau2}, \code{I2}, \code{Q},
\code{Q_pval}, \code{k_effective}, \code{n_baseline_studies_augmented},
\code{FDR_BH_within_cluster}, \code{direction_applied}.
}
\description{
Modello: \code{~ treatment + (1|study)} su counts assembled cross-study.
Pipeline: DGEList -> filterByExpr -> normLibSizes(TMM) ->
voomWithDreamWeights -> dream -> eBayes -> per-gene extraction.
}
\details{
Fallback: se dream fallisce (rare convergence issues), retry con limma-voom
\itemize{
\item duplicateCorrelation(block=study) come modello surrogate dello stesso
contrasto, preservando la stessa interfaccia di output (logFC, SE, p).
}
}
\keyword{internal}
