% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer-b-utils.R
\name{.rank_and_dedup_genes}
\alias{.rank_and_dedup_genes}
\title{Ordina i geni per significativita e deduplica per simbolo genico}
\usage{
.rank_and_dedup_genes(sig)
}
\arguments{
\item{sig}{data.frame/tibble gia' filtrato (es. per \code{FDR<thr}), con
colonne \code{gene_symbol}, \code{logFC_pool}, \code{FDR_BH_within_cluster}.}
}
\value{
data.frame ordinato e deduplicato, stesso schema colonne di \code{sig}.
}
\description{
Revisione scientifica (showcase Layer B): ordinare per \verb{|logFC_pool|}
pesca geni ad alta varianza e bassa robustezza (es. recettori olfattivi,
cheratine, geni con \code{k_effective} basso) e seppellisce i marcatori
canonici veri (logFC modesto ma alta significativita e alto k). Criterio
corretto: \code{FDR_BH_within_cluster} ASCENDENTE (piu significativo prima),
tie-break \code{abs(logFC_pool)} DISCENDENTE.
}
\details{
Dedup: ARCHS4 mappa piu Ensembl gene_id sullo stesso simbolo HGNC
(artefatto multi-Ensembl, es. RDH13 x7, AGER x4) gonfiando le
tabelle/heatmap con righe ridondanti dello stesso gene biologico.
Collassa le righe che condividono lo stesso \code{gene_symbol} NON vuoto alla
singola riga PIU significativa (la prima dopo l'ordinamento, quindi
\code{FDR_BH_within_cluster} minimo). Righe con \code{gene_symbol} NA o \code{""}
(nessuna annotazione HGNC) NON vengono deduplicate tra loro: restano
righe distinte (sono gia' univoche per \code{gene_id}).
}
\keyword{internal}
