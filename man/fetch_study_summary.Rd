% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo-fetch.R
\name{fetch_study_summary}
\alias{fetch_study_summary}
\title{Recupera il summary di uno studio GEO (GSE)}
\usage{
fetch_study_summary(series_id, cache_dir = NULL)
}
\arguments{
\item{series_id}{GSE accession (es. "GSE41166"). Validato contro pattern
\verb{^GSE[0-9]+$}.}

\item{cache_dir}{Directory di cache (NULL = disattivata). Se esiste un
file \verb{<cache_dir>/<series_id>.json}, viene letto senza chiamata di rete.}
}
\value{
Lista con campi \code{series_id}, \code{title}, \code{summary}, \code{overall_design}.
\code{overall_design} puo' essere \code{NA_character_} se non parsabile
dall'XML EUtils (comportamento attuale -- vedi dettagli).
}
\description{
Wrapper su \code{rentrez::entrez_summary(db="gds", ...)} con cache filesystem
opzionale. Ritorna i campi title, summary, overall_design utilizzati
come input dello Stadio 2 (vedi spec sec.4 e sec.5.2).
}
\details{
La cache su disco (un JSON per GSE) e' raccomandata in produzione per
evitare hammering di NCBI EUtils (rate limit 3 req/sec senza API key).

\code{entrez_summary(db = "gds", ...)} non espone \code{overall_design} nel record
summary. Una seconda chiamata \code{entrez_fetch(db = "gds", rettype = "xml")}
sarebbe necessaria per estrarlo. Per ora questo campo viene restituito
come \code{NA_character_} -- lo Stadio 2 prompt funziona comunque perche'
la maggior parte dell'informazione e' nel campo \code{summary}.
}
