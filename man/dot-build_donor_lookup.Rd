% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage3-metadata.R
\name{.build_donor_lookup}
\alias{.build_donor_lookup}
\title{Pre-build named character vector lookup geo_accession -> donor_id (LLM)}
\usage{
.build_donor_lookup(stage1_master)
}
\arguments{
\item{stage1_master}{named list o environment indexed per GSM con
campo \verb{$donor$donor_id} (sample_facts.stage1.v3). NA o NULL accettati
come "donor info assente" -> NA nel lookup.}
}
\value{
named character vector (\code{names = GSM}, \code{values = donor_id} con
possibili NA) oppure NULL se input vuoto/incompatibile.
}
\description{
Implementa il sample-level donor lookup (FASE E0, fix paper-grade della
sotto-stima \code{n_distinct_donors} pre-E0 che leggeva donor dal SOLO primo
sample del gruppo, ignorando gli altri). Post-E0,
\code{.enrich_cluster_metadata()} itera tutti i \code{geo_accession} del cluster
e li mappa via questo lookup.
}
\details{
Accetta sia named list che environment (post Phase 1.5 di
\code{build_stage3_clusters()} lo stage1_master e' convertito a environment
per O(1) hash lookup).
}
\keyword{internal}
