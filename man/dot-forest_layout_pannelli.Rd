% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer-b-plot-forest.R
\name{.forest_layout_pannelli}
\alias{.forest_layout_pannelli}
\title{Layout del forest a due pannelli: altezze proporzionate, un tetto stampabile}
\usage{
.forest_layout_pannelli(n_top, n_bottom)
}
\arguments{
\item{n_top}{integer(1) righe (geni) nel pannello superiore.}

\item{n_bottom}{integer(1) righe (studi + "Pooled") nel pannello inferiore.}
}
\value{
list con \code{heights} (numeric(2), proporzioni per
\code{patchwork::plot_layout(heights=)}), \code{h_inch} (numeric(1), altezza totale
della figura in pollici, con tetto), \code{label_stride} (integer(1) >= 1:
1 = tutte le etichette mostrate, N = una ogni N righe).
}
\description{
Prima di questa funzione i due pannelli avevano SEMPRE lo stesso rapporto
2:1 (\code{patchwork::plot_layout(heights = c(2, 1))}) e l'altezza totale della
figura era una costante per riga moltiplicata per il conteggio, SENZA
TETTO. Su un cluster con 10 geni bersaglio e 59 studi (TGF-beta1, run v15,
misurato sul PNG reale il 2026-08-06) il file usciva 2400x6720 px: il
pannello superiore tutto spazio vuoto fra dieci righe, quello inferiore
con 59 nomi di studio compressi in caratteri illeggibili.
}
\details{
Qui le altezze passate a \code{patchwork} sono proporzionate al numero di righe
di ciascun pannello, e l'altezza totale cresce con \code{n_bottom} ma si ferma a
un tetto stampabile (\code{h_max}, una pagina intera). Quando il tetto comprime
lo spazio per studio sotto la soglia leggibile a \code{.lb_theme()}, le
etichette si DIRADANO invece di sovrapporsi in silenzio: il chiamante deve
riportare \code{label_stride} in didascalia quando e' maggiore di 1 (tutte le
stime restano disegnate, solo il testo dell'etichetta si dirada).
}
\keyword{internal}
