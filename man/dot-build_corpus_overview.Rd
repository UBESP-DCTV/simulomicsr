% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer-b-plot-corpus-overview.R
\name{.build_corpus_overview}
\alias{.build_corpus_overview}
\title{Tre pannelli: distribuzione di k_effective, I2_med, n_sig sulle meta-analisi}
\usage{
.build_corpus_overview(deliverable_annotato, out_dir, config)
}
\arguments{
\item{deliverable_annotato}{data.frame/tibble, una riga per meta-analisi,
con almeno le colonne \code{k_effective}, \code{I2_med}, \code{n_sig} (schema di
\code{annotate_stage4_deliverable()}). Le righe con \code{NA} in una colonna sono
escluse dal solo pannello di quella colonna (comportamento standard di
\code{ggplot2::geom_histogram()}, che avvisa e non fa crashare).}

\item{out_dir}{character path dove salvare \code{corpus_overview.png} (+
\code{corpus_overview.svg} se \code{config$save_svg}).}

\item{config}{list (vedi \code{\link[=layer_b_default_config]{layer_b_default_config()}}).}
}
\value{
list con \code{png_path}, \code{svg_path} (\code{NA_character_} se
\code{config$save_svg} e' \code{FALSE}), \code{caption}, \code{n_gruppi}.
}
\description{
Risponde a colpo d'occhio a "quanto e' grande questa cosa" (spec del
ridisegno, §3.2 "Il corpus"): tre istogrammi affiancati (via
\code{patchwork::wrap_plots()}) col tema condiviso \code{\link[=.lb_theme]{.lb_theme()}} e i colori di
\link{.LB_COLORI}, uno per ciascuna delle tre colonne che il deliverable
annotato porta gia' (\code{annotate_stage4_deliverable()}). La funzione legge
SEMPRE dal data.frame che riceve -- nessun numero e' cablato qui dentro:
alla prossima esecuzione i pannelli riflettono il deliverable vero, non
una fotografia ricopiata a mano.
}
\details{
L'asse dei geni significativi e' in scala log10 (dichiarato nel titolo del
pannello e nella didascalia, non un taglio silenzioso): la distribuzione e'
fortemente asimmetrica (da zero a centinaia di migliaia) e su scala lineare
schiaccerebbe quasi tutti i gruppi in una singola barra.
}
\keyword{internal}
