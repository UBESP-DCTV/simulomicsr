% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer-b-report.R
\name{render_layer_b_report}
\alias{render_layer_b_report}
\title{Render Quarto aggregate report Layer B (HTML standalone)}
\usage{
render_layer_b_report(lb, out_path = NULL, template = NULL)
}
\arguments{
\item{lb}{\code{layer_b_result} (output di \code{\link[=build_layer_b_results]{build_layer_b_results()}} o
\code{\link[=load_layer_b]{load_layer_b()}}).}

\item{out_path}{character path output HTML. Default: \code{lb$dir/layer_b_report.html}.}

\item{template}{character path al .qmd template. Default: incluso nel pacchetto.}
}
\value{
invisible(out_path).
}
\description{
Compila il template \code{inst/templates/layer-b-report.qmd} per produrre un
HTML auto-contenuto (embed-resources) che aggrega tutti i case studies
Layer B (selection table, config snapshot, per-cluster summary card +
PNG plots + top-gene table + narrative).
}
\details{
Quarto viene eseguito con \code{execute_dir = lb$dir} in modo che le immagini
referenziate nel template via path relative \code{cluster_id/plot.png}
vengano risolte da Pandoc e correttamente base64-embeddate
(\code{embed-resources: true}). Path assoluti farebbero scattare l'auto-prepend
\verb{./} di Pandoc, generando \verb{<img src="./home/user/...">} non risolvibili.
}
\section{ATTENZIONE prima di ogni render (lezione pagata, Task 9 2026-08-05)}{

Quarto esegue il template in un sotto-processo R che carica \code{simulomicsr}
via \code{library()} (il pacchetto INSTALLATO), non la sessione di sviluppo con
\code{devtools::load_all()}. Se il template chiama una funzione interna via
\code{simulomicsr:::nome_funzione()} (es. \code{.corpus_overview_from_bundle()}), un
render fallisce con "object 'nome_funzione' not found" finche' non si
esegue \code{devtools::install(quick = TRUE, upgrade = "never")} PRIMA. Non
basta il \code{load_all()} della sessione corrente: il gap non si manifesta
finche' il template chiama solo funzioni esportate.
}

