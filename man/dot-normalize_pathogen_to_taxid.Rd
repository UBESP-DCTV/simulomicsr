% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage3-name-recovery.R
\name{.normalize_pathogen_to_taxid}
\alias{.normalize_pathogen_to_taxid}
\title{Normalizza un termine-patogeno/PAMP testuale a un ID ontologico canonico.}
\usage{
.normalize_pathogen_to_taxid(term, ontology_env = NULL)
}
\arguments{
\item{term}{character(1) termine estratto dai metadati GEO.}

\item{ontology_env}{environment da \code{.load_ontology_dicts()}, oppure
NULL per saltare il ramo taxdump (i rami costanti funzionano comunque).}
}
\value{
lista con campi \code{id}, \code{name}, \code{source}.
\code{id} e' "CHEBI:\if{html}{\out{<int>}}" | "NCBITaxon:\if{html}{\out{<int>}}" | "STR:\if{html}{\out{<slug>}}" | NA.
\code{source} e' uno tra "PAMP_WHITELIST" | "PATHOGEN_VERNACULAR" |
"PATHOGEN_TAXID" | "STR_FALLBACK" | "NO_TERM".
}
\description{
Flusso (precisione decrescente):
\enumerate{
\item Guardia su input mancante/vuoto -> id=NA, source="NO_TERM".
\item Normalizza con \code{.normalize_biological_mention} (rimuove separatori,
porta a minuscolo) per uniformare le chiavi dei dizionari costanti.
\item Genera candidati tolleranti a dose/tempo/verbo/via via
\code{.extract_compound_candidates} (stringa intera + versione spogliata +
token). Cosi' "LPS exposed for 24 hours"->"lps", "poly(I:C) 10 ug/ml"->
"poly(i:c)", "SARS-CoV-2 infected"->"sars-cov-2".
\item Per OGNI candidato (chiavi curate, match esatto sicuro): guardia
host-species (\code{.HOST_SPECIES_STOPLIST}: token "human"/"mouse"/... ->
salta il candidato, mai fabbricare un patogeno da una specie-ospite),
poi \code{.PAMP_WHITELIST} -> id="CHEBI:\if{html}{\out{<int>}}" source="PAMP_WHITELIST",
poi \code{.PATHOGEN_VERNACULAR} -> id="NCBITaxon:\if{html}{\out{<taxid>}}"
source="PATHOGEN_VERNACULAR". PAMP/vernacolo precedono il generic-check di
proposito (vernacoli brevi come "tb"/"flu").
\item Guardia host-species sul termine intero -> id="STR:\if{html}{\out{<slug>}}",
source="STR_FALLBACK" (difensiva, prima del taxdump; "organism: human"
non deve produrre NCBITaxon:9606).
\item Termine biologico generico (\code{.is_generic_biological}) ->
id="STR:\if{html}{\out{<slug>}}", source="STR_FALLBACK".
\item Taxdump (\code{.taxonomy_lookup_name} + \code{.taxonomy_rollup_to_species},
richiede \code{ontology_env} non-NULL) SOLO sui candidati MULTI-PAROLA
(frasi) + termine intero -- i nomi scientifici sono frasi; token singoli
NON interrogano il taxdump (precisione) -> id="NCBITaxon:\if{html}{\out{<taxid>}}",
source="PATHOGEN_TAXID".
\item Miss -> id="STR:<slugify(term)>", source="STR_FALLBACK".
}
}
\details{
PAMP e vernacolo (ramo 3) sono COSTANTI e non richiedono \code{ontology_env}:
e' possibile chiamare la funzione senza env per i PAMP e il vernacolo.
Il ramo taxdump (6) viene saltato se \code{ontology_env} e' NULL.
}
\keyword{internal}
