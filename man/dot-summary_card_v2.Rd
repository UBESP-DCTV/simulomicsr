% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer-b-summary-card.R
\name{.summary_card_v2}
\alias{.summary_card_v2}
\title{Scheda riassuntiva v2: cinque domande, non un dump di campi tecnici}
\usage{
.summary_card_v2(
  riga,
  bersagli_trovati = character(0),
  confronti_imperfetti = NULL,
  soglia_dominanza = 0.5,
  bersagli_attesi = character(0)
)
}
\arguments{
\item{riga}{data.frame/tibble di \strong{una} riga del deliverable annotato
(\code{annotate_stage4_deliverable()}). Colonne usate: \code{cluster_id},
\code{contrast_entity} (ID grezzo del contrasto, opzionale),
\code{contrast_entity_label}, \code{k_effective}, \code{k_kish}, \code{I2_med}, \code{n_sig},
\code{quota_top1}, \code{studio_dominante}, \code{materiale_misto}, \code{coherence_verdict},
e due colonne opzionali non ancora nel deliverable standard --
\code{run_id}, \code{sha256} -- usate se presenti, altrimenti dichiarate \code{N/A}.
Se \code{riga} ha piu' di una riga, si usa solo la prima.}

\item{bersagli_trovati}{character vector di bersagli attesi ritrovati,
gia' formattati per la lettura (es. \code{"SMAD7 +1.41"} -- notazione col
punto decimale, coerente con \code{sprintf("\%+.2f", ...)} in
\code{build_layer_b_results()}, non con la virgola italiana). \code{character(0)}
(default) se nessuno o non calcolato per questo gruppo.}

\item{confronti_imperfetti}{list opzionale con \code{n}, \code{tot} (interi) e
\code{peso} (frazione 0-1) dalla rilettura dei confronti poolati (vedi
\code{docs/findings/2026-08-05-confronti-imperfetti.md}). \code{NULL} (default) se
non misurato per questo gruppo -- dichiarato "non misurato", mai omesso
in silenzio.}

\item{soglia_dominanza}{quota di peso oltre la quale lo studio piu'
pesante viene segnalato come dominante. \strong{Usata SOLO come ripiego},
quando \code{riga} non porta gia' la colonna \code{dominato} -- che
\code{compute_pooling_effectiveness()} calcola a monte con la stessa soglia
di default (0,5) e scrive nel deliverable annotato
(\code{R/stage4-deliverable-annotation.R}). Quando la colonna c'e', vince
sempre lei: ricalcolare qui con un valore indipendente farebbe
divergere la scheda in silenzio se il default a monte cambiasse.}

\item{bersagli_attesi}{character vector (puo' essere vuoto) dei bersagli
DICHIARATI dal chiamante, \strong{indipendentemente} da quanti sono stati
ritrovati (vedi \code{bersagli_attesi_provider} di \code{\link[=build_layer_b_results]{build_layer_b_results()}}).
Serve SOLO a distinguere in riga4 "nessuna aspettativa dichiarata" da
"aspettative dichiarate ma non ritrovate" -- due affermazioni diverse
che, prima di questo parametro, producevano la stessa frase (vedi
\code{\link[=.lb_bersagli_trovati_txt]{.lb_bersagli_trovati_txt()}}). \code{character(0)} (default) -> "nessuna
aspettativa dichiarata", coerente col comportamento precedente quando
nessun bersaglio era dichiarato.}
}
\value{
\code{character(1)}, markdown.
}
\description{
La scheda precedente (\code{.build_summary_card()}, invariata da questo task,
vedi sotto) elenca campi tecnici -- \code{safety_min: 0.06}, \verb{direction_applied distribution: none: 19687}, \verb{Anchor: contrast/gain x HGNC:11766} -- che non
dicono nulla a un biologo, e sceglie il "Top gene" per solo FDR (per
TGF-beta1 e' la cornulina, che con quella via non c'entra). Questa funzione
e' la sostituzione: cinque righe che rispondono a cinque domande --- che
cosa e' stato confrontato, su quanti studi e quanto pesano davvero, quanto
concordano, che cosa si trova, quanti confronti hanno un difetto di
disegno --- seguite da un blocco \code{PROVENANCE} dove finiscono gli identificativi interni (\code{cluster_id},
l'ID grezzo del contrasto, \code{run_id}, sha256): servono a chi verifica, non a
chi legge la scheda accanto alla figura.
}
\details{
A differenza di \code{.build_summary_card()}, che legge \code{cluster_pooled} riga
per riga e scrive un file su disco, questa funzione e' PURA: prende la riga
gia' calcolata del deliverable annotato (\code{annotate_stage4_deliverable()})
e ritorna una stringa markdown. Non tocca il disco -- chi la chiama decide
dove va (file, chunk Quarto, ...).

Nessun taglio silenzioso: quando un dato manca (etichetta, verdetto,
run_id, bersagli, confronti imperfetti) la scheda lo DICE, non lo omette e
non ripiega senza dirlo su un identificativo tecnico.
}
\keyword{internal}
