# `job_status()` lives in `R/jobs.R` together with the other registry / sync /
# progress helpers.
#
# This file remains intentionally code-free so the package keeps a stable source
# layout without reviving an old duplicate implementation.
