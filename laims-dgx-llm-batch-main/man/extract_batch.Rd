% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_batch.R
\name{extract_batch}
\alias{extract_batch}
\title{Run an end-to-end extraction batch workflow}
\usage{
extract_batch(
  records = NULL,
  id_col = NULL,
  text_col = NULL,
  prompt_template = NULL,
  schema = NULL,
  model = NULL,
  model_profile = NULL,
  bundle = NULL,
  generation = list(temperature = 0, max_tokens = 1024),
  submit = TRUE,
  wait = FALSE,
  config = NULL,
  ...
)
}
\arguments{
\item{records}{A \code{data.frame}/tibble with one row per record.}

\item{id_col}{Column containing unique record ids.}

\item{text_col}{Column containing source text.}

\item{prompt_template}{Prompt template or prompt file path.}

\item{schema}{Output schema expected from the model.}

\item{model}{Canonical model size to use: \code{"20B"} or \code{"120B"}.}

\item{model_profile}{Deprecated alias for \code{model}. Still accepted for
backward compatibility.}

\item{bundle}{Optional prebuilt bundle created by \code{\link[=create_bundle]{create_bundle()}}. Supply
either \code{bundle} or the raw \code{records} + creation arguments, not both.}

\item{generation}{Named list of generation parameters.}

\item{submit}{Logical; if \code{TRUE}, submit immediately. If \code{FALSE}, return the
bundle and rendered job assets without launching the job.}

\item{wait}{Logical; if \code{TRUE}, poll until a terminal state when possible.}

\item{config}{A \code{laims_dgx_config} object. If \code{NULL}, uses \code{\link[=dgx_config]{dgx_config()}}.}

\item{...}{Additional arguments forwarded to \code{\link[=create_bundle]{create_bundle()}} and
\code{\link[=submit_job]{submit_job()}}.}
}
\value{
Depending on mode, a bundle object, submit plan, job handle, or
collected results object.
}
\description{
High-level convenience wrapper that orchestrates bundle creation, optional
remote submission, status handling, and result collection for the standard
many-records / one-schema extraction use case.
}
\details{
This function is intentionally opinionated: it treats structured JSON output
as the target artifact and assumes batch execution on a DGX reachable only
via SLURM and Singularity/Apptainer.
}
