% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runtime.R
\name{ensure_runtime}
\alias{ensure_runtime}
\title{Ensure the remote runtime image is available}
\usage{
ensure_runtime(
  model = "20B",
  sif_path = NULL,
  dry_run = FALSE,
  force = FALSE,
  config = NULL,
  model_profile = NULL
)
}
\arguments{
\item{model}{Canonical model size to ensure: \code{"20B"} or \code{"120B"}.}

\item{sif_path}{Optional explicit remote \code{.sif} path. When supplied, this
bypasses managed mode and validates the path as an external runtime.}

\item{dry_run}{Logical; if \code{TRUE}, inspect and report what would happen
without building or mutating the remote runtime.}

\item{force}{Logical; if \code{TRUE}, treat the managed runtime as stale and
rebuild/refresh it unless \code{dry_run = TRUE}.}

\item{config}{A \code{laims_dgx_config} object. If \code{NULL}, uses \code{\link[=dgx_config]{dgx_config()}}.}

\item{model_profile}{Deprecated alias for \code{model}. Still accepted for
backward compatibility.}
}
\value{
An object of class \code{laims_dgx_runtime}.
}
\description{
Resolve the container runtime used for remote SLURM jobs. In \code{managed}
mode, \code{ensure_runtime()} fingerprints the package-managed assets under
\code{inst/runtime}, chooses one of the two canonical managed runtime identities
for the selected \code{model} (\verb{official-20b} or \verb{official-120b}), checks the
remote manifest/current \code{.sif}, and refreshes it when missing or stale.
The concrete bootstrap targets are \verb{<user_root>/runtime/official-20b/} and
\verb{<user_root>/runtime/official-120b/}. In \code{external} mode, it validates an
existing remote \code{.sif} path without attempting a build.
}
