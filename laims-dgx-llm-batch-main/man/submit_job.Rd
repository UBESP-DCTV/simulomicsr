% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/submit_job.R
\name{submit_job}
\alias{submit_job}
\title{Submit a SLURM batch job for DGX LLM inference}
\usage{
submit_job(
  bundle,
  submit = TRUE,
  slurm = list(),
  remote_bundle_dir = NULL,
  remote_output_dir = NULL,
  sif_path = NULL,
  template = NULL,
  stage_fun = NULL,
  config = NULL
)
}
\arguments{
\item{bundle}{Bundle object or local bundle path created by
\code{\link[=create_bundle]{create_bundle()}}.}

\item{submit}{Logical; if \code{TRUE}, run \code{sbatch}. If \code{FALSE}, only render the
script and return the command preview.}

\item{slurm}{Named list with scheduler parameters such as \code{partition},
\code{account}, \code{nodelist}, \code{nodes}, \code{mail_user}, \code{cpus}, \code{mem}, and \code{time}.
GPU count is fixed to 1 and is not user-configurable. \code{nodes} defaults to
1 and is enforced so every job stays on a single DGX node. \code{mail_user}
inherits from \code{dgx_config(mail_user = ...)} unless overridden here; it is
optional for dry runs but required for real submission.}

\item{remote_bundle_dir}{Remote path where the bundle should be staged.}

\item{remote_output_dir}{Remote path where outputs should be written.}

\item{sif_path}{Optional path to the Singularity/Apptainer image on the
remote side. An explicit value always wins and is treated as an external
runtime override. Otherwise runtime resolution follows \code{dgx_config()}
(\code{managed} or \code{external}). For \code{submit = FALSE}, managed mode reports
whether the runtime still needs an \code{ensure_runtime()} step.}

\item{template}{Path to a SLURM template. Defaults to the installed package
template under \code{inst/templates/submit_slurm.sh}.}

\item{stage_fun}{Optional function responsible for copying the bundle to the
remote environment.}

\item{config}{A \code{laims_dgx_config} object. If \code{NULL}, uses \code{\link[=dgx_config]{dgx_config()}}.}
}
\value{
A \code{laims_dgx_job} object if submitted, otherwise a
\code{laims_dgx_submit_plan} object.
}
\description{
Render a generic SLURM submission script from the package template, stage a
bundle to the remote environment, and call \code{sbatch}.
}
\details{
The goal is to keep the user-facing interface in R while using the cluster's
native batch workflow underneath.
}
