% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_bundle.R
\name{create_bundle}
\alias{create_bundle}
\title{Create a reproducible batch bundle for DGX inference}
\usage{
create_bundle(
  records,
  id_col,
  text_col,
  prompt_template,
  schema,
  model = "20B",
  model_profile = NULL,
  generation = list(temperature = 0, max_tokens = 1024),
  bundle_dir = NULL,
  metadata = list(),
  config = NULL
)
}
\arguments{
\item{records}{A \code{data.frame} or tibble with one row per input record.}

\item{id_col}{Name of the column containing unique record identifiers.}

\item{text_col}{Name of the column containing the text to process.}

\item{prompt_template}{Length-1 character string or path to a prompt file.}

\item{schema}{A list or JSON schema-like object describing the expected
structured output.}

\item{model}{Canonical model size to use: \code{"20B"} or \code{"120B"}.}

\item{model_profile}{Deprecated alias for \code{model}. Still accepted for
backward compatibility.}

\item{generation}{Named list of generation parameters.}

\item{bundle_dir}{Optional output directory. If \code{NULL}, a timestamped
directory is created under the local state directory.}

\item{metadata}{Optional named list with project/use-case metadata.}

\item{config}{A \code{laims_dgx_config} object. If \code{NULL}, uses \code{\link[=dgx_config]{dgx_config()}}.}
}
\value{
An object of class \code{laims_dgx_bundle}.
}
\description{
Prepare a local bundle directory containing records, prompt template,
schema, model selection, and run metadata for a one-shot SLURM batch job.
}
\details{
The public interface is intentionally simple: for now users choose only
\code{"20B"} or \code{"120B"}, while the package resolves the official model/runtime
details internally.
}
