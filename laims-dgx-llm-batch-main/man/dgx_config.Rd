% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgx_config.R
\name{dgx_config}
\alias{dgx_config}
\title{Create or retrieve DGX client configuration}
\usage{
dgx_config(
  login_user = Sys.getenv("LAIMS_DGX_LOGIN_USER", unset = ""),
  state_dir = Sys.getenv("LAIMS_DGX_STATE_DIR", unset = tools::R_user_dir("laimsdgxllm",
    which = "data")),
  ssh_key_path = Sys.getenv("LAIMS_DGX_SSH_KEY_PATH", unset = ""),
  user_root = Sys.getenv("LAIMS_DGX_USER_ROOT", unset = ""),
  remote_base_dir = Sys.getenv("LAIMS_DGX_REMOTE_BASE_DIR", unset = ""),
  mail_user = Sys.getenv("LAIMS_DGX_MAIL_USER", unset = ""),
  cpus = as.integer(Sys.getenv("LAIMS_DGX_CPUS", unset = "4")),
  mem = Sys.getenv("LAIMS_DGX_MEM", unset = "32G"),
  partition = Sys.getenv("LAIMS_DGX_PARTITION", unset = "dgx12cluster"),
  account = Sys.getenv("LAIMS_DGX_ACCOUNT", unset = "dctv_dgx"),
  nodelist = Sys.getenv("LAIMS_DGX_NODELIST", unset = "poddgx02"),
  nodes = as.integer(Sys.getenv("LAIMS_DGX_NODES", unset = "1")),
  runtime_mode = Sys.getenv("LAIMS_DGX_RUNTIME_MODE", unset = "managed"),
  sif_path = Sys.getenv("LAIMS_DGX_SIF_PATH", unset = ""),
  runtime_name = Sys.getenv("LAIMS_DGX_RUNTIME_NAME", unset = "laims-runtime"),
  poll_interval = 5,
  connect_timeout = 10,
  ssh_args = c("-o", "BatchMode=yes", "-o", paste0("ConnectTimeout=", connect_timeout)),
  registry_path = fs::path(state_dir, "registry.sqlite"),
  ssh_bin = Sys.getenv("LAIMS_DGX_SSH_BIN", unset = "ssh"),
  scp_bin = Sys.getenv("LAIMS_DGX_SCP_BIN", unset = "scp"),
  squeue_bin = Sys.getenv("LAIMS_DGX_SQUEUE_BIN", unset = "squeue"),
  sacct_bin = Sys.getenv("LAIMS_DGX_SACCT_BIN", unset = "sacct"),
  apptainer_bin = Sys.getenv("LAIMS_DGX_APPTAINER_BIN", unset =
    "/cm/shared/apps/singularity/4.2.0/bin/singularity")
)
}
\arguments{
\item{login_user}{SSH username used to reach the DGX login host.}

\item{state_dir}{Local persistent state directory.}

\item{ssh_key_path}{Optional path to the local SSH private key. When omitted,
the package derives \verb{~/.ssh/<login_user>.key}.}

\item{user_root}{Remote per-user root directory. When unset, it is derived
from \code{remote_base_dir} when possible, otherwise defaults to
\verb{/mnt/projects/dctv/dgx/<login_user>}.}

\item{remote_base_dir}{Base directory for remote runs. When unset, it is
derived as \verb{<user_root>/runs}.}

\item{mail_user}{Optional default notification email for SLURM jobs. This
stays user-supplied (no package personal default), but can be configured
once here so \code{submit_job()}/\code{extract_batch()} inherit it automatically.}

\item{cpus}{Default CPU count for submitted jobs.}

\item{mem}{Default host RAM request for submitted jobs.}

\item{partition}{Default SLURM partition for submitted jobs.}

\item{account}{Default SLURM account for submitted jobs.}

\item{nodelist}{Default SLURM nodelist for submitted jobs.}

\item{nodes}{Default SLURM node count for submitted jobs. Fixed to 1 so
submitted jobs remain on a single DGX node.}

\item{runtime_mode}{Runtime resolution strategy: \code{"managed"} or
\code{"external"}. Defaults to \code{"managed"}; set \code{"external"} when you want
\code{dgx_config()} to validate and reuse a pre-existing remote \code{.sif}.}

\item{sif_path}{Optional default path to an externally managed runtime SIF on
the remote cluster. Used when \code{runtime_mode = "external"} or when an
explicit \code{sif_path} override is supplied to \code{\link[=ensure_runtime]{ensure_runtime()}} or
\code{\link[=submit_job]{submit_job()}}. Defaults to \code{Sys.getenv("LAIMS_DGX_SIF_PATH")}.}

\item{runtime_name}{Optional basename used for package-managed runtime assets
and SIF versions on the remote side. Advanced/backward-compatible knob;
most users should ignore it.}

\item{poll_interval}{Default polling interval for live progress.}

\item{connect_timeout}{SSH connect timeout in seconds.}

\item{ssh_args}{Extra SSH CLI arguments.}

\item{registry_path}{Path to the local SQLite registry.}

\item{ssh_bin}{SSH binary.}

\item{scp_bin}{SCP binary.}

\item{squeue_bin}{\code{squeue} command available on the remote login node.}

\item{sacct_bin}{\code{sacct} command available on the remote login node.}

\item{apptainer_bin}{Apptainer/Singularity binary path or command name.
Defaults to the cluster-specific Singularity binary known to work on the
target DGX SLURM environment.}
}
\value{
An object of class \code{laims_dgx_config}.
}
\description{
\code{dgx_config()} bootstraps the local state directory and the SQLite registry
used to recover submitted runs across R sessions and laptop restarts.
}
\details{
The package hardcodes the stable DGX login host
\code{logindgx.hpc.ict.unipd.it}; users only need to provide the SSH username and,
optionally, an explicit key path. When \code{ssh_key_path} is omitted, the package
derives \verb{~/.ssh/<login_user>.key}.
}
